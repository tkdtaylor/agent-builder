# Codex Agent Adapters

These files are Codex-facing adapters for the Claude agent setup in this repo.
They intentionally do not copy the full prompts. The source of truth stays in
`.claude/agents/`, and `AGENTS.md` defines the short phrases that make Codex
load and follow the matching role automatically.

