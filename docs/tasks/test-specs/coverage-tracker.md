# Test Coverage Tracker

**Project:** agent-builder

## Rules

- Test specs are written **before** implementation begins — no exceptions
- A task is **not** "complete" because the feat commit landed and tests passed. See the verification ladder below.
- Each row maps a task ID to its spec file, current test status, and the verification level achieved

## Coverage

| Task ID | Feature | Spec file | Tests written | Status | Verified by |
|---------|---------|-----------|---------------|--------|-------------|
| 001 | Walking skeleton & project setup | 001-walking-skeleton-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (audit 2026-06-05, post spec-correction) + L6 operator-observed: `go run ./cmd/agent-builder version` -> `agent-builder 0.0.0-scaffold` exit 0; bare `go run ./cmd/agent-builder` -> CLI usage block, exit 2; `go test -count=1 ./internal/supervisor -run 'TestVersionSet\|TestRunDispatchesOneTaskAndLogsLifecycle\|TestRunRejectsMissingDispatchDependencies'` -> `ok`. |
| 002 | Gate orchestrator core + Verdict model | 002-gate-orchestrator-core-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L2 unit-test-only/no runtime surface (re-confirmed audit 2026-06-05 in a standalone verify commit; original ✅ was bundled into the task-031 feat commit): `go test -count=1 ./internal/gate -run 'TestVerify\|TestNewRejects\|TestGateHasNoSkipSurface'` -> `ok github.com/tkdtaylor/agent-builder/internal/gate 0.002s` |
| 003 | Native Go gate steps (build/vet/test/gofmt) | 003-gate-go-checks-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test ./internal/gate/... -run TestGoChecks` -> `ok github.com/tkdtaylor/agent-builder/internal/gate` |
| 004 | golangci-lint gate step | 004-gate-golangci-lint-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test ./internal/gate/... -run TestGolangciLint -count=1` -> `ok github.com/tkdtaylor/agent-builder/internal/gate` |
| 005 | dep-scan blocking gate step | 005-gate-dep-scan-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test ./internal/gate/... -run TestDepScan -count=1` -> `ok github.com/tkdtaylor/agent-builder/internal/gate` |
| 006 | code-scanner blocking gate step | 006-gate-code-scanner-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test ./internal/gate/... -run TestCodeScanner -count=1` -> `ok github.com/tkdtaylor/agent-builder/internal/gate` |
| 007 | Fitness F-003: supervisor isolation | 007-fitness-supervisor-isolation-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L6 operator-observed: `make fitness-supervisor-isolation` -> `PASS fitness-supervisor-isolation: supervisor import graph contains no executor/LLM/web-fetch packages.` Negative import-chain failure named `github.com/tkdtaylor/agent-builder/internal/webfetch`; `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.` |
| 008 | Fitness F-001: no-docker dev-env refs | 008-fitness-no-docker-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L6 operator-observed: `make fitness-no-docker` -> `PASS fitness-no-docker: no forbidden dev-environment references found.` Negative fixtures caught root `Dockerfile`, `dockerfile`, `DOCKERFILE`, `docker-compose.yml`, `Docker-compose.yml`, and `docker run` content; `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.` |
| 009 | Fitness F-002: gate is blocking (no skip) | 009-fitness-gate-blocking-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L6 operator-observed: `make fitness-gate-blocking` -> `PASS fitness-gate-blocking: no verification gate bypass affordances found.` Negative fixtures caught CLI flag, scanner skip env-var, and early-return bypass; `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.` |
| 010 | Roadmap task-source reader (read-only) | 010-roadmap-task-source-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test -count=1 ./internal/tasksource/...` -> `ok github.com/tkdtaylor/agent-builder/internal/tasksource`; `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.` |
| 011 | Task status writer (status-only) | 011-task-status-writer-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test -count=1 ./internal/tasksource/... ./tests/...` -> `ok github.com/tkdtaylor/agent-builder/tests/tasksource` |
| 012 | Agent loop state machine | 012-agent-loop-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test -count=1 ./tests/loop/...` -> `ok github.com/tkdtaylor/agent-builder/tests/loop` |
| 013 | Escalation + retry-N + stop condition | 013-escalation-retry-policy-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test ./tests/loop/... -run 'TestRetryPolicy|TestEscalation' -count=1` -> `ok github.com/tkdtaylor/agent-builder/tests/loop` |
| 014 | Podman containment profile | 014-podman-containment-profile-test-spec.md | ✅ | ✅ | L3: `make check` -> `All checks passed.`; `make fitness` -> `Fitness checks passed.` L6 real-host (podman 5.7.0, rootless, runsc-rootless wrapper, ext4, 2026-06-17; container-probe suite regression run after tasks 050/051): `bash containment/execution-box/run.sh --gate-tools containment/execution-box/gate-tools --worktree . --probe` -> **exit 0**; TC-001 PASS (worktree+scratch writable, rootfs write denied), TC-002 PASS (non-root 1000:1000, no effective capabilities, scratch HOME), TC-004 PASS (rootfs writes denied), TC-005 PASS (Gate toolchain available in-box; no container-engine socket/env reachable), TC-003 PASS (memory cap visible in-box; cpu/pids verified host-side under runsc). |
| 015 | Default-deny egress allowlist | 015-egress-allowlist-test-spec.md | ✅ | ✅ | L3: `make check` -> `All checks passed.`; `make fitness` -> `Fitness checks passed.` L6 real-host (podman 5.7.0, rootless, runsc-rootless wrapper, ext4, 2026-06-17): `bash containment/execution-box/run.sh --gate-tools containment/execution-box/gate-tools --worktree . --egress-probe` -> **exit 0**; TC-003 PASS (allowlisted `api.github.com:443` connect succeeded), TC-004 PASS (non-allowlisted `example.com:443` blocked), TC-004 PASS (direct IP `1.1.1.1:443` blocked). Default-deny egress enforced rootless; the rootless egress sidecar + runtime fixes that unblocked this probe landed in tasks 049/050/051 (ADR 029 idempotent nftables + writable egress-state; ADR 030 egress workload under runc since gVisor's gofer cannot join a rootless pod userns). |
| 016 | Tiered OCI runtime selection seam | 016-tiered-runtime-seam-test-spec.md | ✅ | ✅ | L3: `make check` -> `All checks passed.` L6 real-host (podman 5.7.0, rootless, runsc-rootless wrapper, ext4, 2026-06-17): `bash containment/execution-box/run.sh --gate-tools containment/execution-box/gate-tools --worktree . --print-runtime-plan` -> `TC-016 PLAN: workload=agent runtime=runsc source=default`; `bash …/run.sh --gate-tools containment/execution-box/gate-tools --worktree . --runtime runsc --probe` -> **exit 0** with TC-001..005 PASS (incl. TC-003 under runsc). Runtime-tier seam confirmed: agent→runsc standalone works; the egress-path runc override (ADR 030) is scoped to the egress pod path only and does NOT affect the standalone runsc probe (regression-confirmed alongside 014/033). |
| 017 | Supervisor dispatch-one-task lifecycle | 017-supervisor-dispatch-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5 fake dispatch harness: `go test -count=1 -v ./internal/supervisor -run TestRunDispatchesOneTaskAndLogsLifecycle` -> `event=box.created` -> `event=loop.started` -> `event=box.torn_down` |
| 018 | Wall-clock timeout / runaway kill | 018-wall-clock-kill-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test -count=1 -v ./internal/supervisor -run 'TestRunTimeout(UsesConfiguredDeadlineAndKillsBox\|RecordsTimedOutOutcome)'` -> `event=box.kill.timeout` + `"outcome":"timed-out"` |
| 019 | Run log collection (audit-trail seam) | 019-run-log-collection-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test -v ./tests/supervisor -run TestRunRecordStreamsOutputAndPersistsAfterTeardown -count=1` -> `TC-003-Persist-After-Teardown sample persisted line: {"box_id":"box-019"` |
| 020 | exec-sandbox run() adapter seam | 020-exec-sandbox-adapter-seam-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L2/L3: `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.`; unit-test-only; no runtime surface |
| 021 | sandbox-runtime backing adapter | 021-sandbox-runtime-adapter-test-spec.md | ✅ | 🟡 | L3: `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.`; Task 030 L6 blocked locally: `command -v srt` -> exit 1/no output; `command -v bwrap` -> `/usr/bin/bwrap`; live harness blocked before `srt`: `env AGENT_BUILDER_LIVE_SRT=1 go test -count=1 -v ./tests/sandbox -run TestSandboxRuntimeLiveHarness_TC002_TC003` -> `snap-confine has elevated permissions and is not confined but should be. Refusing to continue to avoid permission escalation attacks` |
| 022 | Claude Code CLI executor adapter | 022-claude-cli-executor-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5 stubbed CLI harness: `go test -count=1 -v ./tests/executor -run TestClaudeCLIRunInvokesSubprocessAgainstWorktreeAndCapturesBranch` -> `Result.Branch = task/022-claude-cli-executor`, `Result.OK == true`. **L6 confirmed (2026-06-17, subscription auth):** the live Phase-0 capstone drove the real Claude Code CLI executor on `CLAUDE_CODE_OAUTH_TOKEN` (no API key) — it produced branch `task/001-live-ok` that became real PR l6#4; `--- PASS (197.57s)` (see 032/061). Key-only/OAuth-only auth under the executor's temp HOME confirmed working. |
| 023 | CLI subcommand surface (run/version/verify) | 023-cli-subcommands-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L6 runtime-visible CLI checks: `agent-builder version`, `agent-builder verify <clean-repo>`, `agent-builder verify <failing-repo>`, and `agent-builder bogus` |
| 024 | Web-ingestion/tool-call boundary seam | 024-ingestion-tool-call-boundary-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test -count=1 ./internal/ingestion/... ./tests/ingestion/...` -> `ok github.com/tkdtaylor/agent-builder/tests/ingestion`; `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.` |
| 025 | armor guard adapter | 025-armor-guard-adapter-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `go test -count=1 ./internal/armor/... ./tests/armor/...` -> `ok github.com/tkdtaylor/agent-builder/tests/armor`; `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.` |
| 026 | armor on web-ingestion / tool-call path | 026-armor-ingestion-wiring-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5 (re-confirmed audit 2026-06-05 in a standalone verify commit; original ✅ was bundled into the task-031 feat commit): `go test -count=1 ./tests/executorharness -run 'TestHarnessProducerConsumerTraceCoversLivePath\|TestArmorGuardedHarnessProducerConsumerTraceCoversLiveExecutorPath'` -> `ok` (TC-005 armor producer-consumer trace) |
| 027 | Executor ingestion/tool-call harness | 027-executor-ingestion-tool-harness-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5 (re-confirmed audit 2026-06-05 in a standalone verify commit; original ✅ was bundled into the task-031 feat commit): `go test -count=1 ./tests/executorharness -run 'TestHarnessProducerConsumerTraceCoversLivePath\|TestArmorGuardedHarnessProducerConsumerTraceCoversLiveExecutorPath'` -> `ok` (TC-005 producer-consumer trace) |
| 028 | Default run wiring | 028-default-run-wiring-test-spec.md | ✅ | ✅ | **L6 PASSED (2026-06-17, subscription auth — `CLAUDE_CODE_OAUTH_TOKEN`, no API key):** the live Phase-0 capstone drove the real agent-builder binary's `run` wiring end-to-end green — task picked → box launched → Claude executor → full in-box gate (build/vet/test/gofmt/golangci-lint/dep-scan/code-scanner all PASS) → real PR opened on l6 (#4, CLOSED+branch-deleted by cleanup) → run record persisted; `--- PASS (197.57s)`. All prior blockers resolved: box-launch (058/ADR032), in-box dep-scan (061/ADR034), and the dead-credit API key (sidestepped by subscription OAuth, 059/060/ADR033). Prior history below. L5: `go test -count=1 -v ./tests/cli ./tests/supervisor -run 'TestRuntimeRunWiresPhase0Pipeline|TestRunConfigFailures'` -> `TC-005 runtime run completed one configured task and persisted run_finished`; final gate `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.` **L6 BLOCKED (2026-06-17):** live `go run … run` against an l6-clone worktree fails at `supervisor: create box: sandbox: create probe exited 2`. Root cause: `box.Create` (internal/runtime/run.go) runs `Command:["/bin/true"]`, but the execution-box image is `ENTRYPOINT ["/bin/sh"]`, so the box runs `sh /bin/true` and the shell interprets the ELF binary as a script (`/bin/true: line 0: ELF: not found`). Latent product bug — `box.Create` was only ever exercised against the L5 *fake* launcher, never the real image. Fix needed: liveness probe should be `["-c","true"]` (or override the image entrypoint). After that, the host-side gate (`make check` on the l6 clone) and real publish are still untested live. **UPDATE 2026-06-17 (task 058):** box-launch bug FIXED (`box.Create` now `["-c","true"]`, ADR 032) — live `go run … run` now clears `box.Create` and reaches the Claude executor. Now blocked at the executor on an **external** issue: `executor error: Claude CLI "claude" failed: exit status 1: Not logged in · Please run /login`; a direct `ANTHROPIC_API_KEY=<key> claude -p …` returns **`Credit balance is too low`** — the provided API key's account has insufficient Claude credit, so no real agent run can complete. Secondary nuance to confirm once credit exists: the executor isolates Claude in a temp `HOME` (claude_cli.go) so it must auth via `ANTHROPIC_API_KEY` alone; verify this CLI version authenticates by key in a fresh HOME (the isolated run reported "Not logged in" while a real-HOME run reached the credit check). **L6 residual: add Claude API credit (and confirm key-only auth under temp HOME), then re-run.** |
| 029 | Claude executor ingestion control | 029-claude-ingestion-control-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (audit 2026-06-05) + L5: `go test -count=1 -v ./tests/executor ./tests/executorharness -run 'TestClaude.*Ingestion|TestArmorGuardedHarnessProducerConsumerTraceCoversLiveExecutorPath'` -> `claude_cli_test.go:534: TC-005 Claude executor web/tool route is reviewed or disabled fail-closed` |
| 030 | Runtime isolation evidence | 030-runtime-isolation-evidence-test-spec.md | ✅ | 🟡 | Evidence ledger updated only with observed blockers: execution-box probes for tasks 014/015/016 all exited `execution-box: podman unavailable on PATH`; `command -v srt` exited 1/no output; `command -v bwrap` -> `/usr/bin/bwrap`. L6 remains pending on a host with rootless Podman, runsc, and `srt`. |
| 031 | Verification ledger cleanup | 031-verification-ledger-cleanup-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (audit 2026-06-05; docs/ledger-only, no runtime surface) + L5 docs ledger consistency: `scripts/check-task-state.sh` -> `OK: every task is tracked in exactly one state directory.`; final gate `env PATH=/tmp/agent-builder-tools:$PATH make check` -> `All checks passed.` |
| 032 | Phase 0 end-to-end acceptance | 032-phase0-end-to-end-acceptance-test-spec.md | ✅ | ✅ | **L6 PASSED (2026-06-17, subscription auth — `CLAUDE_CODE_OAUTH_TOKEN`, no API key):** `go test ./tests/e2e -run TestLivePhase0EndToEndAcceptance_TC032` → `--- PASS (197.57s)`; `TC-054-01 live capstone accepted: task selected, real branch produced, PR recorded, gate passed, cleanup confirmed`; real PR l6#4 (`feat: complete task 001 — create LIVE_OK.txt`) opened then CLOSED+branch-deleted by t.Cleanup. The full in-box gate ran green (build/vet/test/gofmt/golangci-lint/dep-scan/code-scanner). All prior blockers resolved: box-launch (058/ADR032), in-box dep-scan (061/ADR034), dead-credit API key (subscription OAuth via 059/060/ADR033). Prior history below. fake-provider L5: `go test -count=1 -v ./tests/e2e -run TestPhase0EndToEndAcceptance` -> `TC-001 Phase 0 accepted: task selected, branch produced, PR recorded, gate passed, run record persisted`. Live capstone `TestLivePhase0EndToEndAcceptance_TC032` added (task 054) + fixture clones l6 (task 057). **L6 BLOCKED (2026-06-17):** live run reached real Claude/box setup (17s) then failed at `supervisor: create box: sandbox: create probe exited 2` — same root cause as 028: `box.Create` uses `Command:["/bin/true"]` but the execution-box image is `ENTRYPOINT ["/bin/sh"]` → `sh /bin/true` → `ELF: not found`. Latent product bug (box.Create never run against the real image; L5 used a fake launcher). Fix: liveness probe `["-c","true"]` or entrypoint override; then the host-side gate (`make check` on the l6 clone) and real publish remain untested live. The publish leg itself IS proven (see 034: real PR l6#2). **UPDATE 2026-06-17 (task 058):** box-launch FIXED (ADR 032); the live capstone now clears `box.Create` and reaches the Claude executor, where it fails on an **external** blocker — `Not logged in · Please run /login` under the orchestrator, and a direct `ANTHROPIC_API_KEY=<key> claude -p` returns **`Credit balance is too low`** (the provided key's account is out of Claude credit). No real agent run can complete until credit is added. **L6 residual: add Claude API credit (and confirm key-only auth works under the executor's temp HOME, claude_cli.go), then re-run the capstone — the box-launch, fixture-clone, and publish legs are all already proven.** |
| 033 | Execution-box Gate toolchain | 033-execution-box-gate-toolchain-test-spec.md | ✅ | ✅ | L5: `go test -count=1 -v ./tests/containment ./tests/cli -run 'TestExecutionBoxGateToolchain\|TestVerifyMissingGateTool'` -> `ok github.com/tkdtaylor/agent-builder/tests/cli`. L6 real-host (podman 5.7.0, rootless, runsc-rootless wrapper, ext4, 2026-06-17): `bash containment/execution-box/run.sh --gate-tools containment/execution-box/gate-tools --worktree . --probe` -> **exit 0**; TC-005 PASS: execution-box Gate toolchain available for in-box verification (gate-in-box: golangci-lint/gods/code-scanner mounted + on PATH), alongside TC-001/002/003/004 PASS. |
| 034 | Branch and PR publication | 034-branch-pr-publication-test-spec.md | ✅ | ✅ | L5 fake git/gh publication harness: `go test -count=1 -v ./tests/publisher ./tests/e2e -run 'TestBranchPRPublication\|TestPublisherFailureDoesNotMarkDone'` -> `TC-001 verified branch published as PR artifact`; publisher failure/redaction evidence -> `TC-003 publisher failure preserved task as not done`. **L6 real-host (2026-06-17, rootless host, gh authed as tkdtaylor, l6 sandbox remote):** `env AGENT_BUILDER_LIVE_PUBLISH=1 AGENT_BUILDER_LIVE_PUBLISH_REMOTE=l6 go test -count=1 -v ./tests/publisher -run TestLiveBranchPRPublication_TC034` -> `--- PASS` (11.29s); real PR opened `https://github.com/tkdtaylor/agent-builder-l6-sandbox/pull/2`, then closed + branch deleted by t.Cleanup (PR now CLOSED, no stray branch on l6). Live publisher path (real `git push` + real `gh pr create --fill`) confirmed end-to-end. Live test added in task 053; repo-base fix (fetch remote main + branch off it) in task 056. |
| 035 | Podman sandbox.Runner adapter | 035-podman-sandbox-adapter-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16, after resolving 4 BLOCK findings: EXEC_BOX_PIDS_LIMIT emission, allowlist justification-comment assertion, errors.Is sentinel wrapping, process-group kill on timeout) + L5: `go test -count=1 -v ./internal/sandbox/podman/...` -> 7 pass / 1 skip (TC-035-06 live probe gated on AGENT_BUILDER_LIVE_PODMAN); TC-035-05 elapsed 0.10s confirms subprocess group is killed at deadline. L6 N/A — no runtime surface without real Podman + runsc on host. |
| 036 | Podman adapter swap in default run wiring | 036-podman-adapter-swap-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16; all 4 TCs verified — `go list -deps ./internal/runtime/...` shows zero `sandboxruntime`, stale-var rejection test, spec edits in configuration/interfaces/SPEC) + L5: `go test -count=1 -v ./tests/cli -run 'TestRuntimeRunWiresPodmanAdapter\|TestNoSrtInvocation'` -> `TC-036-02 runtime uses Podman adapter; sandboxruntime not imported` and `TC-036-02 no srt subprocess; AGENT_BUILDER_SANDBOX_RUNTIME absent from run contract` (harness lives in `./tests/cli`; no `tests/runtime` dir created). L3: `make fitness` -> `PASS fitness-no-srt: internal/runtime does not import sandboxruntime`. L6 N/A — no real Podman on host. |
| 037 | Phase 1 end-to-end acceptance | 037-phase1-end-to-end-acceptance-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16; all 4 TCs + 2 edge cases verified, phase0 TC-005 doc-honesty regression green, internal/cmd untouched) + L5: `go test -count=1 -v ./tests/e2e -run TestPhase1EndToEndAcceptance` -> `TC-037-01 Phase 1 accepted: task selected, Podman containment used, no srt invocation, run record clean` (real `agent-builder` binary drives a fake Podman launcher via `AGENT_BUILDER_EXEC_BOX_LAUNCHER`, fake Claude/git/gh, Gate-passing worktree; run record carries `containment=podman launcher=<path>` and ZERO `srt`/`sandbox-runtime`/`AGENT_BUILDER_SANDBOX_RUNTIME` occurrences in stdout/stderr/record; failed-probe edge surfaces containment failure on the CLI). L3: `make fitness` -> `fitness-no-srt` passes. Phase0 TC-005 doc-honesty assertion updated to current truth (Podman is the run backend; `srt` framed as removed/historical, not pending). L6 pending: live `runsc` + provisioned execution-box Gate-toolchain directory; `AGENT_BUILDER_LIVE_PODMAN=1 ... TestPhase1LivePodman` skips when Podman/`runsc` unavailable, fails when Gate-toolchain dir missing — on this host Podman is installed but `runsc` + gate-tools are not provisioned. |
| 038 | audit.AuditEvent taxonomy + Sink seam | 038-audit-event-sink-seam-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16; all 6 TCs verified with real assertions, 16/16 tests pass, `go list -deps ./internal/audit/...` confirms stdlib-only leaf). L2: `go test -count=1 ./internal/audit/...` -> `ok github.com/tkdtaylor/agent-builder/internal/audit 0.002s`. L3: `make fitness` -> `Fitness checks passed.` Governing ADR 026 (not ADR 025): typed leaf scaffolding (`AuditEvent` taxonomy, `Sink{Append,Seal}`, `FakeSink`), no `ChainWriter` or writer in this task; production `BlockSink` adapter is task 039. unit-test-only; no runtime surface (L5/L6 N/A). |
| 039 | audit.BlockSink CLI adapter onto shipped audit-trail block | 039-audit-chain-writer-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16; all 7 TCs verified, real-binary L5 ran, stdlib-only leaf confirmed; lone ⚠ on TC-039-04 Seal-deferred-error edge judged structurally inapplicable in the v0 synchronous per-emit design). L5 real-binary path: `go test -count=1 -v ./internal/audit/... -run 'TestBlockSink\|TestBlockSinkEmitArgs\|TestBlockSinkChainVerifies'` -> 12 new tests pass (28 total in package); TC-039-07 L5 PASS: `audit-trail verify` output `{"valid":true,"tamper_detected_at":null,"message":"chain intact"}` on 7-event chain; `go list -deps ./internal/audit/...` shows stdlib-only leaf; `make check` -> `All checks passed.` Governing ADR 026: `BlockSink` CLI adapter (typed `AuditEvent`→`audit-trail emit` argv mapping; `context`/`refs` deferred to IPC transport per ADR 026 Option B). L2: `go test -count=1 ./internal/audit/...` -> `ok github.com/tkdtaylor/agent-builder/internal/audit 0.020s`. L3: `make fitness` -> `Fitness checks passed.` |
| 040 | audit.VerifyChain — block-severity integrity gate | 040-audit-verify-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16, re-verify after strengthening 2 tests: TC-040-04 unreadable-logfile now asserts non-nil `ErrVerifierUnavailable`; TC-040-05 now an in-process `go list -deps` leaf assertion, not a smoke log). L5 real-binary path ran: `go test -count=1 -v ./internal/audit/... -run 'TestVerifyChain'` -> 8 tests pass; TC-040-06 L5 PASS: intact chain `Valid=true TamperedAt=<nil> Message="chain intact"` (exit 0); tampered chain `Valid=false TamperedAt=2 Message="content hash mismatch (tampered)"` (exit 1); missing binary → `ErrVerifierUnavailable` (not "valid"). `go list -deps ./internal/audit/...` stdlib-only leaf confirmed. `make check` -> `All checks passed.` `make fitness` -> `Fitness checks passed.` Governing ADR 026: `VerifyChain` consumes the block's own `audit-trail verify` via `os/exec`; tamper detection delegated entirely to the block; `Valid==false` → block-severity gate; unavailable verifier → `ErrVerifierUnavailable` (never "valid"). |
| 041 | Wire audit.Sink into supervisor action events | 041-audit-supervisor-wiring-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16, opus; all 8 TCs + 6 REQs verified — Seal-before-teardown ordering enforced by an onTeardown hook on success+failure paths, F-003 `go list -deps ./internal/supervisor/...` independently re-run showing internal deps = audit/gate/sandbox only, raw-stream separation + never-silently-skip confirmed). Governing ADR 026: the supervisor projects typed action events through `audit.Sink` (`RunStreams.Audit`) alongside the unchanged 019 RunRecord raw stream and Seals the sink before containment teardown on success and failure; the production `audit.BlockSink` (behind `AGENT_BUILDER_AUDIT_RECORD`, binary resolved + path checked writable fail-fast before dispatch) produces the chain; task 040's `VerifyChain` confirms a produced run's chain. L5 real-binary path ran: `go test -count=1 -v ./tests/e2e ./tests/supervisor ./internal/runtime -run 'TestAuditChainEndToEnd\|TestSupervisorAuditProjection'` (note: the `TestSupervisorAuditProjection*` tests live in `./internal/runtime` — include that package or they report no-tests-to-run) — TC-041-05 L5 PASS: `VerifyChain valid=true`, action sequence `[containment pick attempt verify publish finish]` on a real `agent-builder run` chain produced via `$HOME/Code/Public/audit-trail/audit-trail`; unresolvable-binary and unwritable-path runs fail before dispatch with no chain produced. L2: `go test ./...` all pass. L3+F-003: `make check` -> `All checks passed.` incl. `PASS fitness-supervisor-isolation: supervisor import graph contains no executor/LLM/web-fetch packages`; `go list -deps ./internal/supervisor/...` shows `internal/audit` and no executor/LLM/web package. |
| 042 | fitness-audit-isolation (F-005) | 042-fitness-audit-isolation-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16; all 4 TCs verified, forbidden-token set confirmed byte-identical to F-003, synthetic FAIL path proven genuinely reachable via data-driven awk/grep branches). L6 operator-observed: `make fitness-audit-isolation` -> `PASS fitness-audit-isolation: internal/audit import graph contains no executor/LLM/web-fetch or audit-trail-block packages and the supervisor's audit dependency drags none in.` exit 0; synthetic FAIL demonstrated for executor import and audit-trail module import (non-zero exit, FAIL line naming offending package); `make fitness` -> `Fitness checks passed.`; `make check` -> `All checks passed.` F-005 row added to `docs/spec/fitness-functions.md` with ADR 026 source-of-truth link. Governing ADR 026. |
| 043 | L6 host preflight doctor | 043-l6-host-preflight-doctor-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16; all 5 TCs verified with real grep/exit-code assertions, snap-confine keyed to exact string vs generic FAIL distinguished, l6-preflight confirmed absent from check:/fitness: prereqs). L5 (highest achievable without a live host): `bash scripts/tests/l6-preflight-test.sh` → `=== Results: 5 passed, 0 failed ===` exit 0; TC-043-01 (all-present→READY exit 0), TC-043-02 (missing runsc→MISSING+NOT READY exit 1), TC-043-03 (snap-confine srt→FAIL with snap hint; generic-fail srt→FAIL no snap-confine hint), TC-043-04 (rootless=false→FAIL with rootless hint; info exits non-zero→FAIL), TC-043-05 (`make --dry-run l6-preflight` shows `bash scripts/l6-preflight.sh`; l6-preflight in .PHONY; not in check: or fitness: prereqs). L3: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.` L6 residual: run `make l6-preflight` on provisioned host and observe READY. |
| 044 | L6 probe harness and evidence collector | 044-l6-probe-harness-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-16; all TCs verified with real grep/order/exit-code assertions; adjudicated the 9-vs-10-row spec ambiguity against the authoritative checklist — 10 distinct closing-order task IDs is correct, executor aligned honestly without bending the committed spec, test-spec text corrected 9→10 in this verify commit; coverage-tracker write-invariant confirmed — the script never writes to the tracker). L5 (highest achievable without a live host): `bash scripts/tests/l6-probe-test.sh` → `=== Results: 7 passed, 0 failed ===` exit 0; TC-044-01 (--dry-run emits 10 rows in closing order 014→015→016→021→030→022→028→033→034→032, exit 0), TC-044-02 (runsc absent→016+032 SKIP exit 0; srt absent→021+030+032 SKIP exit 0; gh absent→034+032 SKIP exit 0), TC-044-03 (evidence file 10 TASK- rows all with dry-run placeholder and status field), TC-044-03b (SKIP rows include prereq name in skip-reason), TC-044-04 (NOT READY preflight→non-zero exit + message referencing make l6-preflight, no probes invoked), TC-044-04b (--dry-run bypasses preflight gate, exit 0). L3: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.` `l6-probe` confirmed NOT in check: or fitness: prereqs. L6 residual: run `make l6-probe` on provisioned host after tasks 045+046 are merged (they fix the storage-opt, gate-tools, publish-remote, and srt-var bugs that block live runs); use evidence file to author `verify:` commits (human-reviewed). |
| 045 | Execution-box launcher: host-portable disk quota + fail-loud on launch failure | 045-execution-box-storage-quota-graceful-degrade-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17, twice — initial + after completing fail-loud on all podman create/run callsites incl. the mislabel-guard for legitimate non-zero workload exits; plus a follow-up real-host fix removing the podman-5.7-absent `.HostConfig.StorageOpt` inspect field). L5: `bash containment/execution-box/tests/storage-quota-test.sh` → 8 passed (quota applied/skipped+WARNING/empty-opt-out/podman-create+run exit 125 → named error/normal non-zero propagated/egress-probe variant). **L6 real host (podman 5.7.0, ext4, 2026-06-17):** `bash containment/execution-box/run.sh --worktree . --runtime runc --probe` → degraded-quota `WARNING` emitted, `TC-003 PASS: …(storage quota not enforced on this host)`, exit 0 — the box now launches on ext4 where it previously died with `overlay.size … only supported for backingFS XFS`. Governing ADR 027. L3: `make check`/`make fitness` green. |
| 046 | l6-probe.sh live-run wiring fixes | 046-l6-probe-wiring-fixes-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17, after strengthening 2 edge assertions: positive default gate-tools path via bash -x trace, and gh-absent precedence when PUBLISH_REMOTE set). L5 (highest achievable without a live host): `bash scripts/tests/l6-probe-test.sh` → `=== Results: 10 passed, 0 failed ===` exit 0; TC-046-01 (EXEC_BOX_GATE_TOOLS honored; no `--gate-tools ""` in argv), TC-046-02 (AGENT_BUILDER_PUBLISH_REMOTE threaded when set; SKIP with reason `AGENT_BUILDER_PUBLISH_REMOTE unset` when unset, exit 0), TC-046-03 (no `AGENT_BUILDER_SANDBOX_RUNTIME=srt` in any argv; 028 not skipped when srt absent + claude present; 021 still SKIP when srt absent), TC-046-04 regression guard (all TC-044 cases still green). L3: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.` L6 residual (operator-only, after task 045 merged + provisioned host): run `bash scripts/l6-probe.sh` live; confirm 014/015/016/033 pass the resolved gate-tools path without "Gate toolchain directory does not exist" error; confirm 034/032 skip gracefully when AGENT_BUILDER_PUBLISH_REMOTE unset; confirm 028/032 no longer trigger ADR 021 migration error. |
| 047 | Execution-box TC-016 runtime inspect portability | 047-execution-box-runtime-inspect-portability-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; mismatch-still-fails guard confirmed real, not always-pass). L5: `bash containment/execution-box/tests/storage-quota-test.sh` → `=== Results: 10 passed, 0 failed ===` exit 0; TC-047-01 (stub .OCIRuntime=runc + --runtime runc → TC-016 HOST: …runtime=runc, no FAIL, exit 0; template references .OCIRuntime not .HostConfig.Runtime), TC-047-02 (.OCIRuntime=runc + --runtime runsc mismatch → TC-016 FAIL + non-zero exit). L6: `bash containment/execution-box/run.sh --worktree . --runtime runc --gate-tools containment/execution-box/gate-tools --probe` → `TC-016 HOST: workload=agent runtime=runc` + `TC-016-RUNTIME PASS: workload=agent runtime=runc` + exit 0 on podman 5.7.0/ext4 host (2026-06-17). L3: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.` |
| 048 | Runtime-aware in-box resource-cap verification (gVisor) + probe-path launch-guard mislabel | 048-l6-runtime-aware-inbox-cap-verification-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; allowlist confirmed strictly `= "runsc"` (fail-closed for runc/kata/unknown), critical negative TC-048-03 drives `--probe` through stub podman with NanoCpus=0/PidsLimit=-1 → non-zero (no always-pass), host-side die checks untouched, probe-path mislabel three-way fork verified). L4: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.`; L5: `bash containment/execution-box/tests/storage-quota-test.sh` → `=== Results: 15 passed, 0 failed ===` (TC-048-01/02/03/04/05 all green, incl. TC-048-03 runsc+NanoCpus=0 → non-zero exit proving no always-pass, TC-048-04 kata+memory-only → strict fail). L6 real-host (podman 5.7.0, runsc/gVisor, ext4, 2026-06-17): `bash containment/execution-box/run.sh --worktree . --probe` (default runsc) → `TC-003 PASS: memory cap visible in-box; cpu/pids caps verified host-side under runsc (cpu=unknown pids=unknown)` exit 0; `bash containment/execution-box/run.sh --worktree . --runtime runc --probe` → `TC-003 PASS: cpu, memory, and pids cgroup limits are visible in-box` exit 0. Governing ADR 028. |
| 049 | Egress pod `--userns`/`--pod` conflict (rootless) | 049-egress-pod-userns-conflict-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; userns moved to pod-create, stripped from both members, common_args/non-pod path untouched, nftables/allowlist logic unchanged; REQ-049-06 spec-update is N/A — behaviors.md has no per-container-userns content). L5: `bash containment/execution-box/tests/userns-pod-test.sh` → `=== Results: 3 passed, 0 failed ===` exit 0; TC-049-01 (pod create has `--userns=keep-id`; sidecar run -d and workload run --pod have NO `--userns`; sidecar still has NET_ADMIN/read-only/cap-drop/no-new-privs), TC-049-02 (non-pod `--probe` container create still has `--userns=keep-id`), TC-049-03 (sidecar carries `--user 0:0`; workload carries `--user <uid>:<gid>`). Existing 15 storage-quota-test.sh cases still green. L4: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.` L6 real-host (podman 5.7.0, rootless, ext4, 2026-06-17): `bash containment/execution-box/run.sh --worktree . --egress-probe` → no `--userns and --pod cannot be set together` error; pod+sidecar START; downstream failure is nftables/NET_ADMIN under rootless (`TC-001 FAIL: nftables default-deny egress rules failed to apply` + `/egress-state/fail: Permission denied`); that is a separate rootless-egress follow-up, out of scope for task 049. Verified by: L5 harness `bash containment/execution-box/tests/userns-pod-test.sh` + L6 real-host egress probe past pod+sidecar start. |
| 050 | Rootless egress sidecar fix (idempotent nftables + writable egress-state) | 050-rootless-egress-sidecar-fix-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (all TCs 050-01/02/03 verified; REQ-050-01 ordering captured, REQ-050-02 allow/deny preserved, REQ-050-03 mode 0777 at launch, REQ-050-04 allow/deny logic/NET_ADMIN/cap-drop/resolved-mount unchanged; REQ-050-05 L6 past-sidecar confirmed; REQ-050-06 behaviors.md B-010 + interfaces.md --egress-probe updated with ADR 029 reference). L5: `bash containment/execution-box/tests/egress-rootless-test.sh` → `=== Results: 3 passed, 0 failed ===` exit 0; TC-050-01 (empty table `table inet agent_builder_egress { }` before `flush` before populated table; `TC-001 PASS` printed; `ready` marker written), TC-050-02 (populated table contains `set allowed_tcp4`, `type ipv4_addr . inet_service`, `flags interval`, `policy drop`, `oifname "lo" accept`, `ct state established,related accept`, `ip daddr . tcp dport @allowed_tcp4 accept`, `reject`), TC-050-03 (egress-state source dir mode is 0777 at sidecar launch, confirmed via `stat` in stub podman). Existing TC-049 userns-pod-test (3 pass) and TC-045/047/048 storage-quota-test (15 pass) green. L4: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.` L6 real-host (podman 5.7.0, rootless, ext4, 2026-06-17): `bash containment/execution-box/run.sh --worktree . --gate-tools <populated> --egress-probe` → sidecar applied the default-deny ruleset + wrote its readiness markers; the run advanced **past the sidecar** with NONE of the three sidecar-failure markers (no `…egress.nft:1: Error: No such file or directory`, no `/egress-state/fail: Permission denied`, no `egress sidecar did not become ready`), then hit the downstream `extra host entries must be specified on the pod` constraint — i.e. the two ADR-029 sidecar bugs are fixed. Per the corrected REQ-050-05, "advances past the sidecar" is this task's L6 bar; the downstream `--add-host`/runsc-pod constraints and the full `TC-003`/`TC-004 PASS` + exit-0 green (confirmed reachable under runc during this phase) are **task 051 under ADR 030**. Governing ADR 029. |
| 051 | Rootless egress path runs under runc (+ `--add-host` on pod-create) | 051-egress-path-runc-runtime-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; all 5 TC-051 L5 assertions backed by real assert_contains/assert_not_contains, incl. the strengthened TC-051-01 guard that the stale `agent-builder.runtime=runsc` label does NOT survive onto the egress workload; fail-loud guard fires before pod create; `--add-host` moved to pod create; non-pod path runtime unchanged; regression harnesses not weakened). L5 harness: `bash containment/execution-box/tests/egress-runtime-test.sh` → `=== Results: 5 passed, 0 failed ===` exit 0 (TC-051-01 default+egress→runc, no stale runsc label; TC-051-02 explicit-runsc+egress→die-loudly, no workload run; TC-051-03 explicit-runc+egress→runc no-die; TC-051-04 add-host on pod not workload; TC-051-05 non-pod--probe→runsc unchanged). Existing harnesses: `userns-pod-test.sh` → 3/3 (TC-049 green); `egress-rootless-test.sh` → 3/3 (TC-050 green); `storage-quota-test.sh` → 15/15 (TC-045/047/048 green). L4: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.` L6 real-host (podman 5.7.0, rootless, runsc-rootless wrapper, ext4, 2026-06-17): (a) `bash containment/execution-box/run.sh --worktree . --gate-tools <populated> --egress-probe` → **exit 0** with `TC-003 PASS: allowlisted connect succeeded: api.github.com:443 :: open`, `TC-004 PASS: non-allowlisted connect blocked: example.com:443`, `TC-004 PASS: direct IP bypass blocked: 1.1.1.1:443` — default-deny egress enforced rootless with the workload under runc; (b) `bash …/run.sh --worktree . --runtime runsc --egress-probe` → **exit 1** with `execution-box: explicit --runtime runsc is incompatible with the rootless egress path: gVisor's gofer cannot join a rootless pod's user namespace (ADR 030)…`. The egress probe (015) is green rootless. Governing ADR 030. |
| 052 | ADR 031 + doc honesty (no production code) | 052-adr031-l6-live-mode-doc-honesty-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; 18/18 assertions; BLOCK fixes confirmed — TC-052-02c/d now require runbook+checklist, spec/task committed). L5 doc-honesty sub-tests: `go test ./tests/e2e -run TestPhase0EndToEndAcceptance/TC-052 -count=1 -v` → 4 PASS (TC-052-01 ADR 031 exists + 9 content assertions; TC-052-02a/b no stale srt/argv; TC-052-02c/d live-command updates; TC-052-02e/f ANTHROPIC_API_KEY + host-side doc; TC-052-03 TC-005 regression green); `make check` → `All checks passed.` Doc-only task; no L6. |
| 053 | Live publisher test (TC-034) | 053-live-publisher-test-tc034-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; 4/4 TCs; TC-053-01 confirmed not-a-smoke-test — real git push + gh pr create + PRURL/PRID asserts + t.Cleanup; BLOCK doc-defect fixed efa9b78). L5: `go test -count=1 -v ./tests/publisher` → `TestBranchPRPublication --- PASS`, `TestPublisherFailureRedactsSecrets --- PASS`, `TestLiveBranchPRPublication_TC034 --- SKIP` (env flag unset); exit 0. `make check` → `All checks passed.` |
| 054 | Live capstone test (TC-032) + fixture helper | 054-live-capstone-test-tc032-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; 4/4 TCs; TC-054-01 confirmed not-a-smoke-test — real binary drive + exit/stdout/run-record asserts + t.Cleanup; skip/fatal discipline correct: t.Skipf for tool gaps, t.Fatalf for missing gate-tools). L5: `go test -count=1 -v ./tests/e2e -run TestLivePhase0EndToEndAcceptance_TC032` → `--- SKIP: TestLivePhase0EndToEndAcceptance_TC032 (0.00s)` exit 0 (env flag unset, TC-054-02 skip path confirmed); all existing e2e tests PASS (TestPhase0EndToEndAcceptance, TestPhase1EndToEndAcceptance); `make check` → `All checks passed.` L6 residual: operator sets `AGENT_BUILDER_LIVE_E2E=1 AGENT_BUILDER_LIVE_E2E_REMOTE=l6 ANTHROPIC_API_KEY=<real>` and runs the test on a provisioned host with real Claude, real git, real gh, real podman, and the l6 sandbox remote configured; expects TC-054-01 PASS with real PR URL logged and cleaned up. |
| 055 | Rewire l6-probe.sh 022/028/032/034 gating + fixture-seed helper | 055-rewire-l6-probe-sh-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; 5/5 TCs, no smoke; two prior BLOCKs fixed — fork-bomb hang from unguarded `tr`/`$(mktemp)` under stub PATH, and TC-055-04 now asserts real fixture files; no TC-044/046 regression weakened). L3: `make check` → `All checks passed.`; `make fitness` → `All fitness checks passed.`; L4 (dry-run baseline): `bash scripts/l6-probe.sh --dry-run` → `10 rows` (closing order 014 015 016 021 030 022 028 033 034 032), `exit 0`. L5 validation harness: `bash scripts/tests/l6-probe-test.sh` → `=== Results: 14 passed, 0 failed ===` (TC-044-01/02/03/03b/04/04b/05 all green; TC-046-01/02/03 green; TC-055-01/02/03 green; TC-055-04 new real test invokes seed_live_fixture() and verifies task-root/worktree structure; TC-055-05 regression), `exit 0` (regression guard ensures all TC-044 and TC-046 still pass; ANTHROPIC_API_KEY detection fixed from `tr -d ' '` pipe to pure-bash parameter expansion `${_key// /}` to work under PATH-restricted stub binaries in tests). All critical assertions in TC-055-01 (022/028 env contract + no srt) and TC-055-02/03 (034/032 use live-test names + AGENT_BUILDER_LIVE_* vars) verified. L6 residual: operator `make l6-probe` on provisioned host with real Podman, runsc, gate-tools, Claude, gh, git remote, and ANTHROPIC_API_KEY; confirms probes 022/028 PASS (or skip due to missing host prerequisites, not Anthropic key), 034/032 PASS with real PR publication and live e2e acceptance test. |
| 056 | Live publisher test bases branch on remote default branch | 056-live-test-base-on-remote-main-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; 3/3 TCs; fetch+FETCH_HEAD branch confirmed, no --depth, skip discipline intact). L6 confirmed live: probe opened real PR https://github.com/tkdtaylor/agent-builder-l6-sandbox/pull/2 then closed+deleted branch (PR now CLOSED, no stray branch). L5: `go test ./tests/publisher -count=1` → live test SKIP (flag unset), fakes PASS, `ok` exit 0; `make check` → `All checks passed.` Fix: live test fetches `<remote> main` (full) and branches off `FETCH_HEAD` so `gh pr create --fill` resolves. L6 residual: operator runs the live probe (see 034 row — confirmed PR opened+cleaned). |
| 057 | Live fixtures clone remote | 057-live-fixtures-clone-remote-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; 7/7 TCs, all 5 prior BLOCKs fixed — clone-failure now fatal when remote configured (no silent git-init fallback), origin→l6 rename so the publisher push target exists, dry-run no longer clones, TC-055-04 sources the REAL seed_live_fixture and asserts `l6` remote + `git merge-base --is-ancestor l6/main HEAD` (runs for real since l6 is configured), filteredEnv strips LIVE_* flags). Clone+rename+branch+push+PR path also validated manually against l6 (PR #3 opened+cleaned). L3: `make check` → `All checks passed.`; `make fitness` → `Fitness checks passed.` L4+L5: `go test -count=1 -v ./tests/e2e` → `TestPhase0EndToEndAcceptance PASS`, `TestPhase1EndToEndAcceptance PASS`, `TestLivePhase0EndToEndAcceptance_TC032 SKIP` (flag unset, TC-054-02 regression); `bash scripts/tests/l6-probe-test.sh` → `=== Results: 14 passed, 0 failed ===` exit 0 (TC-055-04 regression updated to assert real git repo with commit; all TC-044/046 cases preserved); `bash scripts/l6-probe.sh --dry-run` → 10 rows in closing order, exit 0. Implementation: `seed_live_fixture()` now clones the l6 remote (full clone, no shallow depth) and renames `origin`→`l6` so the publisher's push target exists; a clone failure when the remote IS configured is FATAL (`return 1`, no silent fallback) — the bare-init minimal-module fallback is used ONLY when no remote is configured (CI). `newLiveCapstoneFixture(t)` likewise clones `AGENT_BUILDER_LIVE_E2E_REMOTE` (default l6), renames origin→remote, and t.Fatalf on clone failure when configured. Both fixtures resolve remote URL dynamically via `git remote get-url`, no hardcoded URLs. `filteredEnv()` now blocks the live gating flags (`AGENT_BUILDER_LIVE_E2E`, `AGENT_BUILDER_LIVE_PUBLISH`, etc.) so the gate (`go test ./...`) can safely set them without causing recursion when the binary runs go test internally — TC-057-03 documented. Ambient git identity preserved (no `user.email` override). L6 residual: operator sets `AGENT_BUILDER_LIVE_E2E=1 AGENT_BUILDER_LIVE_E2E_REMOTE=l6 ANTHROPIC_API_KEY=<real>` on a provisioned host and runs capstone live tests; expects TC-032 PASS with real PR URL logged and cleaned up; probes 022/028 PASS with real Claude agent drives; full `bash scripts/l6-probe.sh` execution PASS on real host. |
| 058 | Box liveness probe /bin/sh-compatible | 058-box-liveness-probe-sh-compatible-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; 4/4 TCs, fix genuine, fakes now model the sh entrypoint so the bug class is caught at L5). Fixes the latent box-launch bug blocking 022/028/032 (ADR 032): `box.Create` probe `["/bin/true"]`→`["-c","true"]` (image is `ENTRYPOINT ["/bin/sh"]`, so `sh /bin/true` read the ELF as a script → exit 2); the Podman fake launchers now `exec /bin/sh "$@"` to model the real entrypoint (would have caught this at L5). L5: `make check` → `All checks passed.` (wiring fakes log `cmd=-c true`). L6 observed (this host, podman 5.7.0+runsc 2026-06-17): `run.sh … -- /bin/true` → exit 2; `run.sh … -- -c true` → **exit 0**. |
| 062 | Adopt exec-sandbox block as default run() backend | 062-adopt-exec-sandbox-run-backend-test-spec.md | ✅ | ✅ | **L6 CAPSTONE PASSED on the exec-sandbox block backend (2026-06-18, commit 358ccb7): `TestLivePhase0EndToEndAcceptance_TC032` --- PASS (87s) — Claude ran in-box, real PR opened+cleaned on l6 sandbox, full host-side gate green (go build/vet/test/gofmt/golangci-lint/dep-scan/code-scanner); run record `containment=exec-sandbox`. spec-verifier APPROVE.** L2: `go test ./...` → `ok` all packages incl. `internal/sandbox/execsandbox` (TC-062-01/02/04/05 existing + TC-062-03 new SandboxID/Tier/Status surface assert PASS); new TC-062-06 wiring tests `TestRuntimeRunWiresExecSandboxBackend` + `TestPodmanBackendReachableViaConfig` both PASS; TC-062-07 worktree validation unit tests (nonexistent/file-not-dir paths fail loud); TC-062-09 marshalling test asserts workdir field set. L3: `make fitness` → `PASS fitness-exec-sandbox-default: internal/runtime wires execsandbox as the default run backend` + all 6 fitness checks pass. L4: all CI green. **Spec updated in same commit:** configuration.md (added AGENT_BUILDER_EXEC_SANDBOX_BIN env var), interfaces.md (Result type extended with SandboxID/Tier/Status/Degraded fields + new execsandbox backend section documenting Worktree→run.workdir mapping). L5 harness: `go test -count=1 -v ./internal/sandbox/execsandbox/...` → `TestExecSandboxParseResult` passes TC-062-03 (SandboxID==sbx-abc123, Tier==bubblewrap, Status==clean surfaced); TC-062-07 `TestExecSandboxWorktreeValidation` validates reject paths loud; TC-062-09 marshalling test workdir assertion; `go test -count=1 -v ./tests/cli/... -run 'TestRuntimeRunWiresExecSandboxBackend|TestPodmanBackendReachableViaConfig'` → 2/2 PASS (TC-062-06 backend selection when env var set + fallback reachability when unset); `make check` → `All checks passed.` (all 7 fitness checks pass). **L6 adapter harness (TC-062-08/09 automatable, pending, requires real exec-sandbox block binary + host worktree mount):** `AGENT_BUILDER_LIVE_EXEC_SANDBOX=1 AGENT_BUILDER_EXEC_SANDBOX_BIN=$HOME/Code/Public/exec-sandbox/bin/exec-sandbox go test -count=1 -v ./internal/sandbox/execsandbox/... -run TestExecSandboxLive` → runs 3 subtests: bubblewrap_timeout_fires (TC-062-08), gvisor_clean_run (TC-062-08), bubblewrap_worktree_mount_read_write (TC-062-09 — seeds file, reads it, writes new file, verifies host persistence). **L6 full capstone (TC-062-08 non-automatable):** pending live run with `AGENT_BUILDER_LIVE_E2E=1 AGENT_BUILDER_LIVE_E2E_REMOTE=l6 CLAUDE_CODE_OAUTH_TOKEN=<token> AGENT_BUILDER_EXEC_SANDBOX_BIN=$HOME/Code/Public/exec-sandbox/bin/exec-sandbox go test -count=1 -v ./tests/e2e -run TestLivePhase0EndToEndAcceptance_TC032` (requires subscription auth); capstone fixture updated to pass-through env var to spawned binary so block backend is selected if configured. Governed by ADR 035. |
| 063 | Complete exec-sandbox block integration (toolchain, egress, command translation) | 063-complete-exec-sandbox-block-integration-test-spec.md | ✅ | ✅ | **L6 CAPSTONE PASSED on the block backend (2026-06-18, commit 358ccb7): same `TestLivePhase0EndToEndAcceptance_TC032` --- PASS — this is the completion of 062 that makes the block actually run agent-builder's gate.** spec-verifier APPROVE (TC-063-01..07). L5: `renderCommand` `sh -c` translation (probe `["-c","true"]`→payload `"true"`, was exit 127); toolchain forwarding via `FileRead{paths}` (GOROOT + gate-tools) + `run.env` PATH=GOROOT/bin:gate-tools — live test `TestExecSandboxLiveToolchain` PASS (`go` resolves in-box: `/snap/go/11200/bin/go`, `go version go1.26.4`); egress `EgressAllowlist`→`wiring.origin_map`, malformed entry (no `:port`) **fails loud** before binary invoke; empty allowlist→empty origin_map. `make check` → All checks passed. **Spec updated same commit:** interfaces.md (toolchain/egress forwarding contract), configuration.md (`AGENT_BUILDER_EXEC_SANDBOX_GOROOT`, `AGENT_BUILDER_GATE_TOOLS`). Depends on exec-sandbox tasks 003 (`run.workdir`) + 004 (`FileRead{paths}` + `run.env`). Governed by ADR 035. Note: the in-box gate is host-side (`runCommandStep`); the host must have gate-tools on PATH incl. `code-scanner`. |
| 061 | Fix in-box dep-scan gate step | 061-inbox-dep-scan-fix-test-spec.md | ✅ | ✅ | L2: `go test ./...` → `ok` (all 18 test packages green, incl. `internal/gate` TC-061-01/02/03/04 rewritten + passing). L3: `make fitness` → `All fitness checks passed.` (F-001/F-002/F-003/F-004/F-005 green, baseline unchanged). **Spec updated same commit (ADR 034):** configuration.md lines 32/34/71 (gods→dep-scan); interfaces.md line 75 (direct dep-scan invoke + no-go.sum pass contract) + lines 548/549 (toolchain lists); behaviors.md B-005 (no-go.sum+present semantics); data-model.md line 85 (output naming). **All god→dep-scan mutations complete:** run.sh help line 15, probe.sh line 43, gate-toolchain.manifest line 12, all 4 containment shell tests (egress-rootless/userns-pod/egress-runtime/storage-quota), all Go tests (execution_box_gate_toolchain/branch_pr_publication/run_wiring/cli_test.go with go.sum added to fixture), and all spec files. L5 validation harness: `make check` → **All checks passed.** (TC-061-01/02/03/04 + CLI TC-003 missing-tool test exercises the new semantics with go.sum present + missing dep-scan tool). **L6 PASSED (TC-061-07; 2026-06-17, this host, claude 2.1.150, subscription auth — `ANTHROPIC_API_KEY` unset, only `CLAUDE_CODE_OAUTH_TOKEN`):** the live capstone `TestLivePhase0EndToEndAcceptance_TC032` ran the full in-box gate — `PASS go build/vet/test/gofmt/golangci-lint/dep-scan/code-scanner` — and went green end-to-end: real PR opened on l6 (#4 `feat: complete task 001 — create LIVE_OK.txt`, since CLOSED + branch deleted by cleanup), `--- PASS (197.57s)`. spec-verifier APPROVE (7/7 TCs; cli TC-003 non-vacuous with go.sum). The dep-scan step now passes in-box; code-scanner (the next step) was already green. |
| 060 | Live capstone accepts subscription OAuth token | 060-live-capstone-oauth-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; TC-060-01/02/03 real asserting code; tracker honest re TC-060-04). L5: `make check` → `All checks passed.` + `Fitness checks passed.`; no-env capstone → SKIP (TC-054-02 discipline intact). Threads `CLAUDE_CODE_OAUTH_TOKEN` through the live capstone fixture: skip-guard skips only when BOTH credentials blank (TC-060-01), `liveCapstoneFixture.env()` forwards the OAuth token alongside the API key (TC-060-02), `filteredEnv()` blocks the ambient OAuth token so the fixture forwards exactly what its map sets (symmetric with ANTHROPIC_API_KEY). Governed by ADR 033. **L6 auth-path observed live (2026-06-17, this host, claude 2.1.150, ANTHROPIC_API_KEY unset):** ran the capstone binary with only `CLAUDE_CODE_OAUTH_TOKEN` — box launched, Claude authenticated on the subscription and ran a real ~64s task (created + committed `LIVE_OK.txt`, l6-clone HEAD `feat: complete task 001`), **no `Not logged in` / `Credit balance is too low`**. Subscription auth is proven end-to-end through the box. **TC-060-04 full gate-green-capstone NOT reached:** the in-box gate's `gofmt -l .` step failed on two PRE-EXISTING gofmt-dirty files in the l6 sandbox baseline (`internal/audit/blocksink_test.go` misaligned struct-field comments, `tests/e2e/live_phase0_e2e_test.go`) — the gate working as designed on a dirty baseline, unrelated to auth or this task. Full end-to-end green (+ real PR) is blocked on gofmt-cleaning the l6 baseline (a sandbox-maintenance follow-on), not on code in this repo. |
| 064 | ADR-036 vault integration decision | 064-adr036-vault-integration-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; 22/22 sub-assertions TC-064-01..05). L5: all 9 task-harness grep assertions pass (Status/Context/Decision/Consequences/ADR-035-ref/proxy/injection_mode/secret_refs/vault_socket/resolve/inject); TC-064-01..05 all pass via grep on ADR file; `make check` → `All checks passed.` Verified by: L5 harness grep + make check. |
| 065 | SecretSource seam (pure refactor) | 065-secret-source-seam-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; 7/7 TC-065-01..07, pure-refactor behavior preserved — OAuth-preferred-when-both logic intact). L5: `go test -count=1 ./internal/secrets/... ./internal/executor/... ./internal/runtime/...` → `ok` (all 3 packages). Leaf check: `go list -deps ./internal/secrets/...` contains no other agent-builder/internal imports (stdlib+self only). e2e: `go test -count=1 ./tests/e2e/... -run TestPhase0EndToEndAcceptance` → `ok`. `make check` → `All checks passed.` (TC-065-01..07 all covered). Verified by: L5 harness — all four harness commands green. |
| 066 | Vault token brokering via proxy mode | 066-vault-token-brokering-test-spec.md | ✅ | 🟡 | code merged on task/066. L5: `go test -count=1 ./internal/vault/... ./internal/secrets/... ./internal/sandbox/... ./internal/runtime/...` → all `ok`; `make check` → `All checks passed.`; `make fitness` → `All fitness checks passed.`; fake-provider `go test ./tests/e2e/... -run TestPhase0EndToEndAcceptance` → `ok` with vault unconfigured (opt-in). TC-066-03 (buildRunRequest wiring + zero-value regression guard) and TC-066-04 (Request.Wiring compiles, Runner unchanged) pass at L5 with a stub binary. spec-verifier APPROVE (2026-06-19). **TC-066-01/02 RUN LIVE against the real vault binary (`cargo build --release` → `$HOME/Code/Public/vault/target/release/vault`, built 2026-06-19) and PASS:** `AGENT_BUILDER_LIVE_VAULT=1 AGENT_BUILDER_VAULT_BIN=… go test -v ./internal/vault/... -run 'TestVaultClientPutResolveRoundTrip|TestVaultDaemonLifecycle'` → `--- PASS: TestVaultClientPutResolveRoundTrip (0.10s)` + `--- PASS: TestVaultDaemonLifecycle (0.10s)` → `ok …/internal/vault` — put/resolve round-trip works, resolve returns an opaque handle never containing the plaintext, daemon Start/Ping/Stop clean. (Fixed a test-helper bug: `startDaemon`'s `defer cancel()` killed the daemon's `exec.CommandContext` before the round-trip ran; bound cancel+Stop to `t.Cleanup`. Production `startVault` uses `context.Background()`, unaffected.) Leaf invariant: `go list -deps ./internal/vault/` → vault-only (stdlib); `internal/secrets` imports only `internal/vault` — no cycle. **L6 capstone (TC-066-05/06) + feasibility probe (TC-066-07) PENDING OPERATOR RUN — needs real git/GitHub/OAuth creds (credentials = external blocker); harness command in the task Verification plan. Row stays 🟡 until the in-box brokering capstone is observed.** Verified by: L5 — vault client/lifecycle live round-trip (real binary) + in-repo wiring/e2e harness green; L6 brokering capstone not yet reached. |
| 067 | ADR-037 signed-checkpoint integration decision | 067-adr-signed-checkpoint-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; 26/26 sub-assertions TC-067-01..05). L5: all 14 official harness grep assertions pass (Status/Decision/Consequences/ADR-026/checkpoint-create/checkpoint-verify/seal/Ed25519/vault/out-of-scope/AGENT_BUILDER_AUDIT_CHECKPOINT/fail-fast/configuration.md/interfaces.md); all 14 extended TC assertions pass (four env vars individually, VerifyChain, one-per-run, PEM, resolveAuditBin, Rekor, vault-not-prerequisite); `make check` → `All checks passed.` Verified by: L5 harness grep + make check. |
| 068 | Checkpoint signer seam, config, and creation at seal | 068-checkpoint-signer-seam-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; 19/19 sub-assertions TC-068-01..08, incl. the load-bearing tampered-chain guard: `checkpointCalls==0` when `IsTampered()==true`, supervisor owns the guard before CreateCheckpoint). L5: `go test -count=1 ./internal/audit/... ./internal/runtime/... ./internal/supervisor/...` → all `ok`; leaf check `go list -deps ./internal/audit/...` → `PASS-leaf` (no non-audit internal imports); `go test -count=1 ./tests/e2e/... -run TestPhase0EndToEndAcceptance` → `ok`; `make check` → `All checks passed.`. **L6 real-binary observation (2026-06-19):** audit-trail binary built (`go build -o audit-trail ./...` at ~/Code/Public/audit-trail); the EXACT argv CheckpointSigner builds — `checkpoint create --logfile … --log-id agent-builder-proof --signing-key <ed25519 PEM> --out checkpoint.json` — run against the real binary produced a valid Ed25519-signed checkpoint JSON (exit 0; `"algorithm":"ed25519"` signature over the chain head). Confirms the CreateCheckpoint seam is correct against the real `audit-trail`. (Gated `TestCheckpointSignerRealBinary` self-skips: the binary has no `keygen` verb — verbs are serve\|emit\|verify\|checkpoint\|rotate — a test-harness keygen assumption, not a CheckpointSigner defect; the manual run confirms the seam.) Verified by: L5 harness (4 cmds green) + L6 real-binary observation of a signed checkpoint file. |
| 069 | verify-checkpoint CLI surface | 069-verify-checkpoint-surface-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; 16/16 sub-cases TC-069-01..06). L5: `go test -count=1 ./internal/audit/... ./tests/cli/...` → `ok` (both packages); leaf check `go list -deps ./internal/audit/... \| grep agent-builder/internal/ \| grep -v internal/audit$` → `PASS-leaf`; `make fitness-audit-isolation` → `PASS fitness-audit-isolation: …`; `go test -count=1 ./tests/e2e/... -run TestPhase0EndToEndAcceptance` → `ok`; `make check` → `All checks passed.`. TC-069-01 (shape/constructors compile), TC-069-02 (argv correctness, valid:false clean verdict, binary-not-found wraps ErrCheckpointVerifierUnavailable), TC-069-03 (exit 0/1/2 with fake binary), TC-069-04 (--logfile threaded/omitted), TC-069-05 (leaf fitness), TC-069-06 (make check green, interfaces.md updated, verify unaffected) — all pass at L5. **L6 real-binary observation (2026-06-19):** built `agent-builder` from the worktree + the real audit-trail binary; `agent-builder verify-checkpoint` run offline against a real Ed25519-signed checkpoint confirmed all three exit paths — valid key → `{"valid":true,"message":"checkpoint signature valid"}` exit 0; wrong public key → `{"valid":false,"message":"checkpoint signature key_id does not match verification key"}` exit 1; missing `--public-key` → `usage error: --public-key is required` exit 2. The "survives agent compromise" offline-verification guarantee proven end-to-end. Verified by: L5 harness (5 cmds green) + L6 real-binary observation of all three exit paths. |
| 059 | Executor accepts subscription OAuth token | 059-subscription-oauth-auth-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-17; 7/7 in-scope TCs map to real asserting tests; TC-059-08 correctly deferred at verify time). L3: `make check` → `All checks passed.` + `make fitness` → `Fitness checks passed.` L5: `go test ./internal/executor/... ./internal/runtime/... -v` → 18 tests pass (TC-059-01 OAuth-only authenticates, TC-059-02 env has exactly one auth var + HOME/XDG wipe intact, TC-059-03 OAuth preferred when both set, TC-059-04 neither-set fails loudly, TC-059-05 ConfigFromEnv accepts either/rejects neither, TC-059-06 sanitizer redacts both tokens). **L6 auth-path observed (2026-06-17, this host, claude 2.1.150):** TC-059-08 — replicating the executor's exact isolation model (`claudeEnv`: fresh `HOME`/`XDG_CONFIG_HOME`/`XDG_CACHE_HOME`, `ANTHROPIC_API_KEY` stripped, only `CLAUDE_CODE_OAUTH_TOKEN` injected from `.env`), `claude -p "Reply with exactly the word: OK"` → output `OK`, **exit 0** — no `Not logged in`, no `Credit balance is too low`. The subscription OAuth credential authenticates under the executor's isolation; this clears the external blocker that stopped 022/028/032 on the dead-credit API key. The full live capstone (TestLivePhase0EndToEndAcceptance_TC032 end-to-end) remains the now-unblocked follow-on. Governed by ADR 033. |
| 070 | ADR-038 policy-engine integration decision | 070-adr038-policy-engine-integration-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; TC-070-01..05 all verified against ADR content). L5 harness: all 12 grep assertions pass on ADR file (Status/Decision/ADR035/ADR036/decide/AGENT_BUILDER_POLICY_BIN/tier_select/vault_injection_floor/audit_emit/fail-closed/out-of-process/configuration.md); `make check` → `All checks passed.` Verified by: L5 harness grep + make check. |
| 071 | `internal/policy` decide client + AuthZEN types | 071-policy-decide-client-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; TC-071-01..06 all verified; fail-closed enforced at client level — all 6 sub-cases A–F yield DecisionDeny). L5: `go test -count=1 ./internal/policy/...` → `ok github.com/tkdtaylor/agent-builder/internal/policy`; leaf check `PASS-leaf`; `make check` → `All checks passed.` Verified by: L5 unit tests (fake Unix-socket server) + leaf check + make check. |
| 072 | PolicyDaemon lifecycle + decide-gate before exec-sandbox | 072-policy-daemon-lifecycle-and-decide-gate-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19, opus; all 6 TCs ✓ + all 5 security invariants confirmed by direct source reading: ordering deny-before-box-construct, raise-only floor preserving VaultSocket/SecretRefs, opt-in zero-regression, fail-closed→deny, require_approval-routed-as-deny). L5: `go test -count=1 ./internal/policy/... ./internal/runtime/...` → both `ok`; `go test -count=1 ./tests/e2e/... -run 'TestPhase0EndToEndAcceptance\|TestPolicyGate'` → `ok`; `make check` → `All checks passed.` (all 6 fitness checks pass). TC-072-01: PolicyDaemon Start/Stop edge cases (empty/missing/non-executable BinPath fail loud, ping-timeout bounded near 5s, second-Start rejected) at L5; live happy-path (`TestPolicyDaemonLifecycle`) gated on `AGENT_BUILDER_LIVE_POLICY=1` (real policy-engine binary, operator path). TC-072-02 (deny → needs-human, box never starts, no publish), TC-072-03 (allow+tier_select → `run.tier=gvisor` on the block create-probe request, box started once), TC-072-04 (vault_injection_floor raise-only — all 5 sub-cases + non-floor/non-string ignore), TC-072-06 (fake-binary e2e: deny + require_approval routes-like-deny + allow+tier + allow+floor; fake serves NDJSON over a real Unix socket; `--socket`/`--allow` flags confirmed logged) all pass at L5 with fake binaries. Ordering invariant confirmed: decide runs AFTER vault handle resolution, BEFORE `sandboxBox.Create`. Opt-in: `AGENT_BUILDER_POLICY_BIN` unset → Phase-0 capstone unchanged (`TestPhase0EndToEndAcceptance` green). **Spec updated in same commit:** configuration.md (3 env vars + policy-engine deployment row), architecture.md (Policy Gate component + ADR-038 cross-cutting entry), diagrams.md (decide-gate in primary runtime flow, date bumped). **L6 capstone (live policy-engine + real exec-sandbox) PENDING OPERATOR RUN** per task Verification plan. Verified by: L5 — `go test ./internal/policy/... ./internal/runtime/...` + fake-binary e2e + make check all green; spec-verifier + L6 not yet reached. |
| 073 | require_approval + audit_emit obligations | 073-require-approval-and-audit-emit-obligations-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; TC-073-01..05 all verified; require_approval reason "policy: requires human approval" observably distinct from deny "policy: decision denied", both block dispatch; audit_emit side-effect not routing-modifier; nil-sink no-op; ActionPolicyDecision in enum+validActions). TC-073-01..05 implemented. TC-073-01 (e2e: require_approval halt reason contains "approval", deny does not; observably distinct), TC-073-02 (unit: deny/"denied" vs require_approval/"approval" gateOutcome table), TC-073-03 (unit: FakeSink receives ActionPolicyDecision with decision+reason on audit_emit+allow), TC-073-04 (unit: deny+audit_emit emits; require_approval+audit_emit emits; no-obligation=no-event; nil-sink=no-panic), TC-073-05 (unit: ActionPolicyDecision in validActions, EventDetail has PolicyDecision/PolicyReason, Validate accepts it, FakeSink stores it). Spec updated: behaviors.md (B-022 require_approval routing, B-023 audit_emit; invariants), data-model.md (ActionPolicyDecision in enum, PolicyDecision/PolicyReason in EventDetail), diagrams.md (audit sink before policy gate, audit_emit branch, distinct deny vs require_approval reasons). make check → `All checks passed.` Verified by: L4: make check green + L5: unit + fake-binary e2e (TestPolicyGateRequireApprovalDistinctFromDeny). |
| 074 | F-006 fitness: policy-isolation | 074-fitness-policy-isolation-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-19; TC-074-01..04 all verified; mirrors F-005 mechanism+format; both checks PASS on clean tree; both FAIL paths demonstrated synthetically with exit 1). L4: make check → `All checks passed.` + L3: make fitness → `All fitness checks passed.` (F-006 PASS line: `PASS fitness-policy-isolation: internal/policy import graph contains no other internal packages, and internal/runtime does not import the policy-engine block as a Go module.`); TC-074-03 negative evidence: synthetic forbidden import list → `FAIL fitness-policy-isolation: internal/policy imports forbidden agent-builder/internal package(s): github.com/tkdtaylor/agent-builder/internal/runtime` (exit 1); all TC markers TC-074-01..04 found in Makefile+tracker. |
| 075 | F-008 fitness: diagrams render | 075-fitness-diagrams-render-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-24; TC-075-01..04 — TC-075-04 was the sole ⚠, blocked only by a pre-existing `fitness-no-docker` false positive on `docs/agent-rules.md`, resolved out-of-band `[allow-main]` and merged into this branch). L5: `make fitness-diagrams-render` → `PASS fitness-diagrams-render: all Mermaid blocks render on GitHub (no parse hazards).` (exit 0); TC-075-02 negative evidence demonstrated twice — synthetic block with `;` in flowchart label → `FAIL … GitHub will likely fail to render…` (exit 1), and `[]` in a sequence message → `FAIL … \`[\`/\`]\` in a sequence message…` (exit 1); F-008 row added to `fitness-functions.md` with category `hygiene`, threshold `0 render hazards`, severity `block`, last-updated bumped; `fitness-diagrams-render` in Makefile `.PHONY` + `fitness:` prerequisites. `make fitness` → `All fitness checks passed.` and `make check` → `All checks passed.` (exit 0, all 8 fitness checks green). Verified by: L5 — `make check` green end-to-end + F-008 PASS line + both FAIL paths demonstrated. |
| 076 | Recipe type + in-process selector (RoutingSpec seam — ADR 043 amended) | 076-recipe-type-and-selector-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (re-verify, commit 4ba6e1f; all 17 TC ✓) + L2/L3 unit-test-only, no runtime surface: `go test -count=1 ./internal/recipe/...` → `ok github.com/tkdtaylor/agent-builder/internal/recipe 0.003s` (8 tests); `make check` → `All checks passed.`; leaf-purity `go list -deps ./internal/recipe/...` → zero forbidden imports (only `internal/supervisor` + stdlib). |
| 077 | runtime.Run assembles from a recipe (stub resolver — replaced by task 095) | 077-runtime-assembles-from-recipe-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (commit 23bee70; all 7 TC ✓ after ADR-044 seam correction — TC-077-03 resolved: no inline `tasksource.New`/`branchpub.NewGitHubCLI`/`newProductionGate` in `Run`; real seams not dummies; leaf-purity + F-003 hold). L5: `go test -count=1 ./internal/recipe/... ./internal/runtime/...` → `ok` (both); `go test -count=1 ./tests/e2e/... -run 'TestPhase0EndToEndAcceptance\|TestPhase1EndToEndAcceptance'` → `ok` (both e2e tests pass without modification — zero-drift confirmed); `make check` → `All checks passed.` Recipe registered with `SelectRecipe("coding-agent")` ✓; `stubResolveExecutor` named and marked for task 095 replacement ✓; `Config.RecipeName` defaults to "coding-agent" ✓; unknown recipe name → error before sandbox creation ✓. Verified by: L5 — `make check` + e2e zero-drift + unit tests for recipe selection and config. |
| 078 | Runtime gate-existence assertion for generated recipes | 078-runtime-gate-existence-assertion-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (commit 622b390; all 4 TC ✓ after live-Run rework). L2/L3 (verification plan caps here — pre-flight check, no runtime surface beyond error). `go test -count=1 ./internal/runtime/... -run 'TestGateExistence'` → `ok` (TC-078-01 nil→error has "gate"+"nil"; TC-078-02 non-Blocker gate rejected; TC-078-03 production gate passes; TC-078-04 fires through live `runtime.Run`, no escape hatch + audit-file-absence proves run never started). `go test ./tests/e2e/... -run 'TestPhase0EndToEndAcceptance\|TestPhase1EndToEndAcceptance'` → both pass (zero-drift). `make check` → `All checks passed.` (F-003 supervisor-isolation green). |
| 079 | Docs-fix recipe (second proof recipe) | 079-docs-fix-recipe-proof-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (commit 6928966; all 5 TC ✓ after 3-fix rework). L2/L3 (verification plan caps here — gate runtime surface exercised by fixture unit tests). `go test -count=1 ./internal/recipe/... ./internal/runtime/...` → all `ok`: TC-079-01 SelectRecipe non-nil + Name + distinct `*DocsFixGate` type + ListRecipes ✓; TC-079-02 gate PASS on well-formed / FAIL with non-empty failures on malformed markdown, gate composes MarkdownLintStep + `gate.CodeScannerStep`, no Go-tooling ✓; TC-079-03 machine-checked `go list` direct-imports → no direct `internal/sandbox` (transitive via supervisor expected) ✓; TC-079-04 real `runtime.Run(recipe="docs-fix")` passes gate-existence assertion (no GateFactory/Blocker/Blocks() error) ✓; TC-079-05 seam self-test `git diff 8988f5b~1 -- internal/runtime/ internal/supervisor/` → **EMPTY** across all 3 commits ✓. `make check` → `All checks passed.` (F-003 supervisor-isolation green). Registration: `init()` in `internal/recipe/docsfix` + import from `internal/cli/cli.go` (zero runtime/supervisor production changes — ADR 041 self-test passes). |
| 080 | Telegram channel adapter + armor guard (consumes internal/envelope — task 096) | 080-telegram-channel-adapter-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (final commit 577f6dc; all 6 TC + 8 prior-BLOCK items resolved with real assertions). security-auditor APPROVE for merge (commits e6ae8ac+577f6dc): zero critical/high; live-path security contract sound (pipeline order verify→replay→decrypt→armor via VerifyAndOpen, fail-closed armor, poison-safe offset advance, bounded replay cache, secret hygiene, no SSRF, no audit-arg injection). L2/L3 (verification plan caps here — stub HTTP server + stub armor; live bot is L6 follow-on). `go test ./internal/channel/telegram/...` → ok (TC-080-01 envelope decrypted + offset→101; TC-080-02 unknown key rejected before armor, 0 invocations + audit reason "unknown_key" + offset advanced; TC-080-03 replay rejected, audit reason "replay/nonce", offset→202; TC-080-04 armor Block, 1 invocation on plaintext, audit carries armor reason, offset advanced; TC-080-05 `var _ supervisor.GoalSource`; TC-080-06 bot token + actual X25519/Ed25519 key bytes (hex+base64+PEM) absent from debug logs). `go test ./internal/audit/...` → ok (model extension intact). `make check` → `All checks passed.` Plaintext→`Task.Spec` (supervisor unchanged). Audit-model extension: `EventDetail.Reason` + `ActionChannelReject` for channel rejections; BlockSink serialization to the audit-trail block deferred to task 081 wiring (documented data-model.md). Security hardening (SEC-001 body/text size caps, SEC-002 channel armor timeout, SEC-003 typed envelope errors) tracked as a follow-up before production exposure (task 081). |
| 081 | Orchestrator core | 081-orchestrator-core-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-28, sonnet; re-verified after adding TC-081-02 no-replay test — 2nd valid-role Resume on consumed plan errors, 0 additional dispatches, HasPendingPlan false) + architect (opus) SOUND (layer sits correctly above runtime/supervisor; ADR 046's 5 decisions faithful; diagrams.md §5 flow + spec/interfaces/F-010 all updated in-diff; future-tense clean; A-001 doc-comment nit fixed). The task-098 SEC-001 carry-forward (assert inbound approval envelope From/To = operator→orchestrator) is implemented here in Resume. Governed by ADR 046 (extends ADR 042); all 5 decisions implemented. New package `internal/orchestrator` (Planner seam + v1 `StructuredPlanner` rule-based, no LLM/no executor import; typed `PlanResult` rendered at the Reporter edge; in-memory `MemoryPlanStore` behind `PlanStore`; pause-and-resume approval via `policy.Decide` action `spawn-plan`; sequential dispatch reusing `runtime.Run` via `DispatchFunc` default). TC-081-01..06 real assertions: TC-01 free-form→1 sub-goal (recipe `coding-agent`, full spec) + structured 2-line→2 sub-goals (exact recipe/spec) + 0 dispatches before approval; TC-02 require_approval→0 dispatch + "Approve?" report naming both sub-goals + pending plan, then valid approval→2 dispatches in order + summary report, role-mismatch (from≠operator OR to≠orchestrator)→error+0 dispatch+plan stays pending (task 098 SEC-001 carry-forward); TC-03 allow→exactly 2 dispatches, recipe names+specs match per index, unknown recipe→failed outcome not dispatch; TC-04 one success+one failure→typed PlanResult aggregated (Outcomes[1].Success=false, Detail "gate failed"), 1 summary report containing both recipes + OK/FAIL; TC-05 hermetic go/parser direct-import walk asserts no `internal/executor` direct import (+ positive: recipe/runtime/policy/supervisor present); TC-06 supervisor empty-diff guard. New fitness F-010 `make fitness-orchestrator-no-executor` (direct-import, not `-deps`). Verified by: L2 `go test -count=1 ./internal/orchestrator/...` → `ok` + L3 `make fitness` → `All fitness checks passed.` + `make check` → `All checks passed.`; L5/L6 N/A (no runtime-observable surface — orchestrator not yet wired into CLI; end-to-end needs tasks 083/084). |
| 082 | agent-builder worker recipe (code-authoring worker) | 082-agent-builder-worker-recipe-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-28, sonnet; all 6 TCs real assertions, governed by ADR 047) + **security-auditor (opus) CONFIRMED CLOSED on SEC-001 (HIGH)**: the gate-existence step originally used substring matching (evadable by comment/string/whitespace-nil/cast-nil/decoy); commit e90baab replaced it with go/ast parsing (`extractRecipeBindings` over `recipe.Register`/`recipe.New`/`Recipe{GateFactory:…}`, `isNilExpr` rejects nil-ident + `(T)(nil)`, recursive walk, parse-error fails closed) + 7 evasion-class negative tests, all rejecting. Positive canonical recipe still passes. Residual static fail-open (factory-returns-nil) is backstopped by `runtime.verifyGateExists` at dispatch + human approval (REQ-082-04). **Carry into task 081:** (1) generated-recipe dispatch MUST route through `verifyGateExists`; (2) gate-existence matcher has import-alias/cross-file blindness — revisit when generated output format loosens (081/083). L4: `make check` → `All checks passed.` + `go test ./internal/recipe/agentbuilderworker/...` → ok (27 tests: TC-082-01 SelectRecipe+Blocker gate+3-step composition+no-Skip; TC-082-02A code-scanner MALWARE finding; TC-082-02B dep-scan CVE-2026-0001; TC-082-03A no GateFactory→fail; TC-082-03B GateFactory:nil→fail; TC-082-03C valid GateFactory→pass; TC-082-04 require_approval→no dispatch+ApprovalSolicited; TC-082-05 FakeSink event contains file path+SHA-256 hash; TC-082-06 direct import check). `make fitness-gate-blocking` → PASS. REQ-082-06 empty-diff confirmed (no changes in internal/runtime or internal/orchestrator). Verified by: L4 CI pending + L3 fitness pass. |
| 083 | Orchestrator↔worker transport (signed envelopes via internal/envelope) | 083-agent-mesh-adoption-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-28, sonnet; all 5 TCs real assertions after adding the TC-083-02 result-envelope From/To check; audit-allowance — envelope+supervisor+audit — judged sound & precedent-consistent with telegram, explicitly documented in F-011) + security-auditor (opus) SAFE TO MERGE (no CRITICAL/HIGH; crypto mirror faithful, fresh nonce, role-binding tested, no plaintext/secret leak, key load fail-closed). **Carry into task 086 (live wiring):** SEC-001 (MEDIUM) — construct ONE long-lived Receiver / shared `*ReplayCache` per direction (per-message receivers each get a fresh cache → replay hole once on the live wire); SEC-003 (LOW) — apply the same fail-closed startup validation to the X25519 seal keys 086 loads. Governed by ADR 048 (in-process v1 behind a transport-adapter seam; envelope is the security layer regardless of the wire; agent-mesh A2A deferred). New leaf package `internal/channel/worker`: `Sender`/`Receiver` wrap a sub-goal's `supervisor.Task` (work-item) and `supervisor.Result` in Ed25519-signed + X25519+AEAD-sealed envelopes (key roles mirror `internal/channel/telegram` exactly — orch signs+seals work-items to worker; worker signs+seals results to orch; fresh `crypto/rand` nonce per message via `envelope.Seal`). Receivers run `envelope.VerifyAndOpen` (verify→replay→open) then assert envelope From/To roles (task 098 SEC-001 carry-forward) before returning the payload; rejections emit `audit.ActionChannelReject` with a classified reason, no plaintext/key in logs. REQ-083-05 startup fail-closed: `LoadSigningKey`/`NewWorkItemSenderFromEnv` return `ErrMissingSigningKey` (NAMED, names `AGENT_BUILDER_WORKER_SIGNING_KEY`) when the key is unset/absent/malformed, at construction. New fitness **F-011** `make fitness-worker-transport-isolation` (direct-import leaf check, like F-010, since `internal/supervisor` legitimately drags in gate/sandbox transitively). Integration depth (ADR 048 §1): leaf transport + startup check + envelope wrap/unwrap seam; live `dispatchPlan` wiring + N-concurrent workers deferred to task 086. TC-083-01 (round-trip byte-exact `reflect.DeepEqual` + fresh nonce + plaintext absent from JSON), TC-083-02 (tamper → `errors.Is ErrBadSignature` + zero Result + 1 audit event), TC-083-03 (replay → `errors.Is ErrReplay` + zero Task + audit "replay"), TC-083-05 (unset + absent file → `errors.Is ErrMissingSigningKey` naming the env var + nil sender), plus role-mismatch test (`ErrRoleMismatch`). Verified by: L2 `go test -count=1 ./internal/channel/worker/... ./internal/orchestrator/...` → `ok`; L3 `make fitness` → `All fitness checks passed.` (incl. `PASS fitness-worker-transport-isolation`) + `make check` → `All checks passed.`. L5/L6 N/A — no runtime-observable surface (transport not yet wired into the CLI/live dispatch; live round-trip needs task 086). Spec updated in-diff: SPEC.md (F-011), fitness-functions.md (F-011 row + source link), interfaces.md (§Orchestrator↔worker transport), configuration.md (`AGENT_BUILDER_WORKER_SIGNING_KEY`). Diagrams unchanged (no diagrammed boundary moved; channel-side crypto leaves are not C4 components and live wiring is deferred). |
| 096 | internal/envelope leaf (Ed25519 sign/verify + X25519+AEAD + ReplayCache) | 096-envelope-leaf-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (final commit c52f58c; all 11 TCs + 4 security-fix assertions ✓). L2/L3 (verification plan caps here — leaf has no runtime surface; unit + import-graph + fitness). Security-auditor pass: SEC-001 (HIGH) fixed — signingBytes now json.Marshal of Envelope{Sig:""} matching agent-mesh (closes field-confusion collision + restores wire-compat); SEC-002 VerifyAndOpen enforces verify→check→open; SEC-004 evict-horizon=2×Window coupling test; SEC-007 nonce doc; SEC-005 typed-keys deferred to task 080. All 11 TCs pass (sign/verify/tamper, seal/open, ReplayCache freshness+replay+bounded eviction, round-trip, leaf-purity, F-007). `make check` → `All checks passed.` (F-007 fitness-envelope-isolation green). crypto/rand only (no math/rand); no secret logging. golang.org/x/crypto v0.53.0 (approved ADR 045). |
| 084 | memory-guard adoption (orchestrator goal/fleet state) | 084-memory-guard-adoption-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-28, sonnet; all 5 TCs real assertions — `validate_write` op+payload asserted via ExecRunner stub, tamper audit event asserts `Detail.TamperDetected==true` + `Action==ActionTamper`, degraded fallback returns the 081 MemoryPlanStore on the live path, audit-leaf extension didn't break F-005 / ActionVerify-needs-Verdict). **Known deferred (L5/L6):** `NewPlanStoreFromEnv` is implemented+tested but not yet called from `cmd/agent-builder` — same orchestrator-not-CLI-wired gap as task 081; a future orchestrator-CLI-wiring task should call it at startup. New leaf `internal/memoryguard` (binary IPC adapter — `validate_write`/`verify_delete` over per-op subprocess; ADR 049). `MemoryGuardPlanStore` + `TamperAwarePlanStore` in `internal/orchestrator` wrap `memoryguard.MemoryGuardStore[Plan]` and implement `PlanStore` + optional `TryPut`/`TryDelete` for write-gate rejections and tamper-halt. `NewPlanStoreFromEnv`: set → `MemoryGuardPlanStore`; unset → `MemoryPlanStore` + structured warning naming `AGENT_BUILDER_MEMORY_GUARD_BIN` + `"memory-guard"`. `WithAuditSink` option wires an `audit.Sink` for tamper events. `audit.AuditAction` extended with `ActionTamper`; `audit.EventDetail` extended with `TamperDetected bool`. New fitness **F-012** `make fitness-memoryguard-isolation` (transitive-deps leaf check). TC-084-01: `validate_write` called on plan write; stub returns allow=true + stored_id; stored_id held + Get works. TC-084-02: `verify_delete` returns confirmed=false/residue_detected=true; Delete returns `ErrTamperDetected`; entry evicted; Resume returns tamper error + 0 dispatches. TC-084-03: `go list -deps ./internal/memoryguard/...` → no other agent-builder/internal imports + `make fitness-memoryguard-isolation` PASS. TC-084-04: unset env → `MemoryPlanStore` returned + logFn called once naming `AGENT_BUILDER_MEMORY_GUARD_BIN` + `"memory-guard"` + store Put/Get/Delete functional. TC-084-05: tamper from stub → Resume returns tamper error + 0 dispatches + `audit.FakeSink` receives event with `Action=ActionTamper` + `Detail.TamperDetected=true` + `TaskID="g3"`. `TestPhase0EndToEndAcceptance` passes unchanged (no `AGENT_BUILDER_MEMORY_GUARD_BIN` set). Verified by: L2 `go test -count=1 ./internal/memoryguard/... ./internal/orchestrator/... ./tests/e2e/...` → all `ok`; L3 `make fitness` → `All fitness checks passed.` + `make check` → `All checks passed.` L5/L6 N/A — no runtime-observable surface (binary not yet wired into CLI run path; live wiring deferred). |
| 085 | Orchestrator self-containment + policy gating + fleet audit | 085-orchestrator-containment-policy-audit-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-28, sonnet; all 5 TCs real, no 081 regression, L5/L6 honestly deferred) + security-auditor (opus) CLEAR TO MERGE after **4 audit rounds on the ADR-042 self-repo bright line**: SEC-001 (HIGH — runtime guard read always-empty SubGoal fields while the live `StructuredPlanner` sets `Task.Repo`; fixed to guard the real `Task.Repo` + a real-planner own-repo-deny test), SEC-002 (HIGH — byte-exact match evadable by scheme/case/`.git`/whitespace/**host:port/userinfo**/scp/backslash/protocol-relative/trailing-dot; fixed via a `net/url`-based `CanonicalizeRepo` — every realistic own-repo variant now denies, near-misses allow, fail-closed on parse failure), SEC-003 (deny-event audit append now hard-errors / requires a sink), SEC-004 (deny audit event records the real reason + self-repo marker) — all CONFIRMED CLOSED. **Optional non-blocking follow-up:** loop the scheme-strip for malformed stacked-scheme inputs (`https://https://…` — not a realistic repo reference). Governed by ADR 050 (extends ADR 042/038/026). Three coupled controls on the Tier-1 orchestrator: (1) **containment** (REQ-085-01/04) — `Orchestrator.Containment()` returns the L2 posture `{Profile:"exec-sandbox", Rootless, ReadOnlyRootfs, ResourceLimited, EgressPolicy:"default-deny"}` (same profile as workers; live Podman+runsc/nftables enforcement L6 operator-deferred, not claimed in CI). (2) **policy gating** (REQ-085-02) — new `SpawnWorkerAction="spawn-worker"` issued per-sub-goal in `dispatchPlan` BEFORE each dispatch, `Resource{Type:"recipe", ID:recipeName, Properties:{target_repo, sink}}`, additive to 081's `spawn-plan`; deny/require_approval/error → worker NOT spawned + denied outcome recorded + denial reported via Reporter (fail-closed). (3) **fleet audit** (REQ-085-03) — `WithAuditSink` wires the orchestrator to emit `goal-intake`/`plan-decided`/per-worker `spawn-decided`/`completion` onto the SAME `audit.Sink` chain workers write to (4 new `AuditAction`s). (4) **self-repo bright line** (REQ-085-05) belt-and-suspenders: RUNTIME `decideSpawnWorker`/`targetsOwnRepo` denies any sub-goal whose `TargetRepo`/`Sink == OwnRepo` before the policy call, fail-closed; STATIC fitness **F-013** `make fitness-no-self-repo-sink` (+ `SelfRepoSinkViolation` predicate). TC-085-01 containment posture fields asserted vs exported constant; TC-085-02 spawn-worker deny → exactly 1 dispatch (denied recipe skipped) + denied outcome + denial report + correct request shape, plus fail-closed-on-error sub-case (0 dispatch); TC-085-03 one FakeSink chain with counts goal-intake=1/plan-decided=1/spawn-decided=2/containment=2/finish=2/completion=1 (≥9 events), spawn-decided precedes worker containment, goal-intake first + completion last; L5 BlockSink+VerifyChain valid=true when `AGENT_BUILDER_AUDIT_BIN` set else L5-binary-deferred skip; TC-085-04 egress=`default-deny`; TC-085-05 runtime deny on target_repo AND sink own-repo (0 dispatch each) + non-own-repo dispatched (targeted guard) + detector fires on violation/clean/import + real `make fitness-no-self-repo-sink` exits non-zero on a violation fixture and 0 on a clean one. F-003/F-010 unchanged (existing fitness checks green). Verified by: L2 `go test -count=1 ./internal/orchestrator/... ./internal/runtime/... ./internal/audit/...` → all `ok` + `go test ./...` all `ok`; L3 `make fitness` → `All fitness checks passed.` (incl. new F-013) + `make check` → `All checks passed.`; L5 fleet-chain `audit-trail verify` deferred (`AGENT_BUILDER_AUDIT_BIN` unavailable in CI env); L6 live Podman+runsc containment / nftables egress operator-deferred (ADR 050 §3). |
| 086 | Multi-worker concurrent dispatch | 086-multi-worker-concurrent-dispatch-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-28, sonnet; all 5 TCs real — barrier-spy proves true concurrency, `-race` clean, 081/083/085 carry-forward regressions all pass) + security-auditor (opus) SAFE TO MERGE (no CRITICAL/HIGH; race-free audit chain + per-index aggregate, per-worker gate fires in-goroutine with no fan-out TOCTOU, self-repo bright line + SEC-003 deny-halt preserved concurrently, shared ReplayCache invariant directly tested). Follow-ups closed in 5aa2c06: SEC-086-01 (added the SEC-003 deny-audit-failure HALT-path test + nil-sink hard-error test, `-race` clean), SEC-086-03 (fixed misleading `replay.go` "not thread-safe" comment), SEC-086-02 (documented best-effort halt semantics in spec + behaviors.md B-026). `dispatchPlan` converted from sequential loop to CONCURRENT fan-out (ADR 042): one goroutine per sub-goal joined by `sync.WaitGroup`, outcomes written into a pre-sized slice BY INDEX (race-free + deterministic order), best-effort partial failure (a worker error is recorded in its own slot, never halts/cancels siblings), SEC-003 deny-audit hard-error collected post-join and halts the plan. Concurrency primitive decision: WaitGroup fan-out (not errgroup — no fail-fast wanted; not channel-fan-in — pre-sized indexed slice already gives ordered race-free aggregation). Race-safety: shared `audit.Sink` is the one fleet hash chain → made `FakeSink` AND `BlockSink` mutex-guarded (serial append is required by a hash chain anyway); `MemoryPlanStore` already mutex-guarded; `envelope.ReplayCache` already mutex-guarded (083 SEC-001 carry-forward — confirmed safe under -race via a new concurrent shared-cache test in `internal/channel/worker`: distinct nonces all accepted, replayed nonce rejected across workers sharing ONE cache). TC-086-01 barrier spy proves all N enter before any returns (sequential would deadlock/timeout); TC-086-02 worker-0-fails → survivors 1,2 dispatched+succeed, all 3 outcomes reported, plan nil-error; TC-086-03 middle-fail mix → ordered 0✓/1✗/2✓ + exactly 1 Reporter summary with OK&FAIL; TC-086-04 5 workers concurrent write aggregate+sink+store → `-race` clean; TC-086-05 single fleet chain covers all 3 workers per-TaskID (spawn-decided+containment+finish) with goal-intake first/completion last, L5 BlockSink+VerifyChain when `AGENT_BUILDER_AUDIT_BIN` set else L5-binary-deferred. Existing 081/085 tests updated for concurrency (order-independent dispatch-set assertions; spec-keyed failure injection; mutex on `fakePolicy`/`recordingPolicy` doubles) — no behavioral regression. Deferred (explicit, in-scope-noted): wiring the worker transport's signed envelopes onto the LIVE dispatch path (orchestrator constructing Sender/Receiver around each runtime.Run) stays behind the DispatchFunc stub so concurrency is L2-testable; the shared-cache invariant is exercised in the worker package. Spec updated in-diff: behaviors.md (B-026), architecture.md (§Tier-1 row + cross-cutting §), interfaces.md (Dispatch + transport integration-depth), diagrams.md (component note + Rel + sequence loop/legend, date bump). Verified by: L2 `go test -race -count=1 ./internal/orchestrator/...` → `ok ... 1.026s` + `go test -race ./internal/channel/worker/...` → `ok ... 1.050s`; L3 `make check` → `All fitness checks passed.` + `All checks passed.`; L5 fleet-chain verify binary-deferred (`AGENT_BUILDER_AUDIT_BIN` unset in CI); L6 live multi-worker run operator-deferred. Pending spec-verifier + L5/L6 evidence before ✅. |
| 087 | Executor registry type + entry config | 087-executor-registry-type-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-27, opus; all 6 TCs map to real asserting tests — exact field/value asserts on RegistryEntry, distinct HarnessDriver constants + `String()`, LoadFromEnv happy-path + ENABLED=false exclusion + missing-ENDPOINT/non-integer-tier descriptive errors, RegisterEntry/LookupEntry round-trip + empty/unknown→false, duplicate→panic naming ID; spec docs updated in data-model/interfaces/configuration). L2: `go test -count=1 ./internal/registry/...` → `ok …/internal/registry`. L3: `go list -deps ./internal/registry/...` leaf-clean (no supervisor/executor/router/runtime/vault/policy) + `make check` → `All checks passed.` Leaf data + catalog package; no runtime-observable surface (L5/L6 N/A). |
| 088 | Vault-brokered per-provider auth | 088-vault-per-provider-auth-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-27, opus; all 5 TCs map to real asserting tests — TC-088-01 env-fallback `AGENT_BUILDER_SECRET_<REF_UPPER>` resolves "codex-token"→`sk-test-codex-key`/unknown→`ErrSecretNotFound` (fatal asserts); TC-088-02 `ProviderToken()` regression `("test-api-key","")` unchanged; TC-088-03 `VaultSecretSource.NamedProviderToken` returns opaque vault handle (asserted ≠ plaintext), missing ref→`ErrSecretNotFound`; TC-088-04 compile-time `var _ secrets.SecretSource` on both `*EnvSecretSource` and `*VaultSecretSource`; TC-088-05 leaf import-graph + `make check`). Added `SecretSource.NamedProviderToken(ref string) (string, error)` + `ErrSecretNotFound`; required interface-conformance stubs added to executor `fakeSecretSource`/runtime `envFuncSecretSource` (return `ErrSecretNotFound`, no behavior change). L2: `go test -count=1 ./internal/secrets/...` → `ok …/internal/secrets`. L3: `go list -deps ./internal/secrets/...` leaf-clean (only `internal/vault` + `internal/secrets`) + `make check` → `All checks passed.`. No runtime-observable surface (L5/L6 N/A — interface extension only; live-vault L5 optional behind `AGENT_BUILDER_LIVE_VAULT=1`). |
| 089 | Codex harness adapter | 089-codex-harness-adapter-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-27, sonnet; re-verified after closing the TC-089-02 smoke-test gap — test now captures the constructed `exec.Cmd` and asserts `OPENAI_API_KEY=sk-test-codex-key`, `CODEX_MODEL=gpt-4o`, and `cmd.Dir==worktree`). L5 (per task verification plan = stub-subprocess unit tests): `go test -count=1 ./internal/executor/...` → `ok …/internal/executor` exercises `CodexCLI.Run()` against a stub subprocess with env+args captured/asserted. + L3: `make fitness-supervisor-isolation` → `PASS fitness-supervisor-isolation: …` + `make check` → `All checks passed.`. TC-089-01 compile-time `var _ supervisor.Executor = (*CodexCLI)(nil)` + runtime nil check; TC-089-02 stub subprocess exits 0 → `Result{Branch:"task/089-test-branch", OK:true}`; TC-089-03 `ErrSecretNotFound` → `Run` returns error wrapping `ErrCodexSecretNotFound`, subprocess not invoked; variant A key present → OK; TC-089-04 subprocess exit 1 → non-nil error containing "codex CLI failed", `Result.OK==false`; TC-089-05 `make fitness-supervisor-isolation` PASS + `make check` PASS. `OPENAI_API_KEY` injected via `entry.SecretRef → NamedProviderToken`. L6 (live `codex` CLI on PATH + valid OpenAI API key, operator-run) deferred — no real binary available in CI. |
| 090 | Gemini harness adapter | 090-gemini-harness-adapter-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-27, sonnet; re-verified after closing the TC-090-04 stderr-surfacing gap — non-zero-exit test now emits `"Gemini API error"` to stderr and asserts it reaches `err.Error()`). L5 (stub-subprocess unit tests): `go test -count=1 ./internal/executor/...` → `ok github.com/tkdtaylor/agent-builder/internal/executor`. + L3: `make fitness-supervisor-isolation` → `PASS fitness-supervisor-isolation: supervisor import graph contains no executor/LLM/web-fetch packages.` + `make check` → `All checks passed.` TC-090-01 compile-time `var _ supervisor.Executor = (*GeminiCLI)(nil)` + runtime nil check; TC-090-02 stub subprocess exits 0 → `Result{Branch:"task/090-test-branch", OK:true}`, asserts `GEMINI_API_KEY=gai-test-gemini-key`, `GEMINI_MODEL=gemini-2.0-flash`, `cmd.Dir==worktree`; TC-090-03 `ErrSecretNotFound` → `Run` returns error wrapping `ErrGeminiSecretNotFound`, subprocess not invoked; variant A key present → OK; TC-090-04 subprocess exit 1 → non-nil error containing "gemini CLI failed", `Result.OK==false`; TC-090-05 `make fitness-supervisor-isolation` PASS + `make check` PASS. `GEMINI_API_KEY` injected via `entry.SecretRef → NamedProviderToken`. L6 (live `gemini` CLI on PATH + valid Gemini API key, operator-run) deferred. Verified by: L5 stub-subprocess + L3 fitness. |
| 091 | Local entry + translation-proxy seam | 091-local-entry-translation-proxy-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-27, sonnet; all 5 TCs real assertions on first pass — TC-091-03 negative invariant (cloud-auth vars absent) proven via captured `cmd.Env`, not smoked; producer→consumer baseURL chain complete; no breaking change to `NewClaudeCLI`). Names/tests/wires the claude-CLI-via-proxy seam (`registry.TranslationProxySeam` + `ANTHROPIC_BASE_URL` injection), closing task 094's open proxy assumption at code/spec level (L6 operator round-trip still deferred). L5 (stub-subprocess unit tests): `go test -count=1 ./internal/registry/... ./internal/executor/...` → `ok …/internal/registry` + `ok …/internal/executor`. `make check` → `All checks passed.` TC-091-01: `LoadFromEnv()` with local-qwen env vars (no SECRET_REF) → `Harness=HarnessClaudeCLI`, `SecretRef=""`, `Budget=QuotaBudget{}` (zero). TC-091-02: `entry.Endpoint=="http://localhost:8080"`, `Harness==HarnessClaudeCLI`; `registry.TranslationProxySeam` constant names the LiteLLM/claude-code-router pattern. TC-091-03a: `claudeEnv("","",proxyURL,...)` → `ANTHROPIC_BASE_URL=proxyURL` present, `ANTHROPIC_API_KEY`/`CLAUDE_CODE_OAUTH_TOKEN` absent. TC-091-03: `NewClaudeCLIFromEntry(localEntry, src, worktree)` + `/bin/true` subprocess → `Result.OK=true, Branch=<expected>`; captured `cmd.Env` contains `ANTHROPIC_BASE_URL=http://localhost:8080`, no cloud auth vars. TC-091-04: `IsUnlimited()` true when `Budget.Limit==0`, false when `Limit>0`. TC-091-05: `NewClaudeCLIFromEntry` compiles alongside `NewClaudeCLI`; local entry has `baseURL` set; cloud entry has empty `baseURL`. L3: `make fitness-supervisor-isolation` PASS. L6 (live claude CLI + real LiteLLM proxy + local inference server, operator-run) deferred. Verified by: L5 stub-subprocess + L3 fitness. |
| 092 | Router + capability/cost model + escalation | 092-router-capability-cost-escalation-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-27, sonnet; all 7 TCs real `t.Fatalf`/`errors.Is` assertions; two-axis independence genuinely tested via tier-direction guard in TC-092-05; `make check` re-run clean — the noted gofmt concern was a non-issue). New `internal/router` (Router: Select / OnGateFailure / ResetEscalation / OnQuotaExhausted / ResolveExecutor) + `registry.Catalog.UpdateEntry`. L2: `go test -count=1 ./internal/router/... ./internal/registry/...` → `ok …/internal/router` + `ok …/internal/registry`. L3: `make fitness-supervisor-isolation` → `PASS fitness-supervisor-isolation: supervisor import graph contains no executor/LLM/web-fetch packages.` + `go list -deps ./internal/supervisor/...` contains no internal/router or internal/registry; `make check` → `All checks passed.` TC-092-01 cheapest eligible (`local`, cost=1) selected at MinCapability=1. TC-092-02 MinCapability=2 filters `local`(tier=1) → `codex`(tier=2, cheapest eligible). TC-092-03 sensitive hint is soft: cheapest `local` selected under both hints; on a cost tie the sensitive hint picks `local`, a None hint picks `cloud` by stable ID (non-local stays eligible). TC-092-04 `OnGateFailure("local")` → next Select=`claude-oauth`; gate-fail it too → `ErrNoEligibleExecutor` (via `errors.Is`); `ResetEscalation` restores `local`. TC-092-05 `OnQuotaExhausted("claude-oauth", resetAt)` → status `exhausted`+ResetAt asserted, next Select at MinCapability=2 routes sideways to weaker `codex` (tier 2 < 3, proving no quality climb). TC-092-06 `OnQuotaExhausted("local", …)` no-op (Budget.Limit=0) → stays available + still selectable. TC-092-07 fitness + import-graph (this row). L5/L6 N/A — router has no runtime-observable surface (live dispatch is task 095). Verified by: L3 (CI fitness + import-graph) + L2 (router/registry unit tests). |
| 093 | Usage/quota tracking | 093-usage-quota-tracking-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-27, sonnet; all 6 TCs real assertions with exact Usage/ResetAt values via FakeClock; no `time.Sleep` in any test; corrupted-file returns non-nil descriptive error; zero TC-092 regressions; integer-seconds Retry-After scope matches spec — HTTP-date not required). L2: `go test -count=1 ./internal/router/...` → `ok github.com/tkdtaylor/agent-builder/internal/router 0.005s` (9 tests: 6 TC-093-* + 3 helpers). L3: `make fitness-supervisor-isolation` → `PASS fitness-supervisor-isolation: supervisor import graph contains no executor/LLM/web-fetch packages.` + `make check` → `All checks passed.` TC-093-01: `RecordDispatch` ×3 → Usage=3, Status=Exhausted, ResetAt=baseTime+5h; Select → ErrNoEligibleExecutor. TC-093-02: advance FakeClock 4h59m → still excluded; advance +2m (total 5h01m) → auto-recovered, Usage=0, Status=available (no sleep). TC-093-03A: `OnRateLimit(…,"60")` → Status=Exhausted, ResetAt=baseTime+60s (exact). TC-093-03B: `OnRateLimit(…,"")` → ResetAt=baseTime+5m (configuredCooldown). TC-093-04: FakeClock at T, ResetAt=T+10s; Select at T → ErrNoEligibleExecutor; advance 11s → entry returned; no time.Sleep. TC-093-05: RecordDispatch ×3 → exhausted; SaveState → valid JSON; new router + LoadState → Usage=3, Status=Exhausted; Select → ErrNoEligibleExecutor; corrupted file → non-nil descriptive error containing "corrupted" or "parse". TC-093-06: cloud exhausted via RecordDispatch (Limit=1); Select → "local-qwen" (always-available fallback). `New(catalog)` unchanged (real clock, DefaultCooldown=5m); `NewWithClock` for tests. No `strings` import needed — `strconv.ParseFloat` for Retry-After. Verified by: L3 (fitness + import-graph) + L2 (unit tests, injected clock). |
| 094 | Local-model evaluation (hardware-specific, operator-run) | 094-local-model-evaluation-test-spec.md | ✅ | ✅ | L6 operator-run (2026-06-27, re-run): **two local entries retained** — `qwen3:8b` (generalist) + `qwen2.5-coder:7b` (coder), both VRAM-resident on 8 GB (5.34 / 4.72 GB, 44–52 TPS); other candidates (mistral:7b, llama3.1:8b, qwen2.5:14b) benchmarked then removed. Speed/VRAM found non-differentiating among 7–8B; both kept for code-vs-general routing (092/095). LiteLLM proxy validated via **curl only**; actual `claude`-CLI-via-proxy round-trip NOT confirmed (model-name constraint) → must be proven in task 091. Methodology re-runnable; rationale in `docs/plans/sprints/094-local-model-benchmark.md` + `docs/spec/configuration.md`. **Update 2026-06-28:** tasks 091 (local entry + `NewClaudeCLIFromEntry` + `ANTHROPIC_BASE_URL` injection, no cloud auth) and 092/095 (router + live wiring) are now MERGED, closing the translation-proxy seam at the code/spec level — the registry/adapter can drive a local-`qwen` entry through the proxy. **REQ-094-02 residual (operator-run, still 🟡):** the live `claude` CLI → LiteLLM proxy → local model → branch-produced + gate-passing round-trip is hardware/operator-run by design (not CI-automatable) and remains the one unverified piece; tooling is present on this host (ollama, litellm, claude CLI, RTX 4060) so the operator can run it on demand. **Update 2026-06-28 (live run on this host):** the protocol round-trip is now **PROVEN live** — LiteLLM (Anthropic `/v1/messages`, incl. `?beta=true` + `thinking`) → ollama `qwen2.5-coder:7b` via ollama's **OpenAI-compatible** `/v1` endpoint + wildcard model + `drop_params` **resolves the model-name constraint**; `claude` CLI via `ANTHROPIC_BASE_URL` returned a clean completion; agent-builder **selected the `local-qwen` entry** and its host-side `claude` reached the proxy (run record: `task 001 selected`; proxy logged 7×`200 OK` from qwen). **Full TC-094-02 (branch produced + gate passes) remains BLOCKED** by two confirmed code bugs in 091's local-entry path → **task 101**: (1) `claudeEnv` injects no auth env var the CLI accepts for local entries (and strips HOME), so the CLI exits `Not logged in · Please run /login`; (2) `ConfigFromEnv` (run.go:224) requires cloud auth even for a local-only registry. **Update 2026-06-28 (post-task-101 live run, this host):** task 101 MERGED and corrected — its FINDING-1 diagnosis (a placeholder in `ANTHROPIC_API_KEY`) was proven WRONG live; the working fix is a placeholder in **`ANTHROPIC_AUTH_TOKEN`** (CLI v2.1.150 validates `ANTHROPIC_API_KEY` as a real credential → `Not logged in`; `ANTHROPIC_AUTH_TOKEN` is the gateway bearer-token var passed through to the proxy). With corrected 101, a live `go run ./cmd/agent-builder run` against a local Go target with a local-only registry **clears both 101 blockers**: the run starts with no cloud creds (FINDING 2) and the `claude` executor authenticates through the proxy (FINDING 1 — proxy logged 4+ `200 OK` from `qwen2.5-coder:7b`, no `Not logged in`). **Full TC-094-02 (branch produced + gate passes) still NOT achieved — residual is tool-call serialization + model capability, not auth/plumbing.** Drill-down 2026-06-28: the executor seam needs structured `tool_use` to edit files/manage the branch. (1) **Proxy routing matters:** LiteLLM `openai/<model>` (Ollama OpenAI-compat `/v1`) returns tool calls as plain-text JSON → CLI can't execute → escalation; LiteLLM **`ollama_chat/<model>`** (Ollama native `/api/chat`) returns structured `tool_calls` → `tool_use` round-trips through the Anthropic proxy (VERIFIED). (2) **Model-specific:** `qwen3:8b` emits Ollama-parseable `tool_calls` (works mechanically) but its agentic completion at 8B is marginal (reasoning wandered, didn't finish a trivial write task in the live loop); `qwen2.5-coder:7b` deterministically emits bare JSON without the `<tool_call>` wrapper (Modelfile SYSTEM override didn't fix), so Ollama 0.17.7 never parses tool_calls — unusable as an agentic executor in this stack though its code output is correct. Verified setup + per-model status now documented in `docs/operating.md` → "Driving local models". Stays 🟡 — promote to ✅ only when a full local round-trip reaches branch+gate (use `ollama_chat/qwen3:8b`; likely needs a stronger local model, a tool-parser serving stack e.g. vLLM, or a purpose-built native-`/api/chat` harness). The verify-gate correctly escalated every incomplete attempt. **Update 2026-06-28 — CLOSED via native harness:** 094's own last note called for "a purpose-built native-`/api/chat` harness" — that is tasks 102–106 (`executor.OllamaNative`, `ollama-native` entry). L6 PASSED on this host: `qwen3:8b` via the native harness (no Claude CLI, no LiteLLM) produced branch `task/001-add-product` (committed `f71191a`, pushed) with ALL 7 gate steps green; only `gh pr create` failed (local remote, by design). The local-model autonomous round-trip (branch produced + gate passes) — 094's core goal — is proven. The original claude-CLI-via-LiteLLM-proxy mechanism of REQ-094-02 is SUPERSEDED by the native harness (ADR 051); kept here as history. |
| 095 | Recipe RoutingSpec wired to real router (replaces task 077 stub) | 095-recipe-routing-spec-real-router-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-28, sonnet; all 6 TCs real assertions; **zero-drift confirmed via `git diff --stat main..a492ff1 -- tests/` = empty** — no e2e/cli test modified; router.Select never bypassed incl. default-entry path; empty-registry error fires before audit-sink construction → 0 events). `stubResolveExecutor` removed; `runtime.Run` resolves `recipe.RoutingSpec` via `router.New(catalog).Select` over `registry.LoadFromEnv()`, synthesizing a default `claude-default` entry when no `AGENT_BUILDER_REGISTRY_*` vars are set (zero-drift). New `internal/runtime/run_routing_test.go`. TC-095-01 source-inspection: stub gone + `registry.LoadFromEnv()`/`router.New(`/`r.Select(toRouterSpec(spec))` wired (`TestStubResolverRemovedAndLiveRouterWired`). TC-095-02 zero-drift e2e: `TestPhase0EndToEndAcceptance`/`TestPhase1EndToEndAcceptance` pass UNMODIFIED. TC-095-03 multi-entry: fake `{local tier1 cost1, claude-oauth tier3 cost10}` → router selects `local` (cheaper); control test at MinCapability=2 selects `claude-oauth` (`TestResolveExecutorSelectsCheapestEligible` + control). TC-095-04 empty registry: `resolveExecutor` wraps `router.ErrNoEligibleExecutor` (asserted via `errors.Is`) and `Run` fails before sandbox creation with **zero audit events** (FakeSink asserted empty in `TestRunEmptyRegistryFailsBeforeDispatchNoAudit`). TC-095-05 unknown recipe → error before dispatch (existing `TestRunWithUnknownRecipeReturnsError`, unmodified). TC-095-06 F-003: `make fitness-supervisor-isolation` PASS + inline `go list -deps` assertion (`TestSupervisorIsolationExcludesRouterRegistry`). Verified by: L4 pending CI + L3: `make fitness` → `All fitness checks passed.`; L2: `go test -count=1 ./internal/runtime/... ./internal/router/...` → `ok`; `make check` → `All checks passed.` Awaiting spec-verifier APPROVE before ✅. |
| 097 | Telegram channel adapter security hardening (SEC-001 body cap, SEC-002 guard timeout, SEC-003 sentinel errors) | 097-telegram-security-hardening-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (final commit e38f9e2; all 8 TC with real assertions). L2/L3 (verification plan caps here). SEC-001: io.LimitReader at MaxBodyBytes (default 4MB) + MaxMessageBytes (default 64KB), oversize text skipped with offset advanced (asserted offset→202); SEC-002: armor DecideContent under context.WithTimeout (GuardTimeout default 5s), timeout drops + audits fail-closed, Next() returns within bound, offset advanced (asserted→101); SEC-003: internal/envelope exports ErrUnknownKey/ErrBadSignature/ErrReplay/ErrStaleTimestamp (%w-wrapped), adapter classifies via errors.Is (no strings.Contains); TC-097-03a drives real envelope.Verify/cache.Check + cross-non-match. Safe zero-value defaults (zero ≠ disabled). `go test ./internal/channel/telegram/... ./internal/envelope/...` → ok (TC-080 regression green). `make check` → `All checks passed.` (F-007 + supervisor isolation green). No new go.mod dep. Closes the task-080 security-audit hardening items. |
| 099 | Orchestrator CLI wiring (`orchestrate` subcommand; NewPlanStoreFromEnv; shared ReplayCache; policy gates; fleet audit; SEC-003 startup check) | 099-orchestrator-cli-wiring-test-spec.md | ✅ | ✅ | L3 fitness pass + L2 unit (all 8 TC real assertions). `go test -count=1 ./internal/cli/... ./internal/orchestrator/... ./internal/channel/worker/...` → `ok` (3 pkgs); `make fitness-orchestrator-no-executor` / `-worker-transport-isolation` / `-memoryguard-isolation` / `-no-self-repo-sink` → all `PASS`; `make check` → `All checks passed.`. `go test -count=1 ./tests/e2e/...` → `ok` (existing `run` path unbroken — REQ-099-02). L6 runtime-observed on dev host: `agent-builder orchestrate -h` → usage exit 0; `agent-builder orchestrate` with `AGENT_BUILDER_WORKER_SIGNING_KEY` unset → `error: AGENT_BUILDER_WORKER_SIGNING_KEY is not set: worker transport: missing signing key material` exit 1 (083 SEC-003 fail-closed before goal intake). **✅ promoted (2026-06-28):** spec-verifier APPROVE (all 8 TCs carry real value/ordering assertions; gate green; tree clean) **and** security-auditor APPROVE (all four security invariants — shared ReplayCache per direction, SEC-003 startup-before-intake, policy fail-closed incl. deny-on-error, self-repo bright line — verified in the live wiring code, not just tests; one Low non-blocking finding: discarded `envelope.GenerateKeyPair()` error in the in-process v1 seal path, hardening for the future out-of-process worker, not attacker-reachable now). L6 runtime observation (above) is the operator-observed evidence; L5 `audit-trail verify valid=true` and the 2-sub-goal live dispatch remain available on a dev host with the `audit-trail` binary present. |
| 100 | LLMPlanner (executor-backed `orchestrator.Planner` impl; ExecutorResolver seam; F-014 fitness; selectable via AGENT_BUILDER_PLANNER) | 100-llm-planner-test-spec.md | ✅ | 🟡 | L2+L3: `go test -count=1 ./internal/orchestrator/planner/... ./internal/orchestrator/...` → `ok …/internal/orchestrator/planner` + `ok …/internal/orchestrator`; `make fitness-llm-planner-no-executor` → `PASS F-014: internal/orchestrator/planner has no direct import of internal/executor.`; `make fitness-orchestrator-no-executor` → `PASS …` (F-010 stays green); `make check` → `All checks passed.` TC-100-01 stub resolver → 2-sub-goal Plan (coding-agent/docs-fix), Goal/GoalID asserted, resolver called once, prompt carries goal text, compile-time `var _ orchestrator.Planner = (*planner.LLMPlanner)(nil)`. TC-100-02 `target_repo=`/`sink=` parsed, metadata stripped from spec; assembled orchestrator (allow-all policy + dispatch spy) → own-repo sub-goal filtered, never dispatched (spy.sawTarget(OwnRepo)==false), non-own-repo dispatched (count==1). TC-100-03 A empty `""` → error + zero Plan; B garbage prose + comment-only → `ErrEmptyResponse` + zero Plan; C own-repo-only → filtered to zero → error; D resolver error / invoker error → wrapped error (`errors.Is`). TC-100-04 seam is `ExecutorResolver` interface (test imports router+registry, NOT executor). TC-100-05 F-014 in `.PHONY` + `fitness:` prereqs + SPEC.md + fitness-functions.md. TC-100-06 `NewPlannerFromEnv`: unset/structured → `*orchestrator.StructuredPlanner`, llm → `*planner.LLMPlanner`, magic → error. **spec-verifier APPROVE (2026-06-28): all 6 spec items / 9 TC rows verified with real assertions (no smoke tests); the `Invoker` seam deviation is spec-permitted ("an equivalent narrow interface") and breaks no assertion; gate green; tree clean.** **Status held at 🟡 — L5/L6 genuinely unreachable here: the only runtime path (`AGENT_BUILDER_PLANNER=llm` on the live binary) needs both a real model endpoint AND the deferred CLI router-resolver wiring (follow-up task). Promote to ✅ only after that L5/L6 run.** |
| 101 | Local-proxy auth fix (placeholder AUTH_TOKEN for claudeEnv local mode + local-only ConfigFromEnv gate) — CORRECTED 2026-06-28 post-merge | 101-local-proxy-auth-fix-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (corrected commit `d8136e6`, after BLOCK on three stale `ANTHROPIC_API_KEY` doc refs → fixed in `configuration.md`/`interfaces.md`/task file) + security-auditor SAFE (all four credential-isolation invariants hold under the new var; `run.go` byte-identical; new strip case for inherited `ANTHROPIC_AUTH_TOKEN`). L2/L3: `go test -count=1 ./internal/executor/... ./internal/runtime/...` → both `ok`; `make check` → `All checks passed.` (TC-101-01: `claudeEnv` local mode injects `ANTHROPIC_AUTH_TOKEN=LocalProxyAuthPlaceholder` + `ANTHROPIC_BASE_URL`, no API_KEY/OAUTH; TC-101-02 placeholder ≠ operator key; TC-101-03 cloud mode unchanged + no AUTH_TOKEN; TC-101-04 local-only registry accepted w/o cloud creds; TC-101-05 cloud entry still requires cred; TC-101-06 `NewClaudeCLIFromEntry` local env verified). **L6 confirmed (live orchestrator run, this host, 2026-06-28):** `go run ./cmd/agent-builder run` against a local Go target with a local-only registry and **no `ANTHROPIC_API_KEY`/`CLAUDE_CODE_OAUTH_TOKEN` in the env** → **FINDING 2 confirmed**: `ConfigFromEnv` accepted the local-only registry and the run started (`task 001 selected`, run record `run_started`); **FINDING 1 confirmed**: the `claude` CLI executor authenticated against the LiteLLM proxy via `ANTHROPIC_AUTH_TOKEN=local-proxy-no-auth` + `ANTHROPIC_BASE_URL=http://localhost:4000` — **the pre-fix `Not logged in · Please run /login` symptom is gone**; proxy logged 4+ `POST /v1/messages?beta=true 200 OK` from `qwen2.5-coder:7b`. The original merged fix (placeholder in `ANTHROPIC_API_KEY`) was proven non-working live before this correction (CLI still printed `Not logged in`, 0 proxy requests). NOTE: the downstream full branch+gate round-trip (REQ-094-02) did NOT complete — the local 7B model failed to write the produced-branch file (escalated) — but that is a **local-model-capability** limitation tracked under task 094, NOT a task-101 defect; task 101's own scope (REQ-101-01/02/03 auth + config) is fully confirmed live. |
| 098 | Outbound channel Reporter seam (ADR 046 D-1: `supervisor.Reporter` + Telegram `sendMessage` reply with ADR-045 seal+sign) | 098-outbound-reporter-seam-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-28, sonnet; all 8 TCs real assertions) + security-auditor (opus) SAFE TO MERGE (no CRITICAL/HIGH; crypto mirror faithful — orch Ed25519 signs, orch X25519 seals→operator, fresh per-msg nonce, no plaintext/secret on wire or in logs). **Carry into task 081 (consumer side):** SEC-001 (MEDIUM, latent) — add explicit `From`/`To` expected-role assertion after inbound `VerifyAndOpen` so cross-direction replay protection no longer rests solely on key-set separation. L3: `make fitness-envelope-isolation` → `PASS fitness-envelope-isolation: internal/envelope is not in internal/supervisor's dependency graph.`; `make fitness-supervisor-isolation` → `PASS`; `make check` → `All checks passed.`; L2: `make fitness-envelope-isolation` → `PASS fitness-envelope-isolation: internal/envelope is not in internal/supervisor's dependency graph.`; `make fitness-supervisor-isolation` → `PASS`; `make check` → `All checks passed.`; L2: `go test -count=1 ./internal/channel/telegram/... ./internal/supervisor/...` → ok (both packages). TC-098-03 round-trip asserted (`string(plaintext) == reportedText`); TC-098-04 tamper asserts `errors.Is(err, ErrBadSignature)` and wrong-signer errors.Is; TC-098-08 log-capture asserts token/key/PEM absent. Live bot `sendMessage` is L6 operator-run, deferred. |
| 102 | Ollama native API client (`internal/executor/ollamaclient/`; `/api/chat` request/response; structured tool_calls parsing; error handling) | 102-ollama-native-client-test-spec.md | ✅ | ✅ | L2/L3: `go test -count=1 ./internal/executor/ollamaclient/...` → `ok github.com/tkdtaylor/agent-builder/internal/executor/ollamaclient`; `make check` → `All checks passed.`; leaf verification: `go list -deps ./internal/executor/ollamaclient/...` shows only stdlib + own package (no `internal/` cross-imports). TC-102-01: stubbed `/api/chat` POST returns structured `tool_calls` — parsed correctly with exact `tool_calls[0].Function.Name` + `Arguments` fields intact. TC-102-02: plain-text `message.Content` response parses with empty `ToolCalls` slice (no panic on range). TC-102-03: non-200 HTTP (500/404) returns descriptive error containing status code + optional body truncation (256-byte cap). TC-102-04: malformed JSON response (decoder fails) returns wrapped "decode" error with context. TC-102-05: cancelled context surfaces via `errors.Is(err, context.Canceled)` on already-cancelled `ctx`. TC-102-06: `NewClient("")` + `NewClient("  ")` both reject with non-nil error containing "blank" or "base URL"; nil `*Client` returned. 0 new linter warnings; `errcheck` passes. No internal imports beyond the package itself — pure stdlib leaves (per ADR 051 design). Deferred to spec-verifier + main session for ✅ flip. **L6 PASSED (2026-06-28, this host):** native-harness orchestrator run — `qwen3:8b` via the `ollama-native` entry (no Claude CLI, no LiteLLM proxy) produced branch `task/001-add-product` (committed `f71191a`, pushed to origin) with ALL 7 gate steps green: `go build`/`go vet`/`go test`/`gofmt -l`/`golangci-lint`/`dep-scan`/`code-scanner`. Only `gh pr create` failed (deliberately-local remote). Full native-harness round-trip validated end-to-end. |
| 103 | Ollama agentic tool-execution loop (`executor.OllamaNative`; Chat→tool_calls→dispatch→append cycle; hard iteration cap; branch extraction) | 103-ollama-agentic-loop-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (after BLOCK on TC-103-05 smoke test → fixed to hard assertions `err != nil` + `errors.Is(err, context.Canceled)`, commit b75d69d). L2: `go test -count=1 ./internal/executor/...` → `ok`; L3: `make check` → `All checks passed.` TC-103-01 compile-time `var _ supervisor.Executor` interface assertion. TC-103-02 stub 2-call loop executes `write_file`, writes branch file, returns `Result{Branch:"task/103-test", OK:true}` (exactly 2 calls). TC-103-03 hard cap at 3 iterations returns `Result{OK:false}` nil-error (not panic). TC-103-04 second request includes `role:"tool"` message with tool name + non-empty content. TC-103-05 context-cancelled chatter → `errors.Is(err, context.Canceled)` (hard-asserted). TC-103-06 `make fitness-supervisor-isolation` PASS. `Run()` uses `context.Background()` (matches `ClaudeCLI` — the `Executor` seam carries no context). L5/L6 (live Ollama round-trip) operator-deferred. **L6 PASSED (2026-06-28, this host):** native-harness orchestrator run — `qwen3:8b` via the `ollama-native` entry (no Claude CLI, no LiteLLM proxy) produced branch `task/001-add-product` (committed `f71191a`, pushed to origin) with ALL 7 gate steps green: `go build`/`go vet`/`go test`/`gofmt -l`/`golangci-lint`/`dep-scan`/`code-scanner`. Only `gh pr create` failed (deliberately-local remote). Full native-harness round-trip validated end-to-end. |
| 104 | Worktree-confined Ollama tool set (write_file, read_file, list_dir, run_command allowlist, finish_branch; path-confinement + command-arg + env-isolation security) | 104-ollama-tool-set-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + **security-auditor SAFE** (commit bcf8dd4; re-audit re-executed the exploits to confirm closure). L2/L3: `go test -count=1 ./internal/executor/ollamatoolset/...` → `ok` (19 tests); `make check` → `All checks passed.` Original TC-104-01…10 all pass with exact-value assertions (write/read/list confinement; allowlist exactly `{git,go,gofmt,golangci-lint}`; finish_branch/ExtractBranch round-trip; 5 ToolSchemas). **Security hardening (SEC-104-01/02/03 closed):** (01, was Critical) dangling-symlink confinement bypass — `os.Lstat` rejects symlink leaf+parent, realpath-of-parent + prefix check; regression tests incl. symlink-via-`git checkout-index`; (02, High) argument escape — `checkCommandArgs` rejects `git -C/-c/--exec-path`, `go -C` before subprocess; (03, High) env leak — explicit minimal `cmd.Env` (PATH/HOME/GOCACHE/GOPATH) + `GIT_CONFIG_NOSYSTEM=1`/`GIT_CONFIG_GLOBAL=/dev/null`, asserted via env-probe regression test (sentinel secret absent from child). `go run` ACE is by-design, bounded by the outer exec-sandbox perimeter (per ADR 051). L5/L6 (live round-trip) operator-deferred. **L6 PASSED (2026-06-28, this host):** native-harness orchestrator run — `qwen3:8b` via the `ollama-native` entry (no Claude CLI, no LiteLLM proxy) produced branch `task/001-add-product` (committed `f71191a`, pushed to origin) with ALL 7 gate steps green: `go build`/`go vet`/`go test`/`gofmt -l`/`golangci-lint`/`dep-scan`/`code-scanner`. Only `gh pr create` failed (deliberately-local remote). Full native-harness round-trip validated end-to-end. |
| 105 | Ollama native harness: registry enum `HarnessOllamaNative` + `buildExecutorForEntry` wiring + spec doc updates | 105-ollama-harness-registry-wiring-test-spec.md | ✅ | ✅ | spec-verifier APPROVE. L2: `go test -count=1 ./internal/registry/... ./internal/runtime/... -v -run "Ollama\|TestBuildExecutorForEntry"` → all pass (TC-105-01, TC-105-02, TC-105-03, TC-105-04 all 4 real assertions, no smoke tests); L3: `make fitness-supervisor-isolation` → `PASS fitness-supervisor-isolation`; L2/L3: `make check` → `All checks passed.` TC-105-01: `HarnessOllamaNative == "ollama-native"` + `String() == "ollama-native"` + 3 existing harness constants unchanged (regression guards). TC-105-02: `LoadFromEnv()` with `HARNESS=ollama-native` entry parsed correctly, `Harness==HarnessOllamaNative`, `SecretRef==""` (local), `IsUnlimited()==true`. TC-105-03: `buildExecutorForEntry` with `HarnessOllamaNative` entry type-asserts to `*executor.OllamaNative` non-nil. TC-105-04: unknown harness still errors with "unknown harness" in message (regression). Spec updates: `interfaces.md` (HarnessOllamaNative + 4 harnesses now + ResolveExecutor routing), `configuration.md` (known entry IDs + `local-ollama` example + tool_calls model requirement), `data-model.md` (HarnessDriver value enum). L5/L6 (live Ollama round-trip with qwen3:8b) operator-deferred in same style as task 094/101. **L6 PASSED (2026-06-28, this host):** native-harness orchestrator run — `qwen3:8b` via the `ollama-native` entry (no Claude CLI, no LiteLLM proxy) produced branch `task/001-add-product` (committed `f71191a`, pushed to origin) with ALL 7 gate steps green: `go build`/`go vet`/`go test`/`gofmt -l`/`golangci-lint`/`dep-scan`/`code-scanner`. Only `gh pr create` failed (deliberately-local remote). Full native-harness round-trip validated end-to-end. |
| 106 | Native Ollama harness L6 integration fixes (tool-call `arguments` decode as object; loop commits worktree onto produced branch) | 106-fix-ollama-toolcall-args-test-spec.md | ✅ | ✅ | Two integration gaps found by the FIRST real L6 run of the native harness (102–105) — both masked by stubbed L2 tests. **REQ-106-01/02/03:** `ollamaclient.ToolCallFunction.Arguments` was typed `string` but Ollama's native `/api/chat` returns `arguments` as a JSON **object** → executor errored `cannot unmarshal object ... of type string` on chat call 1; fixed to `json.RawMessage`, loop forwards `string(args)` to Dispatch, stubs migrated to object form (TC-106-01/02/03). **REQ-106-04:** `finish_branch` only recorded a branch name and nothing committed the model's edits → the produced branch was an empty label and the gate verified uncommitted files; `OllamaNative.Run` now `git checkout -B <branch> && add -A && commit` on terminal completion (TC-106-04 asserts the branch contains the committed file + worktree left on the branch). L2: `go test -count=1 ./internal/executor/...` → `ok`; L3: `make check` → `All checks passed.` L6 (live qwen3:8b via native harness, this host): decode fix confirmed (loop runs full); commit fix confirmed (branch contains commit `agent-builder: complete task NNN` with `product.go`); a full-green-gate + committed-branch single run is gated only by 8B output variance (gate correctly fails runs with stray/broken files), not by the harness. **L6 PASSED (2026-06-28, this host):** native-harness orchestrator run — `qwen3:8b` via the `ollama-native` entry (no Claude CLI, no LiteLLM proxy) produced branch `task/001-add-product` (committed `f71191a`, pushed to origin) with ALL 7 gate steps green: `go build`/`go vet`/`go test`/`gofmt -l`/`golangci-lint`/`dep-scan`/`code-scanner`. Only `gh pr create` failed (deliberately-local remote). Full native-harness round-trip validated end-to-end. |

| 107 | Gate-failure feedback: Task contract + loop threading (`supervisor.Task.PriorFailure`; `loop.FormatFailure`; `RetryingLoop.RunOnce` threading; ADR 052) | 107-gate-failure-loop-threading-test-spec.md | ✅ | 🟡 | L2: `go test -count=1 ./internal/supervisor/... ./internal/loop/...` → all 9 test cases pass (TC-107-01/02/03/04/05/06/07/08 all PASS). L3: `make fitness-supervisor-isolation` → `PASS`; `make check` → `All checks passed.` — supervisor import graph unaffected (no new packages), all lint/build/test pass. No runtime-visible surface (internal data-contract change). Spec updated: `data-model.md` (`supervisor.Task.PriorFailure` field), `behaviors.md` (B-011 retry section now documents prior-failure threading). ADR 052 architect-approved contract met: `PriorFailure string` field on Task; `loop.FormatFailure(outcome) string` + `MaxFailureOutputBytes = 2000` exported; loop threads formatted failure after non-terminal OutcomeFail (first attempt empty, retries receive formatted failure); truncation respects UTF-8 rune boundaries. L5/L6 evidence deferred to task 108 (harness integration + operator run with converging retry). |
| 108 | Gate-failure feedback: harness prompt injection — all 4 executors (Claude/Codex/Gemini/OllamaNative include failure section when `PriorFailure` non-empty; ADR 052) | 108-gate-failure-harness-injection-test-spec.md | ✅ | ✅ | L2: `go test -count=1 ./internal/executor/... ./internal/loop/...` → all tests pass, including TC-108-01 through TC-108-09 (TestClaudePromptIncludesFailureSectionWhenPriorFailureSet, TestClaudePromptOmitsFailureSectionWhenPriorFailureEmpty, TestCodexPromptIncludesFailureSectionWhenPriorFailureSet, TestCodexPromptOmitsFailureSectionWhenPriorFailureEmpty, TestGeminiPromptIncludesFailureSectionWhenPriorFailureSet, TestGeminiPromptOmitsFailureSectionWhenPriorFailureEmpty, TestOllamaNativeInitialMessageIncludesFailureSectionWhenPriorFailureSet, TestOllamaNativeInitialMessageOmitsFailureSectionWhenPriorFailureEmpty, TestCrossHarnessFailureSectionConsistency). L3: `make fitness-supervisor-isolation` → PASS; `make check` → All checks passed. Spec updated: `behaviors.md` (B-011 now documents all four harnesses consuming PriorFailure for retry prompts). All four executors (Claude CLI, Codex CLI, Gemini CLI, OllamaNative) conditionally append failure section when `task.PriorFailure != ""` with consistent framing: "Your previous attempt failed the verification gate." + verbatim PriorFailure content. First attempt (`PriorFailure == ""`) prompts unchanged. **L6 PASSED (2026-06-29, this host):** spec-verifier APPROVE (all 11 TCs, exact assertions); live native-harness operator run — `qwen3:8b` via the `ollama-native` entry, `AGENT_BUILDER_MAX_ATTEMPTS=2`, `EXEC_BOX_RUNTIME=runc`, against a Go target seeded with an always-failing test. A logging proxy in front of `/api/chat` captured every initial `user` message: **attempt 1 (request #1)** carried the bare base prompt with NO failure section (omit-when-empty guard confirmed live); **attempt 2 (request #8)** carried `"Your previous attempt failed the verification gate."` followed by the verbatim `task.PriorFailure` content. The run escalated after 2 attempts (gate `FAIL go test ./...` on the seeded `TestSeededGateFailure`). Attempt 1 failed executor-incomplete, so the injected detail was the ADR 052 §4 "did not complete" variant — proving the loop→`OllamaNative.Run` injection path end-to-end and variant-agnostically on the live harness. |
| 109 | Single-shot `Completer` seam + ollama-native completer + `CompleterForEntry` fail-closed dispatcher (`internal/executor`; ADR 053 §1/§2) | 109-completer-seam-ollama-test-spec.md | ✅ | 🟡 | L2: `go test -count=1 ./internal/executor/...` → `ok github.com/tkdtaylor/agent-builder/internal/executor 0.165s` (TC-109-01/02/03/04/05/06 all PASS). L3: `make fitness-supervisor-isolation` → PASS; `make fitness-orchestrator-no-executor` → PASS; `F-014` → PASS; `make check` → `All checks passed.` Spec updated: `interfaces.md` (new `executor.Completer` seam section + `CompleterForEntry` dispatch contract + `ErrSingleShotUnsupported` sentinel). `Completer` in `internal/executor` (non-agentic counterpart to `Executor.Run`); `ollamaCompleter` wraps `ollamaclient.Chat` — one `{Role:"user"}` message, `Tools==nil`, `Stream==false`, returns `resp.Message.Content`; `CompleterForEntry` dispatches `HarnessOllamaNative`→`*ollamaCompleter` (nil/blank endpoint/model error), three cloud harnesses + unknown→`errors.Is(err, ErrSingleShotUnsupported)` (fail-closed, message names harness); `Chatter` error and cancelled-ctx both propagated wrapped, empty string returned on error path. L6 (live ollama round-trip) deferred to operator — no live harness attempted here. **spec-verifier APPROVE (2026-06-29; 9 assertion points across 6 TC IDs verified with real assertions — verbatim return, captured ChatRequest fields, fail-closed cloud arms, ctx threading; 0 lint, fitness green).** Held at 🟡: highest CI-reachable level is L3 (no L5 harness until task 110 wires the planner; L6 is operator-only live ollama). Verified by: L4: `make check` green + L3: fitness pass + spec-verifier APPROVE. |
| 110 | Wire `AGENT_BUILDER_PLANNER=llm` into `orchestrate` — catalog + `ExecutorResolver` over `router.Select` + `Invoker` over `CompleterForEntry`; remove `ErrPlannerNotAvailable` (ADR 053 §3) | 110-wire-llm-planner-orchestrate-test-spec.md | ✅ | 🟡 | Code merged (ADR 053 §3, task 110). Catalog-build option **(b)** chosen: CLI builds its own `*registry.Catalog` from `registry.LoadFromEnv()` + synthetic-default fallback (`defaultCLIClaudeEntryID`, CapabilityTier=1, HarnessClaudeCLI); `internal/runtime` unchanged. **TC-110-01:** `AGENT_BUILDER_PLANNER=llm` → `*llmplanner.LLMPlanner`, nil err, `!errors.Is(err, ErrPlannerNotAvailable)` (symbol removed); unset/`structured` → `*orchestrator.StructuredPlanner`. **TC-110-02:** `=magic` → error naming value + listing `structured`/`llm`; `Main(orchestrate)` → `ExitUsage` (2) via `errUsageConfig` sentinel. **TC-110-03:** `routerResolverAdapter.Resolve(ctx, spec)` delegates to `router.Select`, returns `local-ollama` entry; `MinCapability=99` → `errors.Is(err, router.ErrNoEligibleExecutor)`; cancelled ctx → no change (drops ctx, documented limitation). **TC-110-04:** ollama entry → non-nil completer, no `ErrSingleShotUnsupported`; cloud entry → `errors.Is(err, executor.ErrSingleShotUnsupported)`. **TC-110-05:** drives the REAL `assembleOrchestrate` under `=llm` with NO planner override (so `plannerFromEnv` runs) and captures the exact planner handed to `orchestrator.New` via the test-only `assembleOverrides.onPlanner` seam — asserts it is `*llmplanner.LLMPlanner` (invoked exactly once), proving the planner seam survives the task-112 control-loop rewrite and reaches the orchestrator on the async path (not merely that `plannerFromEnv` returns the right type — that is TC-110-01); F-010/F-014 fitness targets both PASS. **TC-110-06:** `orchestrateUsage` has no "pending task 100"; `configuration.md` PLANNER row updated to describe `llm` as live + ollama-only + cloud fail-closed. `ErrPlannerNotAvailable` removed (no dead exported symbol). `interfaces.md` orchestrate row updated. **L2:** `go test -count=1 ./internal/cli/... ./internal/orchestrator/... ./internal/orchestrator/planner/... ./tests/e2e/...` → `ok` each. **L3:** `make fitness-orchestrator-no-executor` → `PASS F-010`; `make fitness-llm-planner-no-executor` → `PASS F-014`; `make fitness` → `All fitness checks passed.`; `make check` → `All checks passed.` (lint **0 issues** with a freshly-cleaned golangci-lint cache — an earlier "codex_cli_test.go:37 errcheck" was a stale-cache artifact pointing at a deleted worktree path; the real line carries `//nolint:errcheck`). TC-110-05 was reworked from a duplicate-of-TC-110-01 into a real assembly-driven assertion (via the `onPlanner` seam) after a spec-verifier BLOCK on exactly that gap; **spec-verifier APPROVE (2026-06-29; re-verified the reworked TC-110-05 — confirmed it drives the real `assembleOrchestrate`, the `onPlanner` hook captures the exact value fed to `orchestrator.New`, the assertion is non-vacuous, and the seam is nil/inert in production; all 7 items verified; lint 0 issues, fresh cache).** **L6 (operator-only):** live `orchestrate` with `=llm` against local ollama `qwen3:8b` — residual, deferred to operator. **Held at 🟡.** Verified by: L2: `ok ./internal/cli` + L3: `All fitness checks passed.` + spec-verifier APPROVE. |
| 111 | SEC-001 hardening — propagate the discarded `envelope.GenerateKeyPair()` error in `newTransportDispatch` (task-099 audit finding; ADR 053 Consequences) | 111-sec001-keypair-error-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (2026-06-29; all 13 per-assertion checks verified — fault-injection seam wired, error propagation exercised end-to-end through `assembleOrchestrate`, no smoke tests) + L4: CI green + L3: fitness pass (`fitness-orchestrator-no-executor`, `fitness-worker-transport-isolation`). L2: `go test -count=1 ./internal/cli/...` → `25 PASS, 1 SKIP (L5-guarded), 0 FAIL`; `make check` → `All checks passed.` TC-111-01: fault-injected seam → nil dispatcher + non-nil err wrapping errFakeRand; TC-111-02: assembleOrchestrate returns err, zero config, goal source never read; TC-111-03: real keygen → non-nil dispatcher + nil error, replay-cache round-trip clean. **unit-test-only; no runtime surface** — L5/L6 not reachable and not required per task verification plan. |
| 112 | Async control-loop core + actor-per-goal + live status registry — replace serial `runGoalIntakeLoop` with a non-blocking control loop; fleet-wide `MAX_WORKERS` semaphore inside `dispatchPlan`; `MAX_GOALS` admission cap; mutex-guarded status registry (ADR 054 §1/§3) | 112-async-control-loop-core-test-spec.md | ✅ | 🟡 | Code merged (ADR 054 §1/§3/§6). New: `internal/orchestrator/registry.go` (StatusRegistry projection, GoalState enum), `internal/orchestrator/semaphore.go` (weighted semaphore on buffered channel; no new dependency), `internal/cli/asynccore_test.go` (TC-112-01/02/04 control-loop tests), `internal/orchestrator/asynccore_test.go` (TC-112-03/05/06/07 concurrency tests). **TC-112-05 (real ordered-transition assertion):** a latched planner + latched dispatch hold the goal at each phase while the test samples the LIVE registry — asserts Queued (pre-Handle) → Planning (planner held) → Dispatching + sub-goal `running` (dispatch held) → Done + sub-goal `done` (after join), in order, not inferred from the terminal state. **TC-112-06 (chain validity + no event loss):** 3 goals × 2 sub-goals concurrent → exact per-action event counts (27 total: 3 goal-intake + 3 plan-decided + 6 spawn-decided + 6 containment + 6 finish + 3 completion) proving no loss/dup under contention on the one mutex-guarded sink; **L3 chain-validity confirmed against the real audit-trail binary** (`AGENT_BUILDER_AUDIT_BIN=…/audit-trail`): `audit-trail verify → valid=true on 27-event 3×2 chain`. **Real `-race` bug found+fixed during impl:** `logReporter.Report` wrote `config.Stdout` unguarded under M concurrent actors → now mutex-guarded (+ removed the control loop's duplicate stdout write). **Verified by — L2 (`-race`):** `go test -race -count=1 ./internal/cli/... ./internal/orchestrator/...` → `ok …/internal/{cli,orchestrator,orchestrator/planner}`, **no race report**; **L3:** `AGENT_BUILDER_AUDIT_BIN=…/audit-trail go test -race -run TestTC112_06…` → `verify valid=true`; `make fitness-orchestrator-no-executor`/`-audit-isolation` PASS; `make check` → `All checks passed.` Spec updated same commit: configuration.md (`MAX_WORKERS`/`MAX_GOALS`), behaviors.md (B-027), diagrams.md. spec-verifier APPROVE pending re-run after the TC-112-05/06 assertion fixes. **Held at 🟡 — residual L6 operator-only:** live `orchestrate` binary with low `MAX_WORKERS` observing concurrent dispatch under the cap. |
| 113 | Inbound message protocol + command router — new `supervisor.MessageSource` seam + typed `Message`/`MessageKind` (`new-goal`/`status`/`info`/`cancel`); line-oriented env/stdin source; control-loop router dispatches by kind (ADR 054 §2) | 113-inbound-message-protocol-test-spec.md | written | 🟡 | Code merged (ADR 054 §2). New: `internal/supervisor/message.go` (`MessageKind`/`Message`/`MessageSource` — pure-stdlib, F-003 clean), `internal/cli/router.go` (`envMessageSource` line parser + `commandMailboxes` map + `ErrMalformedInput`), `internal/cli/router_test.go` (TC-113-01…06). `runControlLoop` generalized to read `MessageSource` + route by kind (new-goal→actor with mailbox-created-before-register; status→handler seam, body=114; info/cancel→addressed mailbox; unknown→graceful "no such goal"). Dead `envGoalSource`/`newEnvGoalSource` removed (superseded by `envMessageSource`); goal-only `GoalSource` overrides adapted via `goalSourceAdapter`. **TC-113-01** compile-time `var _ supervisor.MessageSource = (*envMessageSource)(nil)` + `GoalSource` signature-unchanged asserts (`stubGoalSource`/`recordingGoalSource` still satisfy goal-only). **TC-113-02** table-driven parse maps each line→Kind/GoalID/Text incl. EOF→`ok=false`; separate test proves malformed `cancel`/`info goal-7` → `ErrMalformedInput`, `ok=false`, no panic, not a new-goal. **TC-113-04** scripted 4-kind source against the LIVE `runControlLoop`: new-goal spawns actor (latch-held), info+cancel land in goal-1's mailbox (read back: MsgInfo+text, then MsgCancel), status handler invoked once with empty(fleet) GoalID. **TC-113-05** unknown goal-X: ≥2 "no such goal: goal-X" Reporter reports (info+cancel), status routed to handler, no panic, no mailbox auto-created. **TC-113-06** (`-race`) new-goal goal-2 + cancel goal-2 with actor latch-held post-registration → cancel delivered to mailbox without loss (register-then-start). **Verified by — L2 (`-race`):** `go test -race -count=1 ./internal/cli/...` → `ok …/internal/cli`, **no race report** (all 7 TC-113 PASS); message types clean. **L3:** `make fitness-supervisor-isolation` PASS; `make check` → `All checks passed.`; `make fitness` → `All fitness checks passed.` Spec updated same commit: interfaces.md (`MessageSource` seam + `Message`/`MessageKind`; orchestrate row), configuration.md (stdin command grammar + GOAL_SPEC-first semantics). **Dep: 112.** **Held at 🟡 — residual L6 operator-only:** live `orchestrate` binary driven over stdin with all four kinds; NOTE the spec-listed full `-race` gate `./internal/supervisor/... ./internal/cli/...` also surfaces TWO **pre-existing** supervisor timeout-test races (`TestRunTimeout*`, present on pristine main `d35d761`, in the `runLoop` path task 113 never touched) — task-113 surface is `-race` clean. **spec-verifier APPROVE (2026-06-29; all 6 TC verified with behavior-sensitive assertions — live `runControlLoop` dispatch, mailbox round-trip read-back, register-then-start under `-race`; negative-controls confirmed non-vacuous; pre-existing supervisor races confirmed on pristine main and unrelated).** |
| 114 | Status-query handler + immediate reporter answer — a `status` message reads the registry and answers over the Reporter **without** blocking on `Handle`; fleet + per-goal render with sub-goal progress (ADR 054 §3) | 114-status-query-handler-test-spec.md | written | 🟡 | Code merged (ADR 054 §3). New: `internal/cli/status_handler.go` (`newStatusHandler`, `renderFleetStatus`, `renderGoalStatus` — pure registry read + Reporter write; no Handle/Resume path). `assembleOrchestrate` wires the live handler when no test override is set (`newStatusHandler(context.Background(), registry, reporter)` — registry + reporter captured at assembly time). `docs/spec/behaviors.md` updated: B-028 updated ("status handler is live, B-029"); B-029 added (immediate registry snapshot, fleet vs per-goal render shapes, no-panic empty SubGoals, load-bearing no-block property). **TC-114-01** (`-race`) control loop with a dispatch-counting spy: goal-1 enters dispatch (latch held → Dispatching); status reply mentioning goal-1 arrives in Reporter spy; dispatch count = 1 (the actor's own Handle), NOT 2 — proves status never called Handle/Resume. **TC-114-02** (`-race`) fleet status (empty goalID): `reg.Register` three goals at Planning/Dispatching/Done; `handler("")` → one Reporter reply containing goal-1/goal-2/goal-3 + all three state strings + exactly 3 `goal-` entries (no duplicates). **TC-114-03** (`-race`) per-goal goal-7 with two sub-goals (auth/coding-agent=done, docs/docs-fix=running): reply contains goal-7 + Dispatching + auth/coding-agent/done + docs/docs-fix/running; separate test for empty SubGoals (no panic, state Planning rendered). **TC-114-04** (`-race`) channel-driven source: new-goal goal-1 → latch entered; status goal-1 sent while latch held; `replyArrived` channel closed by polling goroutine when Reporter contains goal-1+dispatching; `select` on `replyArrived` times out or passes BEFORE `disp.releaseGoal` — proves reply arrived before latch released. **Verified by — L2 (`-race`):** `go test -race -count=1 ./internal/cli/... ./internal/orchestrator/...` → `ok …/internal/cli 1.053s` + `ok …/internal/orchestrator 1.238s`, **no race report** (all 5 TC-114 PASS). **L3:** `make fitness` → `All fitness checks passed.` `golangci-lint run ./internal/cli/...` → `0 issues.` (pre-existing stale-worktree lint noise from task 110 cache is unrelated — confirmed present on pristine main; `golangci-lint run --new-from-rev=HEAD~1` → `0 issues`). **spec-verifier APPROVE (2026-06-29; all 4 REQ/5 TC verified — TC-114-01's zero-additional-Handle property confirmed non-vacuous (handler closure structurally cannot reach `o.orch`; count==1 = setup Handle only), TC-114-04 latch-ordering observed via channel; pre-existing supervisor `-race` failures confirmed unrelated).** **Dep: 112, 113.** **Held at 🟡 — residual L6 operator-only:** live `orchestrate` binary with a real goal mid-flight; `status` mid-flight; immediate registry reply observed with goal state + sub-goal progress. |
| 115 | Apply-info-at-checkpoint — per-goal pending-info queue; surface with approval solicitation; on approve re-plan augmented goal + replace stored plan; for dispatched goals spawn `G/amend-N` through normal gates; running workers never mutated mid-task (ADR 054 §4) | 115-apply-info-at-checkpoint-test-spec.md | complete | 🟡 | **Code merged (🟡).** All 6 REQ/TC met with real assertions: TC-115-01 info→pending-info queue (exactly one entry on the AwaitingApproval branch) + held worker untouched (G-sub-0 dispatched exactly once, G stays Dispatching); TC-115-02 `SolicitApproval` reply includes the queued info text; TC-115-03 `ResumeWithFold` calls `planner.Plan` a 2nd time with augmented text (original+info captured), dispatch uses re-planned `P1`'s sub-goal not `P0`'s, queue drained; TC-115-04 dispatched goal→`G/amend-1`+`G/amend-2` planned+gated+dispatched, G originals unaffected; TC-115-05 held worker never mutated (both branches); TC-115-06 own-repo amendment + policy-denied amendment NOT dispatched, ≥2 SEC-003 spawn-decided deny audits, IDs distinct. **Verified by:** L2 `go test -race -count=1 ./internal/cli/... ./internal/orchestrator/...` → `ok` (no race; stable ×8) + L3 `make check` → `All fitness checks passed.` / `All checks passed.` L5/L6 (live info during AwaitingApproval / to a dispatched goal on the binary) is operator-only — residual, see Verification plan. **spec-verifier APPROVE (2026-06-29; all 6 REQ/TC non-vacuous — held-worker-untouched via real latch + structural "queue read only at checkpoints, never in dispatchPlan"; TC-115-03 captures both texts in the 2nd planner.Plan call + asserts P1 dispatched + queue drained; TC-115-06 asserts ≥2 SEC-003 deny audits, not just non-dispatch; modified task-113 router tests confirmed still genuine).** **Dep: 112, 113.** Mutually independent of 114/116. |
| 116 | Cancellation + worker teardown — thread `context.Context` through `runtime.Run`→`Supervisor.Run` run-loop `select` (`case <-ctx.Done():` beside wall-clock arm, reusing `box.Kill`/`Teardown`); per-goal cancel ctx; permit release; `errors.Join` partial teardown (ADR 054 §5) | 116-cancellation-worker-teardown-test-spec.md | complete | 🟡 | **Code merged (🟡).** `context.Context` threaded as the leading param of `runtime.Run`/`Supervisor.Run`; all callers updated (`RunFromEnv`, `defaultDispatch`, the `newTransportDispatch` closure, `cli.Main`'s single-task path passes `context.Background()`, all runtime/supervisor/sandbox tests). Run-loop `select` gains `case <-ctx.Done():` beside `case <-timer.C:` — both call a shared `killAndJoin` that runs the SAME `box.Kill`/`Teardown` path and ALWAYS joins the loop goroutine (this also fixed the pre-existing kill-error goroutine-leak/race). New `ErrRunCancelled`. Per-goal `context.WithCancel` derived at `routeNewGoal`, `CancelFunc` stored in `StatusRegistry` (new `SetCancelFunc`/`Cancel`); the actor's `MsgCancel` handler (`applyCancel`) fires it, sets `Cancelled`, and `Orchestrator.ConsumePlanOnCancel` removes the plan under the same delete path `Resume` uses; `dispatchPlan` no longer overwrites terminal state with `Done` when `ctx.Err()!=nil`. Permit release is the existing deferred `Semaphore.Release` (Acquire returns `ctx.Err()` when parked). **All 6 TC met with real assertions:** TC-116-01 `Handle` forwards a cancellable ctx to dispatch (dispatch observes `ctx.Done()`, blocks forever if dropped) + every suite green; TC-116-02 far-future timer + cancel → stub box records Kill then Teardown once each, `ErrRunCancelled`, `event=box.kill.cancel` logged; TC-116-03 cancel G (G,H held at latch) → G's worker observed torn down by ctx (dispatch returned `ctx.Err()`), H untouched, `Get("G")==Cancelled`, `HasPendingPlan("G")==false`, later `Resume(G)` errors + no re-dispatch; TC-116-04 `MAX_WORKERS=1` shared sem, H parks while G holds permit, cancel G → H acquires+dispatches AND `sem.TryAcquireN(1)` succeeds post-drain (no leak); TC-116-05 `box.Kill` error → `errors.Join`'d onto `ErrRunCancelled` (surfaced not swallowed), Teardown still runs; TC-116-06 both orderings (cancel-first/approve-first) → dispatch spy for G called at most once (no double-dispatch), `-race` clean. **Verified by — L2 (`-race`):** `go test -race -count=1 ./internal/cli/... ./internal/orchestrator/... ./internal/supervisor/... ./internal/runtime/...` → `ok` each, **no race report**. **Pre-existing supervisor races FIXED:** `TestRunTimeoutUsesConfiguredDeadlineAndKillsBox` + `TestRunTimeoutSurfacesKillErrorAndStillTearsDown` were FAIL-with-DATA-RACE on pristine main; now PASS `-race` clean (root cause: kill-error path returned without joining the loop goroutine + unsynchronized test-fake `callLog` — both fixed). **L3:** `make fitness` → `All fitness checks passed.`; `make check` → `All checks passed.` Spec updated same commit: interfaces.md (`Run`/`RunFromEnv` ctx signature + DispatchFunc forward note + cancel handler body), behaviors.md (B-013 updated + B-031 added), diagrams.md (run-loop ctx.Done() arm + per-goal cancel flow, date bumped). **Dep: 112, 113.** **Spans 3 modules (runtime+supervisor+cli) by design — kept whole per ADR 054 §Recommended-decomposition.** **Held at 🟡 — residual L6 operator-only:** live `orchestrate` with a real sandbox box (`EXEC_BOX_RUNTIME=runc`), cancel mid-flight, observe `box.Kill`/`Teardown` in logs + state Cancelled + permit freed. Mutually independent of 114/115. **spec-verifier APPROVE (2026-06-29, after one BLOCK→fix round on TC-116-05: all 7 cases verified non-vacuous; TC-116-05 leak surfacing is now in PRODUCTION `killAndJoin` (`box %q (worktree %s) leaked — operator attention required: %w`) reaching `outcome.Detail`→`RenderPlanResult`, with real assertions for outcome Failed + rendered leak substring + timer-not-disabled + loud log; error chain preserved via `%w`; both `TestRunTimeout*` confirmed `-race` clean in production via the `killAndJoin` join).** |
| 117 | Telegram wiring (message-aware), LAST — `telegram.Adapter` emits typed `Message`s (kind/GoalID derived at adapter edge); `ReplyAdapter` outbound; wired into `assembleOrchestrate` behind `AGENT_BUILDER_INBOUND=telegram`; per-message goal IDs (ADR 054 §2) | 117-telegram-message-wiring-test-spec.md | complete | 🟡 | **Code merged (🟡).** `telegram.Adapter.Next()` now returns `(supervisor.Message, bool, error)` — satisfies `supervisor.MessageSource`. `goalIDCache` (mutex-guarded) threads reply-to goalIDs for status/info/cancel. Kind/GoalID derivation in `deriveMessage` after envelope-verify+armor (security invariant: derivation never runs on unverified text). `inboundFromEnv` + `assembleTelegramInbound` in `internal/cli/orchestrate.go` select Telegram vs env/stdin via `AGENT_BUILDER_INBOUND`; all required `AGENT_BUILDER_TELEGRAM_*` vars fail-fast at assembly with clear error messages (never nil-adapter panic). `allowAllContentGuard` (fail-open) used when no armor binary is configured — this is a NEW fail-open default introduced with the first live Telegram inbound wiring (there was **no** prior no-armor fallback on the run path; the load-bearing trust gate here is the always-enforced envelope-verify pipeline — Ed25519+X25519+AEAD+replay — armor being an additional injection filter over already-authenticated operator plaintext). All 6 TCs pass with real assertions: TC-117-01 (compile-time `var _ supervisor.MessageSource` + `Next()` emits `MsgNewGoal` with `Goal.Spec` and derived `tg-<chatID>-<msgID>` goalID); TC-117-02 (table-driven: bare→`MsgNewGoal`+goalID; `status` bare→`MsgStatus` empty GoalID fleet; `status` reply-to→threaded goalID; `info reply-to→MsgInfo`+Text; `cancel` reply-to→`MsgCancel`; empty text→no panic/MsgNewGoal); TC-117-03 (`ReplyAdapter.Report` → exactly 1 POST, sealed envelope not plaintext, envelope fields non-empty); TC-117-04 (`inboundFromEnv`: unset+env→env/stdin+nil reporter; telegram+keys→`*telegram.Adapter`+`*telegram.ReplyAdapter`; missing each required var→fail-fast error naming the var; unknown value→`errUsageConfig`); TC-117-05 (two distinct msgIDs→distinct goalIDs, no collision; reply-to msgID=1 threads goal-A not goal-B); TC-117-06 (tampered signature dropped before derivation, audit event `unknown_key`; armor-block dropped, audit event `armor_blocked`; neither emits `MsgCancel`). **Verified by:** L2 `go test -race -count=1 ./internal/channel/telegram/... ./internal/cli/...` → `ok` (no race); L3 `make check` → `0 issues. All checks passed.`; `make fitness` → `All fitness checks passed.` Spec updated same commit: configuration.md (`AGENT_BUILDER_INBOUND` row + `AGENT_BUILDER_TELEGRAM_*` rows), diagrams.md (Telegram inbound channel paragraph + date bump). L6 (real bot: drive new-goal/status/info/cancel from Telegram chat, observe routing end-to-end) is operator-deferred. **spec-verifier APPROVE (2026-06-29; all 6 TC verified non-vacuous — goalID derivation incl. reply-to threading, kind mapping, config selection both directions, ReplyAdapter reuse, envelope-pipeline drop-before-derive all asserted on concrete inputs).** **Operator follow-up (security):** the no-armor fail-open default on `AGENT_BUILDER_INBOUND=telegram` should be replaced with a real armor guard (executorharness `NewArmorGuarded` pattern) before production; the false "existing run-path no-armor fallback" claim in the original comments was corrected in this commit. **Held at 🟡.** |
| 118 | Plan-derived authorization on orchestrate — orchestrator-side gate (ADR 055 seam 1): `Plan.AllowedResources()` = deduped {GoalID}∪{recipe names}∪{task IDs}; `decideSpawnWorker` denies an out-of-plan recipe/task WITHOUT consulting policy, in-plan resources proceed to the policy engine; effective decision = `plan-allows ∧ policy-allows` (policy stays the independent ceiling). Daemon-side allow feeding split to task 122. | 118-orchestrate-plan-derived-authz-test-spec.md | complete | 🟡 | **Code merged (🟡).** L2: `go test -count=1 ./internal/orchestrator/...` → `ok` (5 new tests: TC-001 AllowedResources set, TC-002 dedup, TC-003 in-plan→policy allow, TC-004 out-of-plan denied with 0 policy calls, TC-005 in-plan∧policy-deny→deny). L3 `make check` green. spec-verifier pending. L5/L6 reached once 119–122 land (end-to-end dispatch). |
| 119 | Route the dispatched sub-goal task to the worker (ADR 055 seam 2) — worker executes `sub.Task`, not just `TaskRoot` files | 119-orchestrate-route-task-to-worker-test-spec.md | complete | 🟡 | **Code merged (🟡).** L2: `go test -count=1 ./internal/cli/... ./internal/runtime/...` → `ok` (TC-001 `TestTC119_01_DispatchedTaskDrivesWorker`: worker spy records `cfg.DispatchedTask` matching dispatched `{ID:"goal-1-0", Spec:"add a Reverse function", Repo:"exec-sandbox"}`; TC-002 `TestTC119_02_RunPathUsesTaskSourceWhenDispatchedTaskNil`: spy recipe's GoalSourceFactory called, tasksource picks seeded task ID "001" — run path unchanged; TC-003 `TestTC119_03_BlankTaskIDIsHardError` + `TestTC119_03_BlankTaskSpecIsHardError`: both return descriptive error, worker never called). L3: `make check` → `All checks passed.` (all fitness checks green). Producer-consumer trace: orchestrate dispatch closure (orchestrate_seams.go) → validates blank ID/Spec → sets `cfg.DispatchedTask = &sub.Task` → calls `runWorker(ctx, cfg, …)` → `runtime.Run` → `singleTaskSource{task: *config.DispatchedTask}` used when non-nil. spec-verifier pending. L5/L6 in end-to-end orchestrate run (with 118/120/121). |
| 120 | Propagate the worker's real result (ADR 055 seam 3) — replace hardcoded `Result{OK:true}` with the worker's actual outcome | 120-orchestrate-propagate-result-test-spec.md | complete | 🟡 | **Code merged (🟡).** L2: `go test -count=1 ./internal/cli/...` → `ok` (TC-001 `TestTC120_01_SuccessfulRunReportsOK`: spy nil → dispatch nil, reporter success message names sub-goal; TC-002 `TestTC120_02_FailedRunReportsNotOK`: spy gate-failure error → dispatch non-nil, error text carried, reporter failure message names sub-goal; TC-003 `TestTC120_03_IdleRunReportsNotDone`: spy idle error → dispatch non-nil, not "completed", reporter names sub-goal). L3: `make check` → `All checks passed.` + `All fitness checks passed.` Producer-consumer trace: `runWorker` (runtimewiring.Run seam) outcome → `result := supervisor.Result{OK: runErr == nil}` → sealed into return-path envelope → `reporter.Report` receives success/failure text. A failed run does NOT surface as OK. spec-verifier pending. L5/L6 in end-to-end orchestrate run (118/119/121 stack). |
| 121 | Blocked-action feedback + reevaluation (ADR 055 seam 4) — denied necessary action → typed feedback → bounded replan/reevaluate → independent human escalation; agent never self-grants | 121-orchestrate-blocked-action-feedback-test-spec.md | complete | 🟡 | **Code merged (🟡).** L2 (`-race`): `go test -race ./internal/loop/... ./tests/loop/... ./internal/orchestrator/... ./internal/cli/...` → all `ok`. TC-001 (`tests/loop` `TestBlockedActionClassifiedAsDistinctFailureKind`): `loop.ClassifyBlockedAction` → `FailureBlockedAction`, asserted NOT `FailureGate`/`FailureExecutorError`/`FailureExecutorIncomplete`, `Blocked.{Resource,Action,Reason}` populated. TC-002 (`TestReevaluationPerformsExactlyNReplansBeforeEscalation` N=1,3): exactly N replans precede escalation; `TestReevaluationResolvesWhenReplanRoutesAround`, `TestReevaluationZeroBoundEscalatesImmediately`, `TestReevaluationNegativeBoundIsExplicitError`. TC-003 (`TestEscalationCarriesDeniedActionAndReason`): `Escalation.Status == needs-human`, `Reason()` contains resource+action+deny-reason, single needs-human write. TC-004 (`TestReevaluationNeverUnionsDeniedResourceIntoAllowSet` + `TestReevaluationEscalatesRatherThanGrantsWhenStillNeeded`): applied allow set == re-derived `Plan.AllowedResources` via `reflect.DeepEqual`, denied resource never unioned back; still-needed → escalation, never self-grant. Live producer/consumer (`internal/orchestrator/blocked_action_test.go`): `TestDispatchOneProducesBlockedActionOnSpawnDeny`, `TestReevaluateBlockedSpawnEscalatesWhenStillNeeded`, `TestReevaluateBlockedSpawnResolvesWhenReplanRoutesAround`. L3: `make check` green. Producer→consumer trace: `decideSpawnWorker` deny → `classifyBlockedSpawn` → `SubGoalOutcome.Blocked` (producer); `ReevaluateBlockedSpawn` → `ReplanFunc` re-runs `planner.Plan` → `Plan.AllowedResources()` → `loop.ReevaluationPolicy.ReevaluateBlocked` → needs-human + `Escalation` (consumer). spec-verifier pending. L5/L6 in end-to-end orchestrate run (118/119/120/121 stack). |
| 122 | Feed the policy daemon the plan-scoped allow set (ADR 055 seam 1, daemon side) — daemon configured with `Plan.AllowedResources()` ∩ optional `AGENT_BUILDER_POLICY_ALLOW`; fixes empty-allowlist denial so the independent engine allows in-plan resources | 122-orchestrate-policy-plan-scoped-allow-test-spec.md | complete | 🟡 | **Code merged (🟡).** Verified by L3: `make check` (fitness pass) + L2: `go test ./internal/cli/... ./internal/orchestrator/...` → `ok`. Pure helper `effectiveAllow(plan, base)` (`internal/cli/orchestrate.go`): TC-002/003/004 via `TestEffectiveAllowTable` (narrow/empty/disjoint rows) + `TestEffectiveAllowWhitespaceBaseIsFullSet` + `TestParseAllowBaseTrimsAndDropsBlanks`. Cross-module (daemon RECEIVES the derived set via `daemonStarter` recording `--allow` argv): TC-001 `TestDaemonReceivesPlanDerivedAllowNoBase` (set-equality, order-insensitive), TC-002 `TestDaemonReceivesIntersectionWithBase`, TC-003 `TestDaemonReceivesFullSetWhenBaseEmpty`, TC-004 `TestDaemonReceivesEmptyAllowOnDisjointBaseFailClosed` + `TestDecideBeforeConfigureIsDeny` (no daemon → deny). Live wire (orchestrator): `TestHandleConfiguresPolicyForPlanBeforeDeciding` (Handle calls `PlanScoper.ConfigureForPlan` before `decideSpawn`), `TestHandleFailsClosedWhenConfigureForPlanErrors`. Producer→consumer: `Plan.AllowedResources()` ∩ base (`effectiveAllow`, producer) → `planScopedPolicy.ConfigureForPlan` → daemon `--allow` (consumer); wired live by `Orchestrator.Handle` via the optional `PlanScoper` seam. Fail-closed preserved: empty effective set → empty `--allow` → deny; pre-config Decide → deny; orchestrator plan-gate (118) + self-repo bright line untouched. spec-verifier pending. L5/L6 in end-to-end orchestrate run (118/119/120/122 stack) — pending main-session run. |

| 123 | Wire dispatchPlan → ReevaluateBlockedSpawn — close ADR 055 seam 4 end-to-end: post-join reevaluation loop in `dispatchPlan` calls `ReevaluateBlockedSpawn` for each `outcome.Blocked != nil`; folds `loop.ReevaluationOutcome` into `SubGoalOutcome`; `WithStatusWriter` option + CLI wiring; reevaluation bound from `AGENT_BUILDER_MAX_ATTEMPTS` (or `AGENT_BUILDER_MAX_REEVALUATIONS` if split) | 123-orchestrate-wire-blocked-reevaluation-test-spec.md | complete | 🟡 | **Code merged (🟡).** L2 (`-race`): `go test -race -count=1 ./internal/orchestrator/... ./internal/cli/...` → `ok`. L3: `make check` + `make fitness` green. Post-join reevaluation loop wired in `dispatchPlan`; `WithStatusWriter` option + CLI wiring; bound from `AGENT_BUILDER_MAX_ATTEMPTS`. spec-verifier APPROVE pending re-run on the merged tree. L5/L6 in end-to-end orchestrate run. |
| 124 | `MsgConfirm` message kind (protocol only) — append `MsgConfirm=4` to `MessageKind` enum in `internal/supervisor/message.go`; `String()` returns `"confirm"`; update `docs/spec/interfaces.md` + `data-model.md`; author ADR 058 before implementation (ADR 058 — conversational human-gated front door) | 124-msgconfirm-message-kind-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L2: `go test -count=1 ./internal/supervisor/...` -> `ok (3 tests pass)`. unit-test-only; no runtime surface. |
| 125 | CLI confirm grammar — extend `parseMessageLine` in `internal/cli/router.go` with a `"confirm"` case: `confirm <goalID>` → `MsgConfirm`; missing goalID → `ErrMalformedInput`; bare `go`/`proceed` deferred; update `docs/spec/interfaces.md` grammar table (ADR 058) | 125-cli-confirm-grammar-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L2: `go test -count=1 ./internal/cli/...` -> `ok (4 tests pass)`. unit-test-only; no runtime surface. |
| 126 | Telegram confirm derivation — extend `deriveMessage` in `internal/channel/telegram/adapter.go` to recognize `confirm`/`go`/`proceed` reply-to keywords → `MsgConfirm` with goalID from `goalIDCache`; no reply-to → `MsgNewGoal`; update `docs/spec/interfaces.md` (ADR 058) | 126-telegram-confirm-derivation-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L2: `go test -count=1 ./internal/channel/telegram/...` -> `ok (all tests pass)`. unit-test-only; no runtime surface. |
| 127 | `StateClarifying` registry state + route `MsgConfirm` — add `StateClarifying` constant to `internal/orchestrator/registry.go`; widen `routeCommand`/`handleCommand` to deliver `MsgConfirm` to goal mailboxes; widen drain loop to accept without panic; update `docs/spec/behaviors.md` lifecycle table (ADR 058) | 127-state-clarifying-route-msgconfirm-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L2: `go test -count=1 ./internal/cli/... ./internal/orchestrator/...` -> `ok (4 tests pass verifying MsgConfirm delivery, unknown goalID graceful report, and actor sweep consumption)`. |
| 128 | Clarifier seam + HeuristicClarifier v1 + intake state machine — `Clarifier` interface + `Clarification` type (new `clarifier.go`); `HeuristicClarifier` v1 (3 heuristics, no LLM); split `Handle` → `BeginGoal` + `ConfirmAndPlan` by extraction; Clarifying-linger loop in `goal_actor.go`; `AGENT_BUILDER_INTAKE=auto` escape hatch; `scripts/validate-orchestrate-intake.sh`; L5 hard-assertion conversation test TC-128-07 (ADR 058) | 128-clarifier-seam-intake-state-machine-test-spec.md | ✅ | ✅ | spec-verifier APPROVE + L5: `./scripts/validate-orchestrate-intake.sh` successfully exits 0 after validating both auto and interactive flows. |
| 129 | Approval-default — `AGENT_BUILDER_REQUIRE_APPROVAL` (default true) forces AwaitingApproval pause on policy-Allow plans; extract `pauseForApproval`; `WithRequireApproval` option; lenient-false opt-out; update `docs/spec/configuration.md` + `behaviors.md` (ADR 058) | 129-approval-default-require-approval-test-spec.md | complete | ✅ | **Verified by spec-verifier APPROVE.** L2: `go test -count=1 ./internal/cli/... ./internal/orchestrator/...` → `ok` (TC-129-01 `TestRequireApprovalDefaultIsTrueForPolicyAllow`: default is true, pauses; TC-129-02 `TestRequireApprovalFalseAutoDispatchesOnPolicyAllow`: false auto-dispatches; TC-129-03 `TestRequireApprovalDoesNotAffectPolicyDeny`: false does not affect deny; TC-129-04 `TestWithRequireApprovalOption` white-box option verification; TC-129-05 `TestAssembleOrchestrateReadsRequireApprovalEnv` environment variable parsing). L3: `make check` → `All checks passed.` + `All fitness checks passed.` Producer-consumer trace: `ConfirmAndPlan` require-approval check (producer) → `dispatchPlan` or `pauseForApproval` (consumer). |
| 130 | Escalation over the channel — `reporterStatusWriter` implements `loop.StatusWriter` via `Reporter.Report`; replaces `tasksource.NewStatusWriter` on orchestrate path; closes synthetic-goal-ID gap from task 123; update `docs/spec/behaviors.md` (ADR 058; prerequisite: task 123 merged) | 130-escalation-over-channel-test-spec.md | complete | ✅ | **Verified by spec-verifier APPROVE.** L2: `go test -race -count=1 ./internal/cli/...` → `ok` (TC-130-01 `TestReporterStatusWriterFormatsNeedsHumanLine`, TC-130-02 `TestReporterStatusWriterSyntheticGoalID`, TC-130-03 `TestReporterStatusWriterSatisfiesLoopStatusWriterInterface`). L3: `make check` → `All checks passed.` + `All fitness checks passed.` Producer-consumer trace: `ReevaluateBlocked` statusWriter (producer) → `reporterStatusWriter.WriteStatus` (consumer) → `Reporter` (outbound channel). |
| 131 | LLM clarifier (deferred — could-have) — `LLMClarifier` behind `AGENT_BUILDER_CLARIFIER=llm` reusing `Invoker` seam from task 100; `clarifierFromEnv`; F-010 preserved; do NOT start until tasks 124–130 are ✅ (ADR 058) | 131-llm-clarifier-test-spec.md | complete | 🟡 | **Code merged (🟡).** L2: `go test -v ./internal/orchestrator/planner/...` → `ok` (TC-131-01 `TestLLMClarifierCallsInvokerWithGoalText`, TC-131-02 `TestLLMClarifierParsesReadyResponse`, TC-131-03 `TestLLMClarifierParsesQuestionsFromResponse`), and `go test -v ./internal/cli/... -run TestAssembleOrchestrateSelectsLLMClarifierWhenEnvSet` → `ok` (TC-131-04 default/llm/unknown selection). L3: `make check` → `All checks passed.` + `All fitness checks passed.` |
| 132 | Gemini subscription/OAuth auth path — `GeminiCLI` subscription mode keyed on empty `SecretRef` (no `GEMINI_API_KEY`, CLI uses cached OAuth login); mirrors ADR 033; API-key path (task 090) unchanged; multi-LLM router gains the third brain | 132-gemini-subscription-oauth-auth-test-spec.md | done | 🟡 | L3: go test -race -count=1 ./internal/executor/... ./internal/runtime/... + make check green; ADR 056 written (self-managed OAuth login distinct from ADR 033); API-key regression TC-132-03/04 confirmed. **NOTE: the Gemini CLI backend was shut down 2026-06-18 — L6 unreachable; superseded operationally by Antigravity (task 133, `agy`).** |
| 133 | Antigravity (`agy`) executor harness — new `HarnessAntigravityCLI` driver invoking `agy --print` (agy is multi-model: Gemini/Claude/GPT-OSS); subscription/OAuth via `~/.antigravity` keyring (empty `SecretRef`); successor to the shut-down Gemini CLI; the real third brain | 133-antigravity-executor-harness-test-spec.md | done | ✅ | spec-verifier APPROVE (re-verified after BLOCK fix); merged ccb8400; ADR 057; fitness-supervisor-isolation passes. **L6 PASSED (2026-06-29, this host):** live `agent-builder run` — Antigravity (`agy` → `Gemini 3.1 Pro (High)`, subscription/OAuth, empty SecretRef) autonomously implemented task 001 on a Go calc target; produced branch `feature-subtract`; **all 7 gate steps PASS** (`go build`/`go vet`/`go test`/`gofmt -l`/`golangci-lint`/`dep-scan`/`code-scanner` — run record `gate passed: PASS …`); pushed to origin. Only `gh pr create` failed (local bare remote, by design — same as tasks 063/102–106). Verified jointly with task 134 (which supplies the commit). |
| 134 | Antigravity executor owns the branch + commit — agy edits but its git ops don't persist; executor does `git checkout -B && add -A && commit` (mirrors OllamaNative/task 106); `BRANCH:` advisory + `task/<id>` fallback; no-changes → error. Enables the agy L6. | 134-antigravity-executor-owns-commit-test-spec.md | done | ✅ | spec-verifier APPROVE; merged 81a3a5f; 11 TCs pass incl. `git show` commit verification; ADR 057 amended. **L6 PASSED (2026-06-29, this host):** the live `agent-builder run` (see task 133 row) produced committed branch `feature-subtract` — commit `agent-builder: complete task 001` by `agent-builder@local` (the executor-owned commit with the no-git-user fallback identity, **proving REQ-134-01/04 live**) containing `subtract.go` + `subtract_test.go`; all 7 gate steps PASS on that committed branch. Without this task agy produces no branch (its own git doesn't persist); with it, the agy harness reaches L6. |
| 135 | Claude single-shot completer — `CompleterForEntry(HarnessClaudeCLI)` returns a `claudeCompleter` running `claude -p <prompt>` (auth mirrors `NewClaudeCLIFromEntry`); first cloud brain on the general non-coding path (ADR 059) | 135-claude-single-shot-completer-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE.** L2: `go test -race ./internal/executor/...` → TC-135-01..05 pass (argv `[claude,-p,<prompt>]` + stdout `"Paris"`, auth cloud-vs-local env, token-redacted error, ctx cancel). L3: `make check` green. **L6 PASSED (2026-06-30, this host):** `agent-builder ask --entry claude-oauth "What is the capital of France?…"` → `Paris` (real `claude -p`, OAuth). Updated ADR-053-era `TestTC10905`. Spec: interfaces.md + SPEC.md. |
| 136 | agy single-shot completer — `CompleterForEntry(HarnessAntigravityCLI)` returns an `antigravityCompleter` running `agy --print <prompt> --model <model>` (subscription/OAuth, empty SecretRef); second cloud brain on the general path (ADR 059) | 136-agy-single-shot-completer-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE.** L2: `go test -race ./internal/executor/...` → TC-136-01..04 pass (`agy --print <prompt> --model <model>`, NO `--add-dir`/`--dangerously-skip-permissions`, stdout `"Paris"`). L3: `make check` green. **L6 PASSED (2026-06-30, this host):** `agent-builder ask --entry antigravity "What is the capital of France?…"` → `Paris` (real `agy --print`, Gemini 3.1 Pro, subscription/OAuth). Spec: interfaces.md. |
| 137 | `ask` CLI subcommand — `agent-builder ask [--entry <id>] <prompt>` selects a brain, constructs its `Completer`, prints the answer; first general (non-coding) entrypoint (ADR 059) | 137-ask-cli-subcommand-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE.** L2: `go test ./internal/cli/...` → TC-137-01..04 pass (registered + in usage; no-prompt → ExitUsage; stubbed completer prints `"Paris\n"` + ExitOK + fake got prompt/entry; unknown `--entry` → non-OK; unsupported-harness error surfaced, stdout empty). L3: `make check` green. **L6 PASSED (2026-06-30, this host):** `agent-builder ask --entry local-ollama "What is the capital of France?…"` → `Paris` (real qwen3:8b), and likewise for claude-oauth + antigravity — all three brains answered through `ask`. Spec: interfaces.md (CLI surface). |
| 138 | GoalAnalyzer seam + HeuristicGoalAnalyzer — classifies goal Kind (answer\|coding) + Complexity (simple\|complex) so the orchestrator can route (ADR 060); deterministic heuristic, no executor import | 138-goal-analyzer-seam-test-spec.md | complete | 🟡 | **Code merged (🟡).** L2: `go test -race ./internal/orchestrator/...` → TC-138-01..04 pass (kind table both directions, complexity, rationale). L3: `make check` green (F-010/F-014 intact). No flow change — consumed by task 139. |
| 139 | orchestrate answer route (single-turn) — `Answerer` seam + `KindAnswer` branch in `BeginGoal`: analyze → answer via Completer → report over channel; read-only (no clarifier/planner/gate/approval); `KindCoding` falls through unchanged (ADR 060) | 139-orchestrate-answer-route-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE (2026-06-30) + L6 live.** L2: `go test -race ./internal/orchestrator/...` → TC-139-01..03 (answer route: answerer gets spec+complexity, reporter gets answer, StateDone; coding fall-through: answerer NOT called, clarifier question, StateClarifying; no-answerer → StateFailed + clear report). L3: `make check` green (F-010/F-014 intact). **L6 PASSED (2026-06-30, this host):** `orchestrate` with `AGENT_BUILDER_GOAL_ANALYSIS=true` + local brain + `GOAL_SPEC="What is the capital of France?…"` → **`Paris`** reported over the channel (not "which repository?"). |
| 140 | orchestrate answer wiring — inject `GoalAnalyzer` + Completer-backed `cliAnswerer` into orchestrate assembly, opt-in via `AGENT_BUILDER_GOAL_ANALYSIS` (default off = pre-060 coding); `cliAnswerer` routes by complexity→capability (ADR 060) | 140-orchestrate-answer-wiring-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE (2026-06-30) + L6 live.** L2: `go test ./internal/cli/...` → TC-140-01 (env gate: enabling values→heuristic, empty/false→nil) + TC-140-02 (`cliAnswerer` satisfies `orchestrator.Answerer`). L3: `make check` green **with flag off — the full existing orchestrate suite is unaffected** (the reason for opt-in default). **L6 PASSED (2026-06-30):** same live run as task 139 (`Paris` over the channel via local brain). Spec: configuration.md (`AGENT_BUILDER_GOAL_ANALYSIS`), diagrams.md §5 note. |

| 141 | Multi-turn conversation for KindAnswer goals — `StateConversing` + goal-actor follow-up loop (`info`=follow-up via `ContinueAnswer`, `cancel`/EOF=end) + transcript composition; in-process per-goal history (ADR 060 §6) | 141-multi-turn-conversation-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE (2026-06-30) + L6 live.** L2 (`-race`): `go test -race ./internal/orchestrator/...` → TC-141-01..04 (`TestConversingAnswerAndFollowUp` — first answer→StateConversing+IsConversing, follow-up re-answers with prior turns in the prompt (context carried), history→4 turns, still conversing; `TestComposeTranscript` exact; `TestEndConversation`→StateDone). `answerGoal` now sets `StateConversing` (TC-139-01 updated). L3: `make check` green (F-010/F-014 intact). **L6 PASSED (2026-06-30, this host):** `orchestrate` + analysis-on + local brain — `GOAL_SPEC="capital of France?"`→**`Paris`**, `info q1 capital of Germany?`→**`Berlin`** (answered in-conversation), `cancel q1`→ended. Goal-actor linger + `applyInfo` `StateConversing` routing verified live. |
| 142 | LLM-backed GoalAnalyzer — `LLMGoalAnalyzer` via the Invoker seam, env-selected by `AGENT_BUILDER_GOAL_ANALYSIS=llm`; heuristic fallback on malformed output (ADR 060 §4) | 142-llm-goal-analyzer-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE (2026-06-30) + L6 live.** L2 unit tests: TC-142-01 (well-formed JSON responses parse into Kind/Complexity with case-insensitive normalization + JSON-in-prose extraction), TC-142-02 (malformed/invoke/resolve errors fall back to the heuristic result — asserts returned `GoalAnalysis` equals `NewHeuristicGoalAnalyzer().Analyze(goal)`, no error to intake), TC-142-03 (env selection: `llm`+seams→LLMGoalAnalyzer, `llm`+no-seams→fallback-to-heuristic, `heuristic`/truthy→heuristic, unset/false→nil). L3: `make check` → `All checks passed.` (89 go tests PASS, golangci-lint green, 14 fitness checks PASS including F-014: planner has no direct executor import). **L6 PASSED (2026-06-30, this host):** `orchestrate` + `AGENT_BUILDER_GOAL_ANALYSIS=llm` + live ollama `qwen3:8b` registry entry + `GOAL_SPEC="Write a short haiku about autumn leaves"` → classified **KindAnswer** and the haiku was reported over the channel; the same goal under `=heuristic` mis-routes to CODING (`"Which repository should I work on?"`) because `write` is a code-build verb — TC-142-04 confirmed. |
| 143 | Claude completer resolves subscription OAuth from disk — `DiskOAuthSecretSource` reads `${HOME}/.claude/.credentials.json` `claudeAiOauth.accessToken`; env→disk chain (env token wins, ADR 033); wired into the `HarnessClaudeCLI` cloud completer so `ask --entry claude-oauth` / the orchestrate answer route work off an on-disk subscription login without a hand-exported token; HOME isolation preserved (ADR 059) | 143-claude-completer-ondisk-oauth-test-spec.md | complete | ✅ | **spec-verifier APPROVE (2026-07-01, opus 4.8; all 6 TCs real assertions, 45 pkgs pass 0 fail) + L6 operator observation.** L2 unit tests: TC-143-01 (DiskOAuthSecretSource reads accessToken from .claude/.credentials.json), TC-143-02 (graceful absence: missing/malformed/empty → empty tokens, no error), TC-143-03 (ChainedSecretSource precedence: env>disk, disk only consulted when env empty), TC-143-04 (**live `CompleterForEntry` wiring**, internal-pkg stub: child env carries `CLAUDE_CODE_OAUTH_TOKEN=<diskToken>` + isolated `HOME` ≠ creds dir + token absent from argv + refresh token never read), TC-143-05 (stub CLI echoes raw disk token + exits non-zero → `sanitizeCLIOutput` redacts it from the returned error), TC-143-06 (local empty-`SecretRef` entry never consults disk). **Non-vacuous confirmed via mutation test**: reverting `completer.go` to bare `NewEnvSecretSource()` makes TC-143-04 and TC-143-05 FAIL; the chained source makes them pass. L3: `make check` → `All checks passed.` (89 go tests + 14 fitness checks green; F-010/F-014 intact). Amendment to ADR 059 documents the on-disk OAuth fallback decision + security rationale; `docs/spec/configuration.md` and `docs/spec/interfaces.md` updated with auth-resolution order. **Verified by (L6):** `go run ./cmd/agent-builder ask --entry claude-oauth "What is the capital of France? Reply with only the city name."` with `CLAUDE_CODE_OAUTH_TOKEN`/`ANTHROPIC_API_KEY` unset + real `~/.claude` subscription login → output `Paris` (2026-07-01, opus 4.8 coordinator; auth resolved from on-disk credential, no hand-exported token). |
| 144 | Claude executor + completer honor `entry.ModelID` via `--model` — `ClaudeCLI`/`claudeCompleter` pass `--model <ModelID>` when set (parity with codex/gemini/agy), omit it when empty (CLI default preserved); `NewClaudeCLIFromEntry` threads `ModelID` in both cloud + local branches (ADR 061) | 144-claude-honor-model-id-test-spec.md | complete | ✅ | **spec-verifier APPROVE (2026-07-01, opus 4.8; all 6 REQs, non-vacuous, mutation-confirmed on both files) + L6 operator observation.** L2: `go test ./internal/executor/ -run 'TestClaude\|TestNewClaudeCLIFromEntry'` → 24 pass (TC-144-01..06: argv `--model <id>` present when set / exactly `-p <prompt>` when empty, threaded through `NewClaudeCLIFromEntry` cloud+local, env/HOME undisturbed). Mutation: reverting the `--model` append in either `claude_cli.go` or `claude_completer.go` fails the argv tests. L3: `env PATH=/tmp/agent-builder-tools:$PATH make check` → `All checks passed.` (Go + 14/14 fitness). **L6 (2026-07-01, this host, on-disk `~/.claude` OAuth, no exported token):** `ask --entry claude-oauth` with `AGENT_BUILDER_REGISTRY_CLAUDE_OAUTH_MODEL=claude-haiku-4-5-20251001` → `Paris` (valid model accepted live); with `…_MODEL=bogus-model-xyz-does-not-exist` → real CLI error `There's an issue with the selected model (bogus-model-xyz-does-not-exist)` — proving `--model` is passed to and consumed by the live Claude CLI, not ignored. |
| 145 | Per-model registry entries + recipe capability-tier audit — define `claude-haiku`/`sonnet`/`opus` (+ agy Gemini levels) as env-loadable entries at tiers 1/2/3; router selects cheapest-sufficient by `MinCapability`; audit recipe tiers (ADR 061) | 145-per-model-registry-entries-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE (2026-07-01, sonnet; all 5 REQs + 4 TCs non-vacuous, loader sources tier/cost/model exclusively from env, router cheapest-sufficient confirmed via live `Select`) + L6 operator observation.** L2: `go test ./internal/registry/... ./internal/router/... ./internal/recipe/...` → TC-145-01 `TestLoadFromEnvPerModelClaudeEntries` (haiku/sonnet/opus load with correct harness/tier/cost/ModelID), TC-145-02 `TestRouterSelectsModelByTier` (MinCapability 1/2/3 → haiku/sonnet/opus), TC-145-03 `TestLoadFromEnvAgyModelLevels` (agy-gemini-flash/pro → HarnessAntigravityCLI, empty SECRET_REF accepted), TC-145-04 `TestDocsFixRoutingSpecMinCapability`/`TestAgentBuilderWorkerRoutingSpecMinCapability` (post-audit values 1/2 confirmed unchanged) — all pass. L3: `make check` → `All checks passed.` Spec: `docs/spec/configuration.md` (per-model tier↔model table + full env blocks). **L6 PASSED (2026-07-01, this host, on-disk `~/.claude` OAuth, no exported token):** (A) `ask --entry claude-haiku "capital of France…"` → **`Paris`** — newly-registered entry loads + dispatches live. (B) all three tiers enabled, tier-1 `claude-haiku` given `MODEL=bogus-haiku-xyz-does-not-exist` (tiers 2/3 valid); `ask` (no `--entry`, router MinCapability=1) → live CLI error **`There's an issue with the selected model (bogus-haiku-xyz-does-not-exist)`** — proving the router selected the cheapest-sufficient (tier-1) entry AND its `ModelID` reached the live Claude CLI via `--model`. |
| 146 | Dynamic model-tier selection step — extend the goal analyzer (ADR 060) to emit a `CapabilityTier` alongside `Kind`; wire it into the answer route's `RoutingSpec.MinCapability`; default when absent (ADR 061 §4) | 146-dynamic-model-tier-classifier-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE (2026-07-01, opus; all 5 REQs + 3 TCs non-vacuous, single tier→MinCapability source confirmed with the old complexity→capability mapping deleted, producer→consumer wire driven through the real build path) + L6 operator observation (cross-brain).** L2: `go test ./internal/orchestrator/... ./internal/orchestrator/planner/... ./internal/cli/...` → all `ok`. TC-146-01 `TestAnalyzerEmitsTierBySimpleGoal` (heuristic: simple→1, complex→2, design/architecture/security/auth/crypto/concurrency→3) + `TestHeuristicAnalyzerNeverEmitsUnsetTier` + `TestLLMGoalAnalyzerParsesWellFormedResponse` (LLM `"tier"` authoritative 1/2/3) + `TestLLMGoalAnalyzerFallbackOnMalformed` (missing/out-of-range tier falls back to heuristic tier); TC-146-02 `TestEmittedTierReachesRoutingSpec` (real producer `answerMinCapability` + real consumer `router.Select(RoutingSpec{MinCapability})`: tier N→floor N, tier 0→default 1); TC-146-03 `TestTierIndependentOfSensitivity` + `TestTierMinCapabilityIndependentOfSensitivity` (tier orthogonal to `SensitivityHint`) — all pass. L3: `make check` → `All checks passed.` (Go + 14/14 fitness, incl. F-014 planner-no-executor). **L6 PASSED (2026-07-01, this host, live `orchestrate` answer route, `AGENT_BUILDER_GOAL_ANALYSIS=heuristic`, on-disk `~/.claude` OAuth + `~/.antigravity` agy login):** (A) trivial goal *"capital of France?"* → heuristic **tier 1** → router MinCap=1 picked the cheapest tier-1 entry whose `MODEL=bogus-tier1` the live Claude CLI rejected by exact name — proving the LOW tier was selected. (B) **cross-brain** security goal *"security tradeoffs AES-GCM vs ChaCha20-Poly1305"* → heuristic **tier 3** → eligible {`claude-opus` tier3 cost10 (bogus model), `agy-gemini-pro` tier3 cost8 (Gemini 3.1 Pro High, valid)}; router picked the **cheaper valid `agy-gemini-pro`** and returned a real answer with **no `bogus-opus-tier3` error** — proving the emitted tier reached `MinCapability` AND cross-brain cheapest-sufficient selection (agy chosen over Claude opus at the same tier). Supporting: all 8 `agy models` (Gemini 3.5 Flash L/M/H, Gemini 3.1 Pro L/H, Claude Sonnet/Opus 4.6 Thinking, GPT-OSS 120B) dispatch live via the per-model registry entries (task 145). Spec: `interfaces.md` (GoalAnalysis + CapabilityTier), `behaviors.md` (B-037), `configuration.md`, `diagrams.md`. |
| 147 | Answer route terminates on source EOF — conversing-linger selects on `shutdown` (source drained) in addition to `ctx.Done()`, so a `KindAnswer` goal ends cleanly (`StateDone`) on a finite source instead of hanging until killed (ADR 060 §6, surfaced by task 146 L6) | 147-answer-route-terminates-on-source-eof-test-spec.md | complete | ✅ | **Verified — spec-verifier APPROVE (2026-07-01, sonnet; all 5 REQs + 4 TCs, mutation check independently reproduced) + L6 operator observation.** L2: `go test ./internal/cli/... ./internal/orchestrator/...` → `ok` (TC-147-01 `TestConversingGoalEndsOnShutdown`, TC-147-02 `TestControlLoopReturnsOnFiniteSourceAfterAnswer`, TC-147-03 `TestConversingGoalStillEndsOnCancel` all pass; TC-147-04 `TestConversingAnswerAndFollowUp` (task 141, unchanged) still passes). **Mutation-confirmed (reproduced by spec-verifier):** reverting the linger to `<-ctx.Done()` only makes TC-147-01/02 fail deterministically (3s/5s timeout-guarded, not a hang), TC-147-03 still passes (cancel path unaffected); restore → all pass. L3: `make check` → `All checks passed.` (Go + 14/14 fitness, F-014 intact). **L6 PASSED (2026-07-01, this host, real `~/.claude` OAuth + `~/.antigravity` agy login):** rebuilt binary, ran `AGENT_BUILDER_GOAL_SPEC="What is the capital of France?" AGENT_BUILDER_GOAL_ANALYSIS=heuristic … orchestrate < /dev/null` (finite source, reproducing task 146 OBS B) → answer **`Paris`** reported **and** process **exited 0 on its own in 3.9s** (`/usr/bin/time` Exit status: 0), no external kill — directly closing task 146 OBS B's `exit=124` hang. Fix: `internal/cli/goal_actor.go` conversing-linger now `select`s on `ctx.Done()` **or** the pre-existing `a.shutdown`. Spec: `behaviors.md` (B-037 conversation-lifecycle clause: ends on cancel or source drain). |
| 148 | `agent-cli keygen` — new `examples/agent-cli/` operator client (entrypoint + dispatcher scaffold together, ADR 062); generates operator Ed25519+X25519 and orchestrator Ed25519+X25519 keypairs, emits a paste-ready `AGENT_BUILDER_TELEGRAM_*` env block, writes a 0600 operator keyfile | 148-agent-cli-keygen-test-spec.md | ready | ✅ | spec-verifier APPROVE (commit 0a3b3d9, after 2 BLOCK/fix cycles — TC-148-02 upgraded to a real source-grep, TC-148-09 fixed from a vacuous cross-invocation compare to a same-invocation leakage check + mutation-tested: injected operator & orch leaks both tripped the assertions). L2: `go test -race -count=1 ./examples/agent-cli/...` → all 12 TCs (TC-148-01..09) pass. L3: `make check` → `All checks passed.` (Go lint + tests + fitness, examples/agent-cli has 0 issues). L6-lite (no live bot token yet): `go run ./examples/agent-cli keygen --keyfile /tmp/agent-cli-demo/operator.json` → stdout emits paste-ready env block (7 vars, all hex-encoded, no operator privates), stderr emits labeled banner + confirmation, keyfile at 0600 mode, JSON contains operator privates + orchestrator publics only. Spec: `docs/spec/interfaces.md` + `configuration.md` updated with keygen CLI signature + keyfile shape. |
| 149 | `agent-cli send` — seal+sign a plaintext command into an `envelope.Envelope` (operator→orchestrator) and POST it to the Telegram Bot API `sendMessage`; `--reply-to` threading; load-bearing round-trip through the real `envelope.VerifyAndOpen` with the live adapter's key roles | 149-agent-cli-send-test-spec.md | ready | ✅ | spec-verifier APPROVE (commit b03a236, after 1 BLOCK/fix cycle — TC-149-02 base64 negative-contrast pin added; verifier independently traced TC-149-01 key roles against adapter.go/confidentiality.go and independently mutation-tested TC-149-06 (token leak + plaintext-in-body leak both tripped, reverted) and TC-149-02 (crypto.go flipped to base64 → test failed, reverted)). L2: `go test -race -count=1 ./examples/agent-cli/...` → all 8 TC-149 tests pass (TC-149-01 load-bearing round-trip through live `envelope.VerifyAndOpen` with adapter's exact key roles recovers plaintext byte-for-byte; TC-149-02 hex encoding verified; TC-149-03 POST structure correct; TC-149-04/05 `--reply-to` threading + validation; TC-149-06 token+privates+plaintext absent from outputs, mutation-tested: injected token leak → assertion failed, injected plaintext leak → assertion failed, reverted; TC-149-07 keyfile errors fail-closed zero HTTP calls; TC-149-08 empty command rejected zero HTTP calls). L3: `make check` → `All checks passed.` (Go lint + tests + fitness). Spec: `docs/spec/interfaces.md` + `configuration.md` updated with `send` command signature + flags. Verification: awaiting spec-verifier APPROVE + L5/L6 evidence (live bot token + real `sendMessage` call observed). |
| 150 | `agent-cli reply-open` — decrypt+verify a sealed outbound reply envelope (orchestrator→operator, reverse key roles from task 149) from file/stdin/flag input; no `getUpdates` polling by design | 150-agent-cli-reply-open-test-spec.md | complete | ✅ | spec-verifier APPROVE (commit 4d2abb6, after 3 BLOCK/fix cycles — TC-150-01 rewritten to round-trip a REAL telegram.ReplyAdapter.Report emission (not hand-built); TC-150-04 classification fallback removed AND a real bug fixed: reply-open now classifies post-sentinel VerifyAndOpen failures as "decryption failed" instead of leaking the raw nacl error; internal/envelope untouched). Verifier independently mutation-tested TC-150-07 (key-leak), TC-150-04 (classification swap + raw-error re-leak) — all tripped and reverted clean. L2: go test -race -count=1 ./examples/agent-cli/... → all TC-150-01..08 + edge case pass. L3: make check → All checks passed. L6-lite: real ReplyAdapter-emitted envelope decrypted via `go run ./examples/agent-cli reply-open` → exact plaintext printed. |
| 151 | Telegram auth-mode config plumbing — `AGENT_BUILDER_TELEGRAM_AUTH_MODE`/`_APPROVED_STORE`, new `internal/channel/telegram/authz` persisted `0600` approved-sender store (load/persist/normalize/membership), `envelope`(unchanged default)/`disabled`(reject all)/`allowlist`(static-seeded plaintext accept) modes, armor+size-caps+audit retained on plaintext (ADR 063 Decision 1/2/4/5) | 151-telegram-authz-mode-plumbing-test-spec.md | complete | ✅ | Verified by spec-verifier APPROVE (per-assertion; all TC-151-01..12 backed by real, mutation-resistant assertions incl. negative `guard.calls==0`/`assertNoAuditReason` checks; live-wire confirmed `authMode`/`authStore` read at top of `Next()`). L2: `go test -count=1 ./internal/channel/telegram/... ./internal/cli/...` → `ok` (TC-151-01..12; `authz` store round-trip/normalize/isolation, adapter mode-branch TC-01..06, cli config TC-07/09/10). L3: `make check` → `All checks passed.` incl. F-003 (`fitness-supervisor-isolation`) + F-007 (`fitness-envelope-isolation`) PASS; a package-local source-grep test (`TestAuthzIsStdlibLeaf`) asserts the `authz` stdlib-only leaf invariant. Note: pre-existing unrelated `-race` flake in `TestTC128_06_ClarifyingLingerLoop` (goal_actor clarifier, present on `main`); non-`-race` suite green. L5/L6 residual: live-bot allowlist observation deferred (no bot token in env, per Verification plan). |
| 152 | Telegram `pairing` mode — in-chat owner-approve flow; `AGENT_BUILDER_TELEGRAM_OWNER_ID`; owner-gated `approve`/`deny <id>` grammar parsed before `deriveMessage` verb routing; approvals persist to task 151's store and survive a simulated restart (ADR 063 Decision 3/4 — the OpenClaw `dmPolicy` persistence-hole fix) | 152-telegram-pairing-mode-test-spec.md | complete | ✅ | Verified by spec-verifier APPROVE (per-assertion; both LOAD-BEARING cases adversarially scrutinized — TC-152-05 owner-identity gate proven ahead of verb routing at both the `authz.DecidePairing` seam and adapter level, TC-152-08 restart-survival via two genuinely independent adapter/store graphs + empty-path negative control). Highest reached: L3 `make check` → `All checks passed.` incl. F-003 (`fitness-supervisor-isolation`) + F-007 (`fitness-envelope-isolation`) PASS; `authz` stays stdlib-only leaf (`TestAuthzIsStdlibLeaf`). L2: `go test -race -count=1 ./internal/channel/telegram/...` → `ok`; `go test -count=1 ./internal/cli/...` → `ok` (TC-152-01..09; LOAD-BEARING TC-152-05 stranger-cannot-self-approve mutation-tested at both the `authz.DecidePairing` seam and the adapter level, TC-152-08 restart-survival via two independent adapter/store instances + empty-path negative control). Spec-marker grep clean (TC-152-01..09 each referenced by real assertions in `internal/channel/telegram/{adapter_152_test.go,authz/pairing_test.go}` + `internal/cli/orchestrate_152_test.go`). Pre-existing unrelated `-race` flake in `TestTC128_06_ClarifyingLingerLoop` (goal_actor clarifier, reproduces on `main`; diff touches no goal_actor code); non-`-race` cli suite green. L5/L6 residual: live-bot pairing observation deferred (no bot token in env, per Verification plan). |
| 153 | Telegram `open` mode + mandatory startup WARNING + docs — plaintext from any sender, reachable only by the exact literal `open` value; unconditional stderr warning naming the risk; closes out the ADR 063 mode-matrix documentation footprint | 153-telegram-open-mode-and-docs-test-spec.md | complete | ✅ | **Verified (✅) — spec-verifier APPROVE; the `ParseMode` exact-literal fix was itself mutation-tested by the verifier (reintroducing `TrimSpace` reproduced the fail-open bug and failed TC-153-03's padded-variant assertions), and L6 live-binary observation of the stderr WARNING was reached.** `authz.Decide`/`ParseMode` already recognized `ModeOpen` and its unconditional-accept branch from tasks 151/152; this task (a) fixed a genuine `ParseMode` whitespace-trimming gap that would have silently accepted `" open"`/`"open "` as `open` (now an exact, case-sensitive, whitespace-exact literal match — a padded/mis-cased near-miss is a fail-fast `ErrUnknownMode`, never a silent envelope fallback or an accidental open match); (b) added the mandatory unconditional `WARNING`-prefixed stderr line (`assembleTelegramInbound`/`inboundFromEnv` now take a `warnOut io.Writer`, wired from `config.Stderr` at the one real call site in `runOrchestrate`) emitted exactly once iff the resolved mode is `open`; (c) closed out the ADR 063 mode-matrix documentation in `configuration.md`, `behaviors.md` (B-038), and `diagrams.md`'s Telegram inbound auth-mode paragraph (present tense, no "lands in task 153" placeholder language remaining). TC-153-01/02 (new `internal/channel/telegram/adapter_153_test.go`): open mode accepts a never-before-seen sender ID (no store, no owner gate — proven with a nil `AuthStore`), asserts `ContentGuard.DecideContent` invoked and `MsgStatus`/`MsgNewGoal` derived correctly for two distinct unknown senders; oversized plaintext is still rejected before armor with the `text_too_long` audit reason (retained controls unweakened). TC-153-03 (new `internal/cli/orchestrate_153_test.go`): unset/blank still resolves to `envelope`; `"Open"`/`"OPEN"`/`" open"`/`"open "`/`"oPeN"` all fail assembly as `errUsageConfig` with no accidental resolution to `ModeOpen`. TC-153-04: full pre-existing 151/152/117 suites pass unmodified in behavior (signatures picked up a trailing `nil`/`warnOut` arg only). TC-153-05: `assembleTelegramInbound`/`inboundFromEnv` end-to-end — `open` emits exactly 1 `WARNING` line containing "any account ... command it"; `envelope`/`disabled`/`allowlist`/`pairing` emit 0; the warning fires even with an unrelated `APPROVED_STORE` var also set. TC-153-06: documentation review, confirmed present-tense with no "lands in task 153" placeholder remaining in any of the three spec files. **Verified by:** L2 `go test -race -count=1 ./internal/channel/telegram/... ./internal/cli/...` → all pass except the documented pre-existing unrelated `-race` flake `TestTC128_06_ClarifyingLingerLoop` (reproduces identically on `main`, outside this diff — confirmed by reproducing it directly on `main` before this task's changes); non-`-race` `go test -count=1 ./internal/channel/telegram/... ./internal/cli/...` → `ok` for both packages. L3: `make check` → `0 issues.` / `All checks passed.` incl. all fitness gates (F-003/F-007 supervisor/envelope isolation, `TestAuthzIsStdlibLeaf`) PASS. L6: built the real `cmd/agent-builder` binary and ran `orchestrate` with a full valid env (`AGENT_BUILDER_TELEGRAM_AUTH_MODE=open` + all required crypto/task-root/publish vars) — observed the exact live stderr line `WARNING: AGENT_BUILDER_TELEGRAM_AUTH_MODE=open — any account that finds the bot can command it (no sender-ID gate, no allowlist, no pairing approval). Plaintext only; armor/size-caps/audit are still enforced, but there is no gate on WHO can send commands.` printed exactly once before the (expected, sandboxed-network) `getUpdates` connection-refused error; re-ran with the mode unset (`envelope` default) on the same binary and confirmed 0 `WARNING` lines. Spec-marker grep clean (TC-153-01..06 each referenced — 04/06 via documented regression-guard/doc-review comments, per the test spec's own Notes). **spec-verifier APPROVE (per-assertion, all TC-153-01..06 real mutation-resistant assertions; ParseMode fix mutation-tested by the verifier; L6 live-binary WARNING observed).** |
| 154 | Classify AEAD/decryption failures with a sentinel in `internal/envelope` — new `ErrDecryptionFailed` wrapping `Open`'s AEAD failure; wired consistently through `internal/channel/telegram`'s adapter classifier, `internal/channel/worker`'s `Receiver.classify`, and `examples/agent-cli reply-open` (replaces its else-branch heuristic), closing the one gap task 097's sentinel pattern did not cover | 154-envelope-decryption-failure-sentinel-test-spec.md | complete | ✅ | spec-verifier APPROVE (commit 560b96a, after 1 BLOCK/fix cycle — added TC-154-04 ErrUnknownKey/ErrStaleTimestamp no-cross-contamination subtests + TC-154-07 source-grep for the explicit `errors.Is(err, envelope.ErrDecryptionFailed)` branch + task-file malformed-hex notes). Verifier independently ran 4 mutations (removed the `%w` wrap, the adapter branch, the worker branch, and reverted reply-open to the else-heuristic) — all tripped the correct tests, reverted clean; confirmed additive-only diff, existing 4 sentinel branches byte-for-byte unchanged, no stale `envelope_rejected` assertions. L3: `go test -race -count=1 ./internal/envelope/... ./internal/channel/telegram/... ./internal/channel/worker/... ./examples/agent-cli/...` all ok; `make fitness-envelope-isolation` + `make fitness-worker-transport-isolation` PASS unchanged; full `make check` passed. |
| 155 | Thread `context.Context` through `supervisor.Executor`/`supervisor.InBoxLoop` (Fix 1a, full-project review) — interface + plumbing change so a caller-cancelled context reaches the in-flight executor subprocess; sandboxBox/timeout wiring is task 156 | 155-executor-context-threading-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-155-01..08; verifier independently mutation-tested TC-155-06/07 — reverting `run.go` `retryingInBoxLoop.RunInside` to `context.Background()` failed both loudly ("executor never observed ctx.Done()"), and reverting `claude_cli.go`'s ctx forward failed TC-155-01/02 — all restored clean). Highest reached: L5 — `go test -race -count=1 -v ./internal/runtime/... -run 'TestTC155_0(6\|7)'` → `--- PASS: TestTC155_07_EndToEndCancellationReachesExecutor (0.20s)` — cancelled `Supervisor.Run(ctx)` reaches the in-flight executor through the REAL `Supervisor → retryingInBoxLoop → agentloop.RetryingLoop → Loop → Executor.Run` chain, `errors.Is(err, supervisor.ErrRunCancelled)` holds. L2: full `-race` run of executor/loop/tests-loop/supervisor/tests-supervisor/runtime/router all `ok`. L3: `make check` → `All checks passed.` incl. `PASS fitness-supervisor-isolation` + `PASS fitness-orchestrator-no-executor`. TC-155-06/07 live in `internal/runtime/context_threading_155_test.go` (not the spec's suggested `internal/supervisor/` path) because `retryingInBoxLoop` is unexported — judged by the verifier to satisfy the spec's real-production-chain intent. Merged to main at 4fc6fc8 (feat 0dacd3d). |
| 156 | Wall-clock timeout and cancel actually stop the running executor subprocess (Fix 1b) — supervisor-derived cancellable child context; fixes the false `cancel_test.go:41-43` comment and documents `sandboxBox.Kill`/`Teardown`'s no-op status accurately | 156-supervisor-cancel-timeout-stops-executor-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-156-01..08; verifier independently re-ran the mutation test — removing the timeout-arm `cancelRun()` makes TC-156-02 fail loudly at its 3s bound; restored clean — and independently reproduced the agentloop error-reformat that justified swapping the `errors.Is(context.Canceled)` assertion for the stronger `exec.observed` channel proof). Highest reached: L5 — `go test -race -count=1 -v ./internal/supervisor/... ./internal/runtime/... -run 'TestTC156'` → `--- PASS: TestTC156_02_WallClockTimeoutTerminatesInFlightExecutor (0.05s)` — a 50ms wall-clock timeout terminates an in-flight context-aware executor through the REAL `Supervisor → retryingInBoxLoop → agentloop → Executor.Run` chain (`ErrRunTimedOut`, no goroutine leak). Cancel-arm task-116 suites pass unmodified (TC-156-04). TC-156-05/06/07 promoted to machine-checked source-guard tests (false box.Kill comment gone ×2, accurate no-op docs present, stale ollama TODO gone). L3: `make check` → `All checks passed.` incl. `PASS fitness-supervisor-isolation`. TC-156-02/03 live in `internal/runtime` (F-003 forbids supervisor→runtime imports; task-155 precedent). Merged to main at 256ba88 (feat dc5977f). |
| 157 | Telegram `Adapter.Next` no longer terminates the control plane on an empty/rejected poll (Fix 2) — closes the remote-unauthenticated-DoS-by-one-junk-message finding and the idle-poll non-durability gap; `ok=false` reserved for genuine shutdown | 157-telegram-adapter-no-terminate-on-idle-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-157-01..09; verifier independently mutation-tested the load-bearing cases — reverting `Next()` to the pre-fix single-poll shape made TC-157-02/03/04/05 and the L5 TC-157-06/07 all FAIL with the exact "loop terminated on an idle poll"/"DoS reproduced" messages; restored clean — and confirmed the `Config.Ctx` wiring is live end-to-end, not a dead field: `runOrchestrate` ctx → `assembleOverrides.ctx` → `inboundFromEnv` → `assembleTelegramInbound` → `NewAdapter(Ctx:...)`, same ctx shared with `runControlLoop`). Highest reached: L5 — `go test -race -count=1 -v ./internal/cli/... -run TestTC157` → `--- PASS: TestTC157_06_ControlLoopSurvivesIdlePoll` / `TestTC157_07_ControlLoopSurvivesRejectedBatch` (the DoS finding) / `TestTC157_08_ControlLoopTerminatesOnShutdown`, real `runControlLoop` over a scripted stub Telegram server. L2: `go test -race -count=1 ./internal/channel/telegram/... -run TestTC157` → `ok` (TC-157-01..05). L3: `make check` → `All checks passed.` New `AGENT_BUILDER_TELEGRAM_POLL_BACKOFF` (default 1s) documented in configuration.md; behaviors.md B-040. Pre-existing unrelated `-race` flake `TestTC128_06_ClarifyingLingerLoop` independently reproduced on the merge-base by the verifier (not this task). Merged to main at afa47d2 (feat a3e1cd1). |
| 158 | Wire a real armor guard on the Telegram inbound path (Fix 3) — new `AGENT_BUILDER_TELEGRAM_ARMOR_BIN`; fail-closed assembly when a plaintext mode (allowlist/pairing/open) has no armor configured; envelope/disabled modes keep the documented fail-open default | 158-telegram-armor-guard-wiring-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-158-01..07; verifier independently re-ran the dead-wire mutation — neutering the `armorBin != ""` wiring branch kills TC-158-02 across envelope/allowlist/pairing/open; restored clean — and confirmed the `orchestrate_153_test.go` fixture change is additive (`/bin/true` armor bin to satisfy the new fail-closed gate), not a weakening; both feat commits together complete/consistent). Highest reached: L5 — real `assembleTelegramInbound`-constructed `Adapter.Next()` with a real `armor.NewGuard` (fake in-process runner) proves `armor_blocked` audit reason + message drop end-to-end (TC-158-02/05, both mutation-tested); no-armor + envelope/disabled keeps fail-open (TC-158-03); no-armor + allowlist/pairing/open fails assembly fast `errUsageConfig` naming `AGENT_BUILDER_TELEGRAM_ARMOR_BIN` (TC-158-04 — the fail-closed decision recorded in ADR 064). L3: `make check` → `All checks passed.` New env var documented in configuration.md; behaviors.md B-038 armor-retention claim now accurate. Pre-existing `-race` flake TestTC128_06 independently reproduced on main by the verifier (not this task). Merged to main at 1f8a1de (ADR 37e01bc, feat e5c0d51+6e1490f). |
| 159 | Seed the pairing-mode owner into the approved-sender store at startup (Fix 4) — closes the self-approval onboarding footgun; replaces `TestTC152_07`'s manual owner-preseed workaround with real-wiring coverage | 159-telegram-pairing-owner-seed-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-159-01..06; verifier independently mutation-tested three ways — deleting the seed block fails TC-159-01/02/04 with the exact expected messages, widening the gate to allowlist reproduces the leak TC-159-03 exists to catch; restored clean — and confirmed the ModePairing⇒ConsultsStore invariant rules out a nil-store deref). Highest reached: L5 — TC-159-04: owner's first plaintext command routes as `MsgStatus` with no `pairing_request` audit event through the REAL `assembleTelegramInbound`-constructed adapter against a scripted stub server, fresh store, zero pre-seeding; proven to FAIL against pre-fix code. Seed is additive + persisted (survives simulated restart alongside pre-existing IDs, TC-159-02); non-pairing modes provably unaffected (TC-159-03). TC-159-05 via spec option (b): package-level TestTC152_07 fixture comment corrected (points to TC-159-04 as production test of record), 152 assertions byte-identical. L3: `make check` → `All checks passed.` Merged to main at 369881a (feat e680f9e). |
| 160 | Wire `Router.OnGateFailure` into the live retry/escalation hook (Fix 5a) — replaces the constant `agentloop.BootstrapEscalationHook` on the live dispatch path so a gate-failing attempt climbs the capability ladder instead of blindly retrying the same executor | 160-router-gate-failure-escalation-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-160-01..08; verifier independently re-ran the mutation — patching the hook to return CurrentExecutor makes TC-160-02 fail `cheap.calls = 2, want 1` and TC-160-03 fail `[cheap cheap cheap]`; restored byte-identical — read the live `Run` → `buildRetryPolicy` wiring to confirm the router-backed hook replaced BootstrapEscalationHook on the production path, confirmed the `buildExecutorForEntry` test seam is t.Cleanup-restored, and confirmed the closure's currentEntryID sync reasoning against RunOnce). Highest reached: L5 — real `Run`/`buildRetryPolicy`/`resolveExecutor`/`RetryingLoop.RunOnce` chain on a fake multi-entry catalog: cheap gate-fails → strong succeeds (TC-160-02), three-entry ladder climbs `[cheap mid strong]` monotonically (TC-160-03), exhausted escalation degrades gracefully to needs-human (TC-160-04), no state leak across independent dispatches (TC-160-05), BootstrapEscalationHook unchanged (TC-160-06), single-entry path unaffected (TC-160-08). L3: `make check` → `All checks passed.` incl. `PASS fitness-orchestrator-no-executor` + `PASS F-014` (TC-160-07). Design note flagged by verifier (not a violation; honestly documented in behaviors.md): the hook escalates on ANY non-terminal failed attempt, not only FailureGate — follow-up task if strict gate-scoping is wanted. Merged to main at 33ccec6 (feat 27d34ed). |
| 161 | Record dispatch usage on the live path — `Router.RecordDispatch` wiring (Fix 5b) — every dispatch attempt now increments the selected entry's `Usage`, so a budgeted entry proactively exhausts and `Select` routes sideways | 161-router-record-dispatch-usage-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-161-01..06; verifier independently re-ran BOTH mutations — reverting the two recordDispatchWrap sites → `Usage = 0` failures in TC-161-01/02a/02b/03, double-firing → `Usage = 4/2` failures; restored clean — and confirmed exactly-once-per-attempt holds across same-executor retries by reading Loop.RunOnce/RetryingLoop). L2 (task's own verification-plan cap — usage-accounting wiring, no runtime surface): `dispatchRecordingExecutor` wrapper fires `RecordDispatch(entryID)` before delegating, wired at both live sites (`resolveExecutor` initial + `newRouterEscalationHook` re-selected executor); fires unconditionally on pass/gate-fail/executor-error; budgeted entry exhausts at Budget.Limit and the next `Select` routes sideways (TC-161-03, via the spec's sanctioned direct-exec.Run shape); unlimited + zero-registry synthetic paths unaffected (TC-161-04/05), all through the real `driveRetry`→`buildRetryPolicy` chain. L3: `make check` → `All checks passed.` Merged to main at f6166db (feat ff9761f). |
| 162 | Persist and load router quota state across process invocations — `Router.SaveState`/`LoadState` wiring (Fix 5c) — new `AGENT_BUILDER_ROUTER_STATE_PATH`; closes "quota state is never recorded/persisted" | 162-router-quota-state-persistence-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-162-01..07 + extra TC-162-03b save-on-dispatch-failure; verifier independently re-ran BOTH mutations — neutering SaveState fails TC-162-03/03b/06 with the exact predicted `aFake.calls = 2, want still 1`, neutering LoadState fails TC-162-04/06; restored clean — and confirmed `Run` calls the router-returning `buildRetryPolicyAndRouter` directly, single implementation, run_160/161 helper files byte-identical per REQ-162-07). Highest reached: L5 — TC-162-06: two sequential, independently-constructed real `Run` invocations sharing one on-disk state file; exhaustion recorded by invocation 1 is observed at invocation 2's FIRST `Select` (entry A never re-selected, B serves). `AGENT_BUILDER_ROUTER_STATE_PATH` opt-in: unset = byte-for-byte pre-task behavior, no file created, no cross-invocation leak (TC-162-05); missing file tolerated (`os.ErrNotExist` only), corrupted file fail-fast before any Select/box construction (TC-162-04); SaveState fires unconditionally of dispatch outcome. L3: `make check` → `All checks passed.` Merged to main at 8d490cb (feat 846ceea). |
| 163 | Low-severity hardening batch — audit chain logfile `0o644`→`0o600`; Telegram inbound envelope `env.From`/`env.To` role assertion (mirrors `worker/transport.go`'s pattern, task 098 SEC-001); `audit.ErrBlockEmitFailed` sentinel now genuinely wrapped/matchable | 163-low-severity-hardening-batch-test-spec.md | ✅ | ✅ | spec-verifier APPROVE (per-assertion TC-163-01..06 + bonus negative control; verifier independently re-ran two mutations — removing the role-check block fails TC-163-03/04, removing the `%w` wrap fails TC-163-05; restored clean — confirmed the split-commit anomaly (c88d5c6+0c9cb20) yields one complete, consistent changeset, and judged the 0o600-literal redundancy acceptable since the unconditional `os.Chmod` is the always-executed guarantee the tests assert). L2/L3 (task's own verification-plan cap — no runtime surface): audit chain logfile created 0600 with pre-existing 0644 files tightened via unconditional chmod at the `requireWritable` call site, content preserved (TC-163-01/02); a validly-sealed role-swapped envelope is rejected by the real adapter immediately after `VerifyAndOpen` with exactly one `ActionChannelReject` event, `Reason == "role_mismatch"` (not `envelope_rejected`), armor never invoked, mirroring the worker transport's SEC-001 pattern (TC-163-03/04); `errors.Is(err, audit.ErrBlockEmitFailed)` matches a real emit failure and NOT a malformed-response failure (TC-163-05). L3: `make check` → `All checks passed.` Merged to main at 36d8fb9 (feat c88d5c6+0c9cb20). |
| 164 | Fail-closed validation of the `tier_select` obligation value: new `sandbox.TierBubblewrap`/`sandbox.TierGvisor`/`sandbox.ValidTier`; `decideGate`'s `DecisionAllow` branch now rejects an unrecognized tier before returning an allowed outcome, closing the residual gap tasks 072/073 left (the VALUE of `tier_select` was never validated before reaching the exec-sandbox block) | 164-sandbox-tier-select-fail-closed-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/sandbox/... ./internal/runtime/... -run 'TestValidTier\|TestTC164'` → `ok github.com/tkdtaylor/agent-builder/internal/sandbox 1.013s` / `ok github.com/tkdtaylor/agent-builder/internal/runtime 1.012s` (TC-164-01/02/03 pass; `TestValidTier` table over empty/bubblewrap/gvisor/wrong-case/`runsc`-alias/nonsense/whitespace, `TestTC164DecideGateTierValidation` pins both valid- and invalid-tier `gateOutcome` shapes, `TestTC164MaybeEmitPolicyDecisionOnHaltedTierOutcome` confirms the audit event still names the engine's `allow` decision). L5: `go test -race -count=1 -v ./tests/e2e/... -run 'TestPolicyGateFakeBinaryE2E\|TestPolicyGateUnknownTierHalts'` → `--- PASS: TestPolicyGateUnknownTierHalts` (new sibling test, TC-164-04: an `allow` decision naming `tier_select=quantum-tier` halts with `"run halted"`/`"unknown tier"`/`"quantum-tier"` in stdout, zero `REQUEST` occurrences in the fake block's log, task marked `needs-human`) and `--- PASS: TestPolicyGateFakeBinaryE2E/TC-072-03_allow_tier_select_starts_box_with_tier` unchanged (TC-164-05 regression). L3: `make check` → `All checks passed.` Note: `go test -race ./internal/sandbox/podman/...` (unfiltered) hits a pre-existing `-race` flake in `TestPodmanRunnerWallClockTimeout`, independently reproduced on `main` before this task's changes (untouched package); `make check`'s non-`-race` `go test ./...` is unaffected. Spec-marker grep clean (TC-164-01..06 each referenced by real assertions). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion evidence TC-164-01..06) + L5 harness commands above (`make check` green). |
| 165 | Add `Client.ValidateRead` to `internal/memoryguard`, the memory-guard block's third IPC verb (`validate_read`), mirroring the existing `ValidateWrite`/`VerifyDelete` wire pattern; pure leaf-package addition, no caller wiring in this task | 165-memoryguard-validate-read-verb-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/memoryguard/... -run TestValidateRead -v` → `ok github.com/tkdtaylor/agent-builder/internal/memoryguard` (TC-165-01..05 all PASS: wire request field-by-field, allow=true redacted-content passthrough, allow=false → `ErrReadGateDenied` with flags preserved, transport error wrapped with unwrap reaching the original error, malformed-JSON parse error contains raw bytes). L3: `make fitness-memoryguard-isolation` → `PASS fitness-memoryguard-isolation: internal/memoryguard import graph contains no other agent-builder/internal packages.`; `make check` → `All checks passed.` Spec updated: `interfaces.md` (`internal/memoryguard` adapter section gains `ErrReadGateDenied`, `ValidateRead`, and the `validate_read` IPC contract line). No wiring into `MemoryGuardStore[P].Get` or any caller (task 172's job); no runtime-observable surface of its own. Verified by: spec-verifier APPROVE (2026-07-12, TC-165-01..07 field-by-field + error-chain assertions) + L3 fitness/unit green. |
| 166 | Distinguish a policy `Decide` transport/parse failure from a genuine deny — `decideGate` captures the previously discarded `Decide` error, produces a distinct halt reason (`"policy: decide call failed, fail-closed to deny: …"`), and emits an `ActionPolicyDecision` audit event UNCONDITIONALLY (via new `gateOutcome.transportFailure`/`classifyReason`) with `Detail.Reason == "policy_transport_error"`, closing the one fail-closed-deny outcome that could never produce an audit trail | 166-audit-policy-transport-failure-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/runtime/... -run TestTC166` → `ok` (TC-166-01 distinct reason via production `newTransportFailureOutcome`; TC-166-03 exactly-one unconditional emission despite `auditEmit==false`, mutation-verified — reverting the gate to `!auditEmit` yields `count = 0`; TC-166-03 negative-control: obligation-driven emission leaves `Detail.Reason` empty; TC-166-04 nil-sink no panic). L5: `go test -race -count=1 -v ./tests/e2e/... -run TestPolicyGateTransportFailureAudited` → `--- PASS` (unparseable fake-engine response → halt names "decide call failed", never "decision denied"; task `needs-human`; box never started). L3: `make check` → `All checks passed.` Spec updated: `data-model.md` (Reason gains `policy_transport_error`), `behaviors.md` (new B-041). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion TC-166-01..07, independently re-ran the emission mutation and full regression) + L5 harness above. |
| 167 | `internal/runstore` — a new stdlib-only leaf durable run journal (ADR 065): append-only JSONL with snapshot/compaction, crash-safe temp+rename + fsync writes; `Store`/`FileStore` with `Save`/`Load`/`ListInFlight`/`Delete`/`Compact`; recording goal/plan/attempt-state/pending-approvals/terminal-status. Storage primitive only, no orchestrator wiring (task 168). New F-015 fitness (`make fitness-runstore-isolation`) | 167-runstore-run-journal-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/runstore/...` → `ok` (TC-167-01 JSON round-trip incl. Plan raw bytes + status wire values; TC-167-02 `var _ Store = (*FileStore)(nil)` + Save/Load; TC-167-03 one 0600 fsync'd line; TC-167-04 50 concurrent Saves, no torn lines, 50 in-flight, `-race` clean; TC-167-05 last-write-wins, 2 journal lines; TC-167-06 terminal exclusion + sorted; TC-167-07 truncated-final-line tolerated; TC-167-08 mid-file corruption fails loud naming file; TC-167-09 compact truncates+snapshots; TC-167-10 rename-failure leaves both files byte-for-byte + temp cleaned; TC-167-13 delete tombstone survives replay). L5: `go test -race -count=1 -v ./internal/runstore/... -run TestTC167_11` → cross-construction durability (store2 reflects store1's fsync'd writes). L3: `make fitness-runstore-isolation` PASS + negative-fixture (temp orchestrator import → Error 1); `make check` → `All checks passed.` Spec updated: `fitness-functions.md` (F-015), `interfaces.md` (runstore seam), `data-model.md` (Record/AttemptState/PendingApproval/Status). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion TC-167-01..14, independently re-ran the L5 cross-construction proof + `make check` green, confirmed the F-015 grep-exclude mechanism by direct `go list -deps` inspection). |
| 168 | Wire `internal/orchestrator` to the durable run journal (ADR 065): `WithRunStore` persists a `runstore.Record` on plan admission + per-sub-goal `AttemptState` (running→completed/failed, RMW guarded by an orchestrator mutex across concurrent goroutines); `RehydrateInFlight` wraps `ListInFlight`; `ResumeFromRecord` re-drives the shared `dispatchSubGoals` path for ONLY non-completed sub-goals (idempotent resume). Opt-in: unset = byte-for-byte unchanged | 168-resume-after-restart-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/orchestrator/... -run TestTC168` → `ok` (TC-168-01 admission persists Running Record + round-trip Plan; TC-168-02 two attempts both Completed, SAME entry updated no dup; TC-168-03 failed attempt = StatusFailed + non-empty Detail, sibling Completed; TC-168-04 RehydrateInFlight == ListInFlight; TC-168-05 skips completed sub-a, dispatches b/c only, total=2 — mutation-verified: removing the completed-filter dispatches sub-a → count 3 FAIL; TC-168-06 re-dispatches running-and-crashed sub-b + sub-c, never completed sub-a; TC-168-08 RunStore unset dispatches both sub-goals unchanged). L5: `go test -race -count=1 -v ./internal/orchestrator/... -run TestTC168_07` → two independently-constructed Orchestrator+FileStore pairs on one dir: orch1 completes sub-a then fails b/c, orch2 rehydrates + resumes b/c ONLY (sub-a never re-dispatched), post-resume Status=Completed, all 3 completed exactly once. Full regression `./internal/orchestrator/... ./internal/runstore/...` `-race` green (REQ-168-06). L3: `make check` → `All checks passed.` Spec updated: `interfaces.md` (WithRunStore/RehydrateInFlight/ResumeFromRecord), `behaviors.md` (B-042), `diagrams.md` (§5 run-journal overlay). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion TC-168-01..09; independently mutation-tested — removing the completed-filter fails TC-168-05/06/07, removing the `persistPlanRecord` nil-guard panics TC-168-08 — confirming both idempotency and the opt-in guard are load-bearing). |
| 169 | Bounded goal-level re-plan loop (ADR 065): `Orchestrator.RunToCompletion(ctx, goal, maxAttempts)` folds a terminal sub-goal failure into the goal via `FoldGoalText` and re-plans, up to a RunStore-persisted attempt budget (`runstore.Record.Attempt`), escalating once over the Reporter on exhaustion (`ErrGoalAttemptsExhausted`). New `AGENT_BUILDER_GOAL_MAX_ATTEMPTS` (default 3, strict parse). `PlanResult.HasTerminalFailure`/`FailureDetails` helpers | 169-sustained-autonomy-loop-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/orchestrator/... ./internal/cli/... -run TestTC169` → `ok` (TC-169-01 parse: unset→3, "5"→5, "abc"/"0"→errUsageConfig, + assembleOrchestrate threads/validates the value; TC-169-02 immediate success = 1 planner call, no escalation; TC-169-03 failure folds "gate failure: lint" into the re-plan goal, 2 planner calls, success; TC-169-05 exhaustion = exactly 1 Reporter message naming goal ID + "2" + "exhausted" + `errors.Is(ErrGoalAttemptsExhausted)`; TC-169-06 same mechanics without RunStore). L5: `go test -race -count=1 -v ./internal/orchestrator/... -run TestTC169_04` → two independently-constructed Orchestrator+FileStore pairs: orch1 exhausts a 2-budget (Attempt=2 persisted), orch2 on the same dir resumes at attempt 3 (1 planner call, Attempt→3, NOT reset), sub-case: last-remaining-attempt-fails exhausts immediately — mutation-verified (loadAttemptCount→0 fails the resume sub-case). L3: `make check` → `All checks passed.` Note: pre-existing unrelated `-race` flake in `TestTC128_06_ClarifyingLingerLoop` (goal_actor.go, task 128) reproduces on clean main; not introduced here; the non-race `make check` gate is green. Spec updated: `configuration.md` (`AGENT_BUILDER_GOAL_MAX_ATTEMPTS`), `interfaces.md` (`RunToCompletion`/`ErrGoalAttemptsExhausted`/PlanResult helpers), `data-model.md` (`Record.Attempt`), `behaviors.md` (B-043). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion TC-169-01..07; independently mutation-confirmed the crash-survival resume — stubbing loadAttemptCount→0 fails TC-169-04 main + sub-case — and confirmed the TC128_06 -race flake is pre-existing on clean main, unrelated). |
| 170 | Sub-goal-level `require_approval` pause recorded in RunStore (ADR 065): new `runtime.Config.OnRequireApproval` hook fired from `Run`'s require_approval branch (only, never a genuine deny) BEFORE `return nil`; orchestrator supplies it when a RunStore is configured, persisting a `runstore.PendingApproval` + `Record.Status=AwaitingApproval`; `dispatchSubGoals` halts further not-yet-started sub-goals once a pause is recorded (checked inside the held worker semaphore). REQ-073-01 return contract unchanged | 170-approval-pause-on-require-approval-test-spec.md | ✅ | ✅ | L5: `go test -count=1 -run TestTC170_02 ./tests/e2e/...` → in-process `runtime.Run` against the REAL fake-policy-engine daemon returning require_approval: hook fires exactly once with task ID "001" + reason containing "approval", Run returns nil; nil-hook sub-case no panic. L2: `go test -race -count=1 ./internal/orchestrator/... -run TestTC170` → `ok` (TC-170-03 Pending persisted {TaskID, Reason} + Status=AwaitingApproval; TC-170-04 RequestedAt within Handle window; TC-170-05 single-worker semaphore: dispatch called exactly 1×, sub-goals 2/3 never dispatched, mutation-verified — removing the `planAwaitingApproval` halt makes dispatch fire 3× FAIL; TC-170-06 RunStore unset → dispatch's `cfg.OnRequireApproval` is nil). Regression: `TestPolicyGateRequireApprovalDistinctFromDeny` + `TestPolicyGateFakeBinaryE2E` + `TestRequireApprovalStatusReason` byte-identical (TC-170-07). L3: `make check` → `All checks passed.` Spec updated: `interfaces.md` (`runtime.Config.OnRequireApproval`), `behaviors.md` (B-044). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion TC-170-01..08; independently confirmed the L5 real-daemon hook fire, mutation-verified the pause-halt, and confirmed REQ-073-01/plan-level pauseForApproval untouched). |
| 171 | Route pending approvals over the channel (ADR 065): new `approve`/`deny <goalID> <taskID>` verbs in both derivation sites (`parseMessageLine` stdin + Telegram `deriveMessage` reply-to), new `MsgApprove`/`MsgDeny` + `Message.TaskID`; `Orchestrator.ResumeApproval` re-dispatches (approve) or marks needs-human + finalizes (deny); `SweepApprovalTimeouts` once-only timeout escalation (new `PendingApproval.Escalated`); `goalActor.applyApproval` routing; new `AGENT_BUILDER_APPROVAL_TIMEOUT` (default 1h). Twin-merge of tasks 125/126 pattern | 171-approval-routing-and-resume-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/cli/... ./internal/channel/telegram/... ./internal/orchestrator/... -run TestTC171` → `ok` (TC-171-01 stdin approve/deny → {Kind,GoalID,TaskID}; TC-171-02 missing-token → ErrMalformedInput, never new-goal; TC-171-03 Telegram reply-to `approve task-3`/`deny task-9` → MsgApprove/MsgDeny with threaded goalID; TC-171-04 standalone approve → MsgNewGoal; TC-171-05 approve re-dispatches paused task-3 only → Status Completed; TC-171-06 deny → task-3 needs-human, Status Failed, finalization report sent; TC-171-07 sweep escalates once naming goal+task, second sweep idempotent via Escalated flag, AND live-wired: `handleCommand` runs the on-check `SweepApprovalTimeouts` so an overdue pending escalates on any processed command (TestTC171_07_HandleCommandTriggersTimeoutSweep); TC-171-08 goalActor.handleCommand(MsgApprove) → ResumeApproval re-dispatches). Regression: full `router_test`/`adapter_test`/orchestrator suites byte-identical (TC-171-09). L3: `make check` → `All checks passed.` Spec updated: `interfaces.md` (grammar + `MsgApprove`/`MsgDeny`/`Message.TaskID` + `ResumeApproval`/`SweepApprovalTimeouts`), `configuration.md` (`AGENT_BUILDER_APPROVAL_TIMEOUT`), `data-model.md` (`PendingApproval.Escalated`), `behaviors.md` (B-045). Note: executed whole in one session (not split into 171a/171b); timeout sweep wired live into `handleCommand` (not deferred). Verified by: spec-verifier APPROVE (2026-07-12; initial BLOCK on TC-171-07 dead-wire resolved by wiring `SweepApprovalTimeouts` live into `goalActor.handleCommand` with a test routing an unrelated message kind to isolate the sweep path; per-assertion TC-171-01..10 confirmed, TC128_06 -race flake confirmed pre-existing/unrelated). |

## Status key

| Symbol | Meaning |
|--------|---------|
| ✅ | **Verified** — validation harness exercised the live runtime path, or operator observed the targeted behaviour |
| 🟡 | **Code merged** — feat-commit landed, unit tests + fitness + CI green, but runtime/live behaviour not yet observed |
| ⏳ | In progress |
| ◻️ | Planned / not started |
| ❌ | Not started |
| ⚠️ | Blocked |

## Verification ladder

A task earns 🟡 at levels 1–4 and ✅ only at level 5 or 6. The `Verified by` column records which level the row reached.

| Level | Evidence | Status this earns |
|-------|----------|-------------------|
| 1 | Code merged | 🟡 |
| 2 | Unit tests pass (paste verbatim final line of `make check`) | 🟡 |
| 3 | `make fitness` passes (verbatim closing line) | 🟡 |
| 4 | CI passes (`gh run watch <id> --exit-status` → success) | 🟡 |
| 5 | **Validation harness** exercises the live runtime path end-to-end — paste the command and the final assertion line | ✅ |
| 6 | **Operator-observed** — operator (or executor via `cargo run` / `npm start` / etc.) saw the targeted behaviour in stdout / logs / UI | ✅ |

If the task targets runtime-observable behaviour (logging, CLI args, TUI, server endpoints, file outputs, side effects), level 5 or 6 is **required** before flipping to ✅. If the task only adds an internal helper covered by unit tests, level 2 may be sufficient — but in that case the row's `Verified by` should explicitly say "unit-test-only; no runtime surface" so future readers don't mistake silence for verification.

## Rule

**The task-executor commits at 🟡 by default.** Only the main session (after spec-verifier APPROVE + the appropriate level-5/6 evidence) updates the row to ✅, in a separate commit titled `verify: confirm task NNN — <level-5/6 evidence>`. This keeps the verification step visible in git history and prevents "merged ≠ done" drift.
| 172 | `DurableStore[P]` in `internal/memoryguard` (ADR 065): a memory-guard-gated, crash-safe cross-session store. Every write ValidateWrite-gated + durably persisted (append-only fsync'd JSONL + temp+rename snapshot, runstore's journal mechanics reimplemented locally since memoryguard is a stdlib-only leaf that can't import runstore); every read ValidateRead-gated + fails closed (never returns a rejected cached value); Delete VerifyDelete + durable tombstone. Sibling to MemoryGuardStore, unmodified |  172-persistent-memory-store-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/memoryguard/... -run TestTC172` → `ok` (TC-172-01 round-trip + 0600 journal; TC-172-02 write-gate denial → ErrWriteGateDenied, 0 journal lines; TC-172-03 allow → 2 durable lines; TC-172-04 read-gate denial → zero value never the cached "v1", mutation-verified: bypassing the ValidateRead error returns v1 → FAIL; TC-172-05 allow returns durable value, unknown key skips the gate via stub call-count; TC-172-06 tamper drops the in-process entry). L5: `go test -race -count=1 -v ./internal/memoryguard/... -run 'TestTC172_07|TestTC172_08'` → two independently-constructed DurableStore on one dir: TC-172-07 tombstone survives reconstruction, TC-172-08 writes survive reconstruction. L3: `make fitness-memoryguard-isolation` PASS (F-012 unaffected); `make check` → `All checks passed.` Spec updated: `interfaces.md` (`DurableStore[P]`), `data-model.md` (independent-reimpl note). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion TC-172-01..10; independently mutation-tested BOTH load-bearing assertions — write-gate-before-disk (TC-172-02) and read-gate fail-closed (TC-172-04)). |
| 173 | Back `orchestrator.PlanStore` with the durable, read-gated memory store (ADR 065): `MemoryGuardPlanStore.inner` swapped from `MemoryGuardStore[Plan]` (in-process, read-ungated) to `DurableStore[Plan]` (task 172); constructors gain `dir` + return error; `NewPlanStoreFromEnv` returns error + resolves `AGENT_BUILDER_PLAN_STORE_DIR`; `Get` now read-gated (denial → not-found, never leaks the plan); added `DurableStore.StoredID` |  173-planstore-on-persistent-memory-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/orchestrator/... -run TestTC173` → `ok` (TC-173-01 both constructors + PlanStore/TamperAwarePlanStore conformance; TC-173-02 read-gate denial → Get returns (Plan{}, false), never the stored plan; TC-173-03 allow → returns the plan; TC-173-06 unset → MemoryPlanStore + warning, nil error). L5: `go test -race -count=1 -v ./internal/orchestrator/... -run TestTC173_05` → two independently-constructed MemoryGuardPlanStore on one dir: store2 reflects store1's plan field-for-field (survives restart). TC-173-04 regression: task-084 Resume tests updated for the new read-gate (Resume's Get now emits `validate_read`; op sequence [validate_write, validate_read, verify_delete]) + constructor signature, byte-identical outcomes otherwise. L3: `make check` → `All checks passed.` Spec updated: `configuration.md` (`AGENT_BUILDER_PLAN_STORE_DIR` + MEMORY_GUARD_BIN read-gate/durability note), `architecture.md` (memory-guard adapter durability+read-gate), `interfaces.md` (`DurableStore.StoredID` + MemoryGuardPlanStore signatures). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion TC-173-01..07; confirmed the read-gate fail-closed, the two task-084 Resume-test adaptations are minimal + consistent with the new gating, and the cli TC128_06 -race flake is pre-existing/unrelated). |
| 174 | `agent-builder daemon` (ADR 065): long-lived control-plane process reusing `assembleOrchestrate`/`runControlLoop` verbatim, with a single-instance `O_CREATE\|O_EXCL` PID lock (`AGENT_BUILDER_DAEMON_LOCK`), graceful `signal.NotifyContext` SIGINT/SIGTERM shutdown, and startup rehydration of in-flight runs (`RehydrateInFlight`/`ResumeFromRecord`) via the newly-wired `AGENT_BUILDER_RUN_STORE_DIR` run journal |  174-daemon-mode-test-spec.md | ✅ | ✅ | L2: `go test -race -count=1 ./internal/cli/... -run TestTC174` → `ok` (TC-174-01 Main dispatches daemon + usage; TC-174-02 pre-existing lock → non-zero, names path, loop never invoked, lock untouched; TC-174-03 lock acquired + held during loop; TC-174-04 ctx-cancel → ExitOK + lock removed; TC-174-05 assembly error → non-zero + lock removed; TC-174-07 RehydrateInFlight fires with the seeded record BEFORE the loop, ordering via shared log; TC-174-08 structural: exactly one `assembleOrchestrate(` call, no forked assembly). L5: `go test -count=1 -run TestDaemonGracefulShutdown ./tests/e2e/...` → real `agent-builder daemon` subprocess with a fake idle Telegram getUpdates server: SIGTERM → graceful-shutdown message + exit 0 + lock removed within 5s. L3: `make check` → `All checks passed.` Spec updated: `configuration.md` (`AGENT_BUILDER_DAEMON_LOCK` + `AGENT_BUILDER_RUN_STORE_DIR`), `interfaces.md` (`daemon` command row), `operating.md` (Running as a daemon). Note: env/stdin source is not ctx-aware (blocks stdin); clean SIGTERM shutdown is via the context-aware Telegram channel (documented). Verified by: spec-verifier APPROVE (2026-07-12, per-assertion TC-174-01..10; confirmed the runStore wiring is opt-in/guarded, and the TC128_06 -race flake is pre-existing on unchanged code via git diff). |
| 175 | Config-declared interval/daily scheduler (ADR 065) firing inside `agent-builder daemon`: `AGENT_BUILDER_SCHEDULE_PATH` JSON (`every`/`at` entries, fail-fast on malformed); stdlib-only `Scheduler` with an injectable `Clock` seam; fired goals route through the SAME control-loop intake via a merged message source (no parallel dispatch); clean shutdown on ctx-cancel | 175-scheduled-goals-test-spec.md | ✅ | 🟡 | L2: `go test -race -count=1 ./internal/cli/... -run TestTC175` → `ok` (TC-175-01 parse every+at; TC-175-02 four malformed files → error; TC-175-03 every:10m fires 3× at 10/20/30m not the trailing 5m via fake clock; TC-175-04 at:03:00 fires once per day-boundary across 2 days = 2; TC-175-05 fired entry reaches the real Orchestrator's Planner through `assembleOrchestrate`+`runControlLoop`+merged source, goal Spec + `sched-0-<RFC3339>` ID; TC-175-06 unset schedule path starts 0 schedulers, TC-175-06b set → exactly 1, via `daemonOnSchedulerStarted` hook; TC-175-07 scheduler goroutine exits within 2s of ctx-cancel via `Done()`). L3: `make check` → `All checks passed.` Spec updated: `configuration.md` (`AGENT_BUILDER_SCHEDULE_PATH` + file format), `operating.md` (Scheduled goals section). Awaiting spec-verifier for ✅. |
